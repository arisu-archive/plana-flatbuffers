# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class AniEventData(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AniEventData()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAniEventData(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # AniEventData
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # AniEventData
    def Name(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # AniEventData
    def Time(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # AniEventData
    def IntParam(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # AniEventData
    def FloatParam(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # AniEventData
    def StringParam(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def AniEventDataStart(builder):
    builder.StartObject(5)

def Start(builder):
    AniEventDataStart(builder)

def AniEventDataAddName(builder, name):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(name), 0)

def AddName(builder, name):
    AniEventDataAddName(builder, name)

def AniEventDataAddTime(builder, time):
    builder.PrependFloat32Slot(1, time, 0.0)

def AddTime(builder, time):
    AniEventDataAddTime(builder, time)

def AniEventDataAddIntParam(builder, intParam):
    builder.PrependInt32Slot(2, intParam, 0)

def AddIntParam(builder, intParam):
    AniEventDataAddIntParam(builder, intParam)

def AniEventDataAddFloatParam(builder, floatParam):
    builder.PrependFloat32Slot(3, floatParam, 0.0)

def AddFloatParam(builder, floatParam):
    AniEventDataAddFloatParam(builder, floatParam)

def AniEventDataAddStringParam(builder, stringParam):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(stringParam), 0)

def AddStringParam(builder, stringParam):
    AniEventDataAddStringParam(builder, stringParam)

def AniEventDataEnd(builder):
    return builder.EndObject()

def End(builder):
    return AniEventDataEnd(builder)


class AniEventDataT(object):

    # AniEventDataT
    def __init__(
        self,
        name = None,
        time = 0.0,
        intParam = 0,
        floatParam = 0.0,
        stringParam = None,
    ):
        self.name = name  # type: Optional[str]
        self.time = time  # type: float
        self.intParam = intParam  # type: int
        self.floatParam = floatParam  # type: float
        self.stringParam = stringParam  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        aniEventData = AniEventData()
        aniEventData.Init(buf, pos)
        return cls.InitFromObj(aniEventData)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, aniEventData):
        x = AniEventDataT()
        x._UnPack(aniEventData)
        return x

    # AniEventDataT
    def _UnPack(self, aniEventData):
        if aniEventData is None:
            return
        self.name = aniEventData.Name()
        self.time = aniEventData.Time()
        self.intParam = aniEventData.IntParam()
        self.floatParam = aniEventData.FloatParam()
        self.stringParam = aniEventData.StringParam()

    # AniEventDataT
    def Pack(self, builder):
        if self.name is not None:
            name = builder.CreateString(self.name)
        if self.stringParam is not None:
            stringParam = builder.CreateString(self.stringParam)
        AniEventDataStart(builder)
        if self.name is not None:
            AniEventDataAddName(builder, name)
        AniEventDataAddTime(builder, self.time)
        AniEventDataAddIntParam(builder, self.intParam)
        AniEventDataAddFloatParam(builder, self.floatParam)
        if self.stringParam is not None:
            AniEventDataAddStringParam(builder, stringParam)
        aniEventData = AniEventDataEnd(builder)
        return aniEventData

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(AniEventDataT, 'AniEventData', (('name', 'string', False), ('time', 'float32', False), ('intParam', 'int32', False), ('floatParam', 'float32', False), ('stringParam', 'string', False)))
