# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class LocalizeSNSExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LocalizeSNSExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLocalizeSNSExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # LocalizeSNSExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # LocalizeSNSExcel
    def Key(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # LocalizeSNSExcel
    def Kr(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # LocalizeSNSExcel
    def Jp(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def LocalizeSNSExcelStart(builder):
    builder.StartObject(3)

def Start(builder):
    LocalizeSNSExcelStart(builder)

def LocalizeSNSExcelAddKey(builder, key):
    builder.PrependUint32Slot(0, key, 0)

def AddKey(builder, key):
    LocalizeSNSExcelAddKey(builder, key)

def LocalizeSNSExcelAddKr(builder, kr):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(kr), 0)

def AddKr(builder, kr):
    LocalizeSNSExcelAddKr(builder, kr)

def LocalizeSNSExcelAddJp(builder, jp):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(jp), 0)

def AddJp(builder, jp):
    LocalizeSNSExcelAddJp(builder, jp)

def LocalizeSNSExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return LocalizeSNSExcelEnd(builder)


class LocalizeSNSExcelT(object):

    # LocalizeSNSExcelT
    def __init__(
        self,
        key = 0,
        kr = None,
        jp = None,
    ):
        self.key = key  # type: int
        self.kr = kr  # type: Optional[str]
        self.jp = jp  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        localizeSnsexcel = LocalizeSNSExcel()
        localizeSnsexcel.Init(buf, pos)
        return cls.InitFromObj(localizeSnsexcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, localizeSnsexcel):
        x = LocalizeSNSExcelT()
        x._UnPack(localizeSnsexcel)
        return x

    # LocalizeSNSExcelT
    def _UnPack(self, localizeSnsexcel):
        if localizeSnsexcel is None:
            return
        self.key = localizeSnsexcel.Key()
        self.kr = localizeSnsexcel.Kr()
        self.jp = localizeSnsexcel.Jp()

    # LocalizeSNSExcelT
    def Pack(self, builder):
        if self.kr is not None:
            kr = builder.CreateString(self.kr)
        if self.jp is not None:
            jp = builder.CreateString(self.jp)
        LocalizeSNSExcelStart(builder)
        LocalizeSNSExcelAddKey(builder, self.key)
        if self.kr is not None:
            LocalizeSNSExcelAddKr(builder, kr)
        if self.jp is not None:
            LocalizeSNSExcelAddJp(builder, jp)
        localizeSnsexcel = LocalizeSNSExcelEnd(builder)
        return localizeSnsexcel

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(LocalizeSNSExcelT, 'LocalizeSNSExcel', (('key', 'uint32', False), ('kr', 'string', False), ('jp', 'string', False)))
