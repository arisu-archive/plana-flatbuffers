# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class Motion(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Motion()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMotion(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Motion
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Motion
    def Name(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Motion
    def Positions(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from FlatData.Position import Position
            obj = Position()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Motion
    def PositionsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Motion
    def PositionsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def MotionStart(builder):
    builder.StartObject(2)

def Start(builder):
    MotionStart(builder)

def MotionAddName(builder, name):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(name), 0)

def AddName(builder, name):
    MotionAddName(builder, name)

def MotionAddPositions(builder, positions):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(positions), 0)

def AddPositions(builder, positions):
    MotionAddPositions(builder, positions)

def MotionStartPositionsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPositionsVector(builder, numElems):
    return MotionStartPositionsVector(builder, numElems)

def MotionEnd(builder):
    return builder.EndObject()

def End(builder):
    return MotionEnd(builder)

import FlatData.Position
try:
    from typing import List
except:
    pass

class MotionT(object):

    # MotionT
    def __init__(
        self,
        name = None,
        positions = None,
    ):
        self.name = name  # type: Optional[str]
        self.positions = positions  # type: Optional[List[FlatData.Position.PositionT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        motion = Motion()
        motion.Init(buf, pos)
        return cls.InitFromObj(motion)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, motion):
        x = MotionT()
        x._UnPack(motion)
        return x

    # MotionT
    def _UnPack(self, motion):
        if motion is None:
            return
        self.name = motion.Name()
        if not motion.PositionsIsNone():
            self.positions = []
            for i in range(motion.PositionsLength()):
                if motion.Positions(i) is None:
                    self.positions.append(None)
                else:
                    position_ = FlatData.Position.PositionT.InitFromObj(motion.Positions(i))
                    self.positions.append(position_)

    # MotionT
    def Pack(self, builder):
        if self.name is not None:
            name = builder.CreateString(self.name)
        if self.positions is not None:
            positionslist = []
            for i in range(len(self.positions)):
                positionslist.append(self.positions[i].Pack(builder))
            MotionStartPositionsVector(builder, len(self.positions))
            for i in reversed(range(len(self.positions))):
                builder.PrependUOffsetTRelative(positionslist[i])
            positions = builder.EndVector()
        MotionStart(builder)
        if self.name is not None:
            MotionAddName(builder, name)
        if self.positions is not None:
            MotionAddPositions(builder, positions)
        motion = MotionEnd(builder)
        return motion

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(MotionT, 'Motion', (('name', 'string', False),))
