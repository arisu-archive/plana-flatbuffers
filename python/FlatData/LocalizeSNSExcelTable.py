# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class LocalizeSNSExcelTable(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LocalizeSNSExcelTable()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLocalizeSNSExcelTable(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # LocalizeSNSExcelTable
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # LocalizeSNSExcelTable
    def DataList(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from FlatData.LocalizeSNSExcel import LocalizeSNSExcel
            obj = LocalizeSNSExcel()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # LocalizeSNSExcelTable
    def DataListLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # LocalizeSNSExcelTable
    def DataListIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def LocalizeSNSExcelTableStart(builder):
    builder.StartObject(1)

def Start(builder):
    LocalizeSNSExcelTableStart(builder)

def LocalizeSNSExcelTableAddDataList(builder, dataList):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(dataList), 0)

def AddDataList(builder, dataList):
    LocalizeSNSExcelTableAddDataList(builder, dataList)

def LocalizeSNSExcelTableStartDataListVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDataListVector(builder, numElems):
    return LocalizeSNSExcelTableStartDataListVector(builder, numElems)

def LocalizeSNSExcelTableEnd(builder):
    return builder.EndObject()

def End(builder):
    return LocalizeSNSExcelTableEnd(builder)

import FlatData.LocalizeSNSExcel
try:
    from typing import List
except:
    pass

class LocalizeSNSExcelTableT(object):

    # LocalizeSNSExcelTableT
    def __init__(
        self,
        dataList = None,
    ):
        self.dataList = dataList  # type: Optional[List[FlatData.LocalizeSNSExcel.LocalizeSNSExcelT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        localizeSnsexcelTable = LocalizeSNSExcelTable()
        localizeSnsexcelTable.Init(buf, pos)
        return cls.InitFromObj(localizeSnsexcelTable)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, localizeSnsexcelTable):
        x = LocalizeSNSExcelTableT()
        x._UnPack(localizeSnsexcelTable)
        return x

    # LocalizeSNSExcelTableT
    def _UnPack(self, localizeSnsexcelTable):
        if localizeSnsexcelTable is None:
            return
        if not localizeSnsexcelTable.DataListIsNone():
            self.dataList = []
            for i in range(localizeSnsexcelTable.DataListLength()):
                if localizeSnsexcelTable.DataList(i) is None:
                    self.dataList.append(None)
                else:
                    localizeSNSExcel_ = FlatData.LocalizeSNSExcel.LocalizeSNSExcelT.InitFromObj(localizeSnsexcelTable.DataList(i))
                    self.dataList.append(localizeSNSExcel_)

    # LocalizeSNSExcelTableT
    def Pack(self, builder):
        if self.dataList is not None:
            dataListlist = []
            for i in range(len(self.dataList)):
                dataListlist.append(self.dataList[i].Pack(builder))
            LocalizeSNSExcelTableStartDataListVector(builder, len(self.dataList))
            for i in reversed(range(len(self.dataList))):
                builder.PrependUOffsetTRelative(dataListlist[i])
            dataList = builder.EndVector()
        LocalizeSNSExcelTableStart(builder)
        if self.dataList is not None:
            LocalizeSNSExcelTableAddDataList(builder, dataList)
        localizeSnsexcelTable = LocalizeSNSExcelTableEnd(builder)
        return localizeSnsexcelTable

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(LocalizeSNSExcelTableT, 'LocalizeSNSExcelTable', ())
