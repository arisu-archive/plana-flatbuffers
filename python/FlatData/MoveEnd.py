# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class MoveEnd(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MoveEnd()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMoveEnd(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # MoveEnd
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # MoveEnd
    def Normal(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FlatData.Motion import Motion
            obj = Motion()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MoveEnd
    def Stand(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FlatData.Motion import Motion
            obj = Motion()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # MoveEnd
    def Kneel(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from FlatData.Motion import Motion
            obj = Motion()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def MoveEndStart(builder):
    builder.StartObject(3)

def Start(builder):
    MoveEndStart(builder)

def MoveEndAddNormal(builder, normal):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(normal), 0)

def AddNormal(builder, normal):
    MoveEndAddNormal(builder, normal)

def MoveEndAddStand(builder, stand):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(stand), 0)

def AddStand(builder, stand):
    MoveEndAddStand(builder, stand)

def MoveEndAddKneel(builder, kneel):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(kneel), 0)

def AddKneel(builder, kneel):
    MoveEndAddKneel(builder, kneel)

def MoveEndEnd(builder):
    return builder.EndObject()

def End(builder):
    return MoveEndEnd(builder)

import FlatData.Motion
try:
    from typing import Optional
except:
    pass

class MoveEndT(object):

    # MoveEndT
    def __init__(
        self,
        normal = None,
        stand = None,
        kneel = None,
    ):
        self.normal = normal  # type: Optional[FlatData.Motion.MotionT]
        self.stand = stand  # type: Optional[FlatData.Motion.MotionT]
        self.kneel = kneel  # type: Optional[FlatData.Motion.MotionT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        moveEnd = MoveEnd()
        moveEnd.Init(buf, pos)
        return cls.InitFromObj(moveEnd)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, moveEnd):
        x = MoveEndT()
        x._UnPack(moveEnd)
        return x

    # MoveEndT
    def _UnPack(self, moveEnd):
        if moveEnd is None:
            return
        if moveEnd.Normal() is not None:
            self.normal = FlatData.Motion.MotionT.InitFromObj(moveEnd.Normal())
        if moveEnd.Stand() is not None:
            self.stand = FlatData.Motion.MotionT.InitFromObj(moveEnd.Stand())
        if moveEnd.Kneel() is not None:
            self.kneel = FlatData.Motion.MotionT.InitFromObj(moveEnd.Kneel())

    # MoveEndT
    def Pack(self, builder):
        if self.normal is not None:
            normal = self.normal.Pack(builder)
        if self.stand is not None:
            stand = self.stand.Pack(builder)
        if self.kneel is not None:
            kneel = self.kneel.Pack(builder)
        MoveEndStart(builder)
        if self.normal is not None:
            MoveEndAddNormal(builder, normal)
        if self.stand is not None:
            MoveEndAddStand(builder, stand)
        if self.kneel is not None:
            MoveEndAddKneel(builder, kneel)
        moveEnd = MoveEndEnd(builder)
        return moveEnd

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(MoveEndT, 'MoveEnd', ())
