# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class StringTestExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = StringTestExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsStringTestExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # StringTestExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # StringTestExcel
    def String(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # StringTestExcel
    def StringLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # StringTestExcel
    def StringIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # StringTestExcel
    def Sentence1(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # StringTestExcel
    def Script(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def StringTestExcelStart(builder):
    builder.StartObject(3)

def Start(builder):
    StringTestExcelStart(builder)

def StringTestExcelAddString(builder, string):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(string), 0)

def AddString(builder, string):
    StringTestExcelAddString(builder, string)

def StringTestExcelStartStringVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartStringVector(builder, numElems):
    return StringTestExcelStartStringVector(builder, numElems)

def StringTestExcelAddSentence1(builder, sentence1):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(sentence1), 0)

def AddSentence1(builder, sentence1):
    StringTestExcelAddSentence1(builder, sentence1)

def StringTestExcelAddScript(builder, script):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(script), 0)

def AddScript(builder, script):
    StringTestExcelAddScript(builder, script)

def StringTestExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return StringTestExcelEnd(builder)

try:
    from typing import List
except:
    pass

class StringTestExcelT(object):

    # StringTestExcelT
    def __init__(
        self,
        string = None,
        sentence1 = None,
        script = None,
    ):
        self.string = string  # type: Optional[List[Optional[str]]]
        self.sentence1 = sentence1  # type: Optional[str]
        self.script = script  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        stringTestExcel = StringTestExcel()
        stringTestExcel.Init(buf, pos)
        return cls.InitFromObj(stringTestExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, stringTestExcel):
        x = StringTestExcelT()
        x._UnPack(stringTestExcel)
        return x

    # StringTestExcelT
    def _UnPack(self, stringTestExcel):
        if stringTestExcel is None:
            return
        if not stringTestExcel.StringIsNone():
            self.string = []
            for i in range(stringTestExcel.StringLength()):
                self.string.append(stringTestExcel.String(i))
        self.sentence1 = stringTestExcel.Sentence1()
        self.script = stringTestExcel.Script()

    # StringTestExcelT
    def Pack(self, builder):
        if self.string is not None:
            stringlist = []
            for i in range(len(self.string)):
                stringlist.append(builder.CreateString(self.string[i]))
            StringTestExcelStartStringVector(builder, len(self.string))
            for i in reversed(range(len(self.string))):
                builder.PrependUOffsetTRelative(stringlist[i])
            string = builder.EndVector()
        if self.sentence1 is not None:
            sentence1 = builder.CreateString(self.sentence1)
        if self.script is not None:
            script = builder.CreateString(self.script)
        StringTestExcelStart(builder)
        if self.string is not None:
            StringTestExcelAddString(builder, string)
        if self.sentence1 is not None:
            StringTestExcelAddSentence1(builder, sentence1)
        if self.script is not None:
            StringTestExcelAddScript(builder, script)
        stringTestExcel = StringTestExcelEnd(builder)
        return stringTestExcel

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(StringTestExcelT, 'StringTestExcel', (('string', 'string', True), ('sentence1', 'string', False), ('script', 'string', False)))
