# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class PropMotion(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PropMotion()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPropMotion(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PropMotion
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PropMotion
    def Name(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # PropMotion
    def Positions(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from FlatData.PropVector3 import PropVector3
            obj = PropVector3()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PropMotion
    def PositionsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PropMotion
    def PositionsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # PropMotion
    def Rotations(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from FlatData.PropVector3 import PropVector3
            obj = PropVector3()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PropMotion
    def RotationsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PropMotion
    def RotationsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def PropMotionStart(builder):
    builder.StartObject(3)

def Start(builder):
    PropMotionStart(builder)

def PropMotionAddName(builder, name):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(name), 0)

def AddName(builder, name):
    PropMotionAddName(builder, name)

def PropMotionAddPositions(builder, positions):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(positions), 0)

def AddPositions(builder, positions):
    PropMotionAddPositions(builder, positions)

def PropMotionStartPositionsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPositionsVector(builder, numElems):
    return PropMotionStartPositionsVector(builder, numElems)

def PropMotionAddRotations(builder, rotations):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(rotations), 0)

def AddRotations(builder, rotations):
    PropMotionAddRotations(builder, rotations)

def PropMotionStartRotationsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRotationsVector(builder, numElems):
    return PropMotionStartRotationsVector(builder, numElems)

def PropMotionEnd(builder):
    return builder.EndObject()

def End(builder):
    return PropMotionEnd(builder)

import FlatData.PropVector3
try:
    from typing import List
except:
    pass

class PropMotionT(object):

    # PropMotionT
    def __init__(
        self,
        name = None,
        positions = None,
        rotations = None,
    ):
        self.name = name  # type: Optional[str]
        self.positions = positions  # type: Optional[List[FlatData.PropVector3.PropVector3T]]
        self.rotations = rotations  # type: Optional[List[FlatData.PropVector3.PropVector3T]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        propMotion = PropMotion()
        propMotion.Init(buf, pos)
        return cls.InitFromObj(propMotion)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, propMotion):
        x = PropMotionT()
        x._UnPack(propMotion)
        return x

    # PropMotionT
    def _UnPack(self, propMotion):
        if propMotion is None:
            return
        self.name = propMotion.Name()
        if not propMotion.PositionsIsNone():
            self.positions = []
            for i in range(propMotion.PositionsLength()):
                if propMotion.Positions(i) is None:
                    self.positions.append(None)
                else:
                    propVector3_ = FlatData.PropVector3.PropVector3T.InitFromObj(propMotion.Positions(i))
                    self.positions.append(propVector3_)
        if not propMotion.RotationsIsNone():
            self.rotations = []
            for i in range(propMotion.RotationsLength()):
                if propMotion.Rotations(i) is None:
                    self.rotations.append(None)
                else:
                    propVector3_ = FlatData.PropVector3.PropVector3T.InitFromObj(propMotion.Rotations(i))
                    self.rotations.append(propVector3_)

    # PropMotionT
    def Pack(self, builder):
        if self.name is not None:
            name = builder.CreateString(self.name)
        if self.positions is not None:
            positionslist = []
            for i in range(len(self.positions)):
                positionslist.append(self.positions[i].Pack(builder))
            PropMotionStartPositionsVector(builder, len(self.positions))
            for i in reversed(range(len(self.positions))):
                builder.PrependUOffsetTRelative(positionslist[i])
            positions = builder.EndVector()
        if self.rotations is not None:
            rotationslist = []
            for i in range(len(self.rotations)):
                rotationslist.append(self.rotations[i].Pack(builder))
            PropMotionStartRotationsVector(builder, len(self.rotations))
            for i in reversed(range(len(self.rotations))):
                builder.PrependUOffsetTRelative(rotationslist[i])
            rotations = builder.EndVector()
        PropMotionStart(builder)
        if self.name is not None:
            PropMotionAddName(builder, name)
        if self.positions is not None:
            PropMotionAddPositions(builder, positions)
        if self.rotations is not None:
            PropMotionAddRotations(builder, rotations)
        propMotion = PropMotionEnd(builder)
        return propMotion

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(PropMotionT, 'PropMotion', (('name', 'string', False),))
