# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class TranscendenceRecipeExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TranscendenceRecipeExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTranscendenceRecipeExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # TranscendenceRecipeExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TranscendenceRecipeExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # TranscendenceRecipeExcel
    def DevName(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # TranscendenceRecipeExcel
    def CostCurrencyType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # TranscendenceRecipeExcel
    def CostCurrencyAmount(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # TranscendenceRecipeExcel
    def ParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # TranscendenceRecipeExcel
    def ParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # TranscendenceRecipeExcel
    def ParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TranscendenceRecipeExcel
    def ParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # TranscendenceRecipeExcel
    def ParcelId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # TranscendenceRecipeExcel
    def ParcelIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # TranscendenceRecipeExcel
    def ParcelIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TranscendenceRecipeExcel
    def ParcelIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # TranscendenceRecipeExcel
    def ParcelAmount(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # TranscendenceRecipeExcel
    def ParcelAmountAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # TranscendenceRecipeExcel
    def ParcelAmountLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # TranscendenceRecipeExcel
    def ParcelAmountIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

def TranscendenceRecipeExcelStart(builder):
    builder.StartObject(7)

def Start(builder):
    TranscendenceRecipeExcelStart(builder)

def TranscendenceRecipeExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    TranscendenceRecipeExcelAddId(builder, id)

def TranscendenceRecipeExcelAddDevName(builder, devName):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(devName), 0)

def AddDevName(builder, devName):
    TranscendenceRecipeExcelAddDevName(builder, devName)

def TranscendenceRecipeExcelAddCostCurrencyType(builder, costCurrencyType):
    builder.PrependInt32Slot(2, costCurrencyType, 0)

def AddCostCurrencyType(builder, costCurrencyType):
    TranscendenceRecipeExcelAddCostCurrencyType(builder, costCurrencyType)

def TranscendenceRecipeExcelAddCostCurrencyAmount(builder, costCurrencyAmount):
    builder.PrependInt64Slot(3, costCurrencyAmount, 0)

def AddCostCurrencyAmount(builder, costCurrencyAmount):
    TranscendenceRecipeExcelAddCostCurrencyAmount(builder, costCurrencyAmount)

def TranscendenceRecipeExcelAddParcelType(builder, parcelType):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(parcelType), 0)

def AddParcelType(builder, parcelType):
    TranscendenceRecipeExcelAddParcelType(builder, parcelType)

def TranscendenceRecipeExcelStartParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartParcelTypeVector(builder, numElems):
    return TranscendenceRecipeExcelStartParcelTypeVector(builder, numElems)

def TranscendenceRecipeExcelAddParcelId(builder, parcelId):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(parcelId), 0)

def AddParcelId(builder, parcelId):
    TranscendenceRecipeExcelAddParcelId(builder, parcelId)

def TranscendenceRecipeExcelStartParcelIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartParcelIdVector(builder, numElems):
    return TranscendenceRecipeExcelStartParcelIdVector(builder, numElems)

def TranscendenceRecipeExcelAddParcelAmount(builder, parcelAmount):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(parcelAmount), 0)

def AddParcelAmount(builder, parcelAmount):
    TranscendenceRecipeExcelAddParcelAmount(builder, parcelAmount)

def TranscendenceRecipeExcelStartParcelAmountVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartParcelAmountVector(builder, numElems):
    return TranscendenceRecipeExcelStartParcelAmountVector(builder, numElems)

def TranscendenceRecipeExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return TranscendenceRecipeExcelEnd(builder)

try:
    from typing import List
except:
    pass

class TranscendenceRecipeExcelT(object):

    # TranscendenceRecipeExcelT
    def __init__(
        self,
        id = 0,
        devName = None,
        costCurrencyType = 0,
        costCurrencyAmount = 0,
        parcelType = None,
        parcelId = None,
        parcelAmount = None,
    ):
        self.id = id  # type: int
        self.devName = devName  # type: Optional[str]
        self.costCurrencyType = costCurrencyType  # type: int
        self.costCurrencyAmount = costCurrencyAmount  # type: int
        self.parcelType = parcelType  # type: Optional[List[int]]
        self.parcelId = parcelId  # type: Optional[List[int]]
        self.parcelAmount = parcelAmount  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        transcendenceRecipeExcel = TranscendenceRecipeExcel()
        transcendenceRecipeExcel.Init(buf, pos)
        return cls.InitFromObj(transcendenceRecipeExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, transcendenceRecipeExcel):
        x = TranscendenceRecipeExcelT()
        x._UnPack(transcendenceRecipeExcel)
        return x

    # TranscendenceRecipeExcelT
    def _UnPack(self, transcendenceRecipeExcel):
        if transcendenceRecipeExcel is None:
            return
        self.id = transcendenceRecipeExcel.Id()
        self.devName = transcendenceRecipeExcel.DevName()
        self.costCurrencyType = transcendenceRecipeExcel.CostCurrencyType()
        self.costCurrencyAmount = transcendenceRecipeExcel.CostCurrencyAmount()
        if not transcendenceRecipeExcel.ParcelTypeIsNone():
            if np is None:
                self.parcelType = []
                for i in range(transcendenceRecipeExcel.ParcelTypeLength()):
                    self.parcelType.append(transcendenceRecipeExcel.ParcelType(i))
            else:
                self.parcelType = transcendenceRecipeExcel.ParcelTypeAsNumpy()
        if not transcendenceRecipeExcel.ParcelIdIsNone():
            if np is None:
                self.parcelId = []
                for i in range(transcendenceRecipeExcel.ParcelIdLength()):
                    self.parcelId.append(transcendenceRecipeExcel.ParcelId(i))
            else:
                self.parcelId = transcendenceRecipeExcel.ParcelIdAsNumpy()
        if not transcendenceRecipeExcel.ParcelAmountIsNone():
            if np is None:
                self.parcelAmount = []
                for i in range(transcendenceRecipeExcel.ParcelAmountLength()):
                    self.parcelAmount.append(transcendenceRecipeExcel.ParcelAmount(i))
            else:
                self.parcelAmount = transcendenceRecipeExcel.ParcelAmountAsNumpy()

    # TranscendenceRecipeExcelT
    def Pack(self, builder):
        if self.devName is not None:
            devName = builder.CreateString(self.devName)
        if self.parcelType is not None:
            if np is not None and type(self.parcelType) is np.ndarray:
                parcelType = builder.CreateNumpyVector(self.parcelType)
            else:
                TranscendenceRecipeExcelStartParcelTypeVector(builder, len(self.parcelType))
                for i in reversed(range(len(self.parcelType))):
                    builder.PrependInt32(self.parcelType[i])
                parcelType = builder.EndVector()
        if self.parcelId is not None:
            if np is not None and type(self.parcelId) is np.ndarray:
                parcelId = builder.CreateNumpyVector(self.parcelId)
            else:
                TranscendenceRecipeExcelStartParcelIdVector(builder, len(self.parcelId))
                for i in reversed(range(len(self.parcelId))):
                    builder.PrependInt64(self.parcelId[i])
                parcelId = builder.EndVector()
        if self.parcelAmount is not None:
            if np is not None and type(self.parcelAmount) is np.ndarray:
                parcelAmount = builder.CreateNumpyVector(self.parcelAmount)
            else:
                TranscendenceRecipeExcelStartParcelAmountVector(builder, len(self.parcelAmount))
                for i in reversed(range(len(self.parcelAmount))):
                    builder.PrependInt32(self.parcelAmount[i])
                parcelAmount = builder.EndVector()
        TranscendenceRecipeExcelStart(builder)
        TranscendenceRecipeExcelAddId(builder, self.id)
        if self.devName is not None:
            TranscendenceRecipeExcelAddDevName(builder, devName)
        TranscendenceRecipeExcelAddCostCurrencyType(builder, self.costCurrencyType)
        TranscendenceRecipeExcelAddCostCurrencyAmount(builder, self.costCurrencyAmount)
        if self.parcelType is not None:
            TranscendenceRecipeExcelAddParcelType(builder, parcelType)
        if self.parcelId is not None:
            TranscendenceRecipeExcelAddParcelId(builder, parcelId)
        if self.parcelAmount is not None:
            TranscendenceRecipeExcelAddParcelAmount(builder, parcelAmount)
        transcendenceRecipeExcel = TranscendenceRecipeExcelEnd(builder)
        return transcendenceRecipeExcel

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(TranscendenceRecipeExcelT, 'TranscendenceRecipeExcel', (('id', 'int64', False), ('devName', 'string', False), ('costCurrencyType', 'int32', False), ('costCurrencyAmount', 'int64', False), ('parcelType', 'int32', True), ('parcelId', 'int64', True), ('parcelAmount', 'int32', True)))
