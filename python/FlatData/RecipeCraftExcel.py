# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class RecipeCraftExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = RecipeCraftExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsRecipeCraftExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # RecipeCraftExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # RecipeCraftExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # RecipeCraftExcel
    def DevName(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RecipeCraftExcel
    def RecipeType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # RecipeCraftExcel
    def RecipeIngredientId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # RecipeCraftExcel
    def RecipeIngredientDevName(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # RecipeCraftExcel
    def ParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # RecipeCraftExcel
    def ParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # RecipeCraftExcel
    def ParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RecipeCraftExcel
    def ParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

    # RecipeCraftExcel
    def ParcelId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # RecipeCraftExcel
    def ParcelIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # RecipeCraftExcel
    def ParcelIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RecipeCraftExcel
    def ParcelIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # RecipeCraftExcel
    def ParcelDevName(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # RecipeCraftExcel
    def ParcelDevNameLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RecipeCraftExcel
    def ParcelDevNameIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # RecipeCraftExcel
    def ResultAmountMin(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # RecipeCraftExcel
    def ResultAmountMinAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # RecipeCraftExcel
    def ResultAmountMinLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RecipeCraftExcel
    def ResultAmountMinIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # RecipeCraftExcel
    def ResultAmountMax(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # RecipeCraftExcel
    def ResultAmountMaxAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # RecipeCraftExcel
    def ResultAmountMaxLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # RecipeCraftExcel
    def ResultAmountMaxIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

def RecipeCraftExcelStart(builder):
    builder.StartObject(10)

def Start(builder):
    RecipeCraftExcelStart(builder)

def RecipeCraftExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    RecipeCraftExcelAddId(builder, id)

def RecipeCraftExcelAddDevName(builder, devName):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(devName), 0)

def AddDevName(builder, devName):
    RecipeCraftExcelAddDevName(builder, devName)

def RecipeCraftExcelAddRecipeType(builder, recipeType):
    builder.PrependInt32Slot(2, recipeType, 0)

def AddRecipeType(builder, recipeType):
    RecipeCraftExcelAddRecipeType(builder, recipeType)

def RecipeCraftExcelAddRecipeIngredientId(builder, recipeIngredientId):
    builder.PrependInt64Slot(3, recipeIngredientId, 0)

def AddRecipeIngredientId(builder, recipeIngredientId):
    RecipeCraftExcelAddRecipeIngredientId(builder, recipeIngredientId)

def RecipeCraftExcelAddRecipeIngredientDevName(builder, recipeIngredientDevName):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(recipeIngredientDevName), 0)

def AddRecipeIngredientDevName(builder, recipeIngredientDevName):
    RecipeCraftExcelAddRecipeIngredientDevName(builder, recipeIngredientDevName)

def RecipeCraftExcelAddParcelType(builder, parcelType):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(parcelType), 0)

def AddParcelType(builder, parcelType):
    RecipeCraftExcelAddParcelType(builder, parcelType)

def RecipeCraftExcelStartParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartParcelTypeVector(builder, numElems):
    return RecipeCraftExcelStartParcelTypeVector(builder, numElems)

def RecipeCraftExcelAddParcelId(builder, parcelId):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(parcelId), 0)

def AddParcelId(builder, parcelId):
    RecipeCraftExcelAddParcelId(builder, parcelId)

def RecipeCraftExcelStartParcelIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartParcelIdVector(builder, numElems):
    return RecipeCraftExcelStartParcelIdVector(builder, numElems)

def RecipeCraftExcelAddParcelDevName(builder, parcelDevName):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(parcelDevName), 0)

def AddParcelDevName(builder, parcelDevName):
    RecipeCraftExcelAddParcelDevName(builder, parcelDevName)

def RecipeCraftExcelStartParcelDevNameVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartParcelDevNameVector(builder, numElems):
    return RecipeCraftExcelStartParcelDevNameVector(builder, numElems)

def RecipeCraftExcelAddResultAmountMin(builder, resultAmountMin):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(resultAmountMin), 0)

def AddResultAmountMin(builder, resultAmountMin):
    RecipeCraftExcelAddResultAmountMin(builder, resultAmountMin)

def RecipeCraftExcelStartResultAmountMinVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartResultAmountMinVector(builder, numElems):
    return RecipeCraftExcelStartResultAmountMinVector(builder, numElems)

def RecipeCraftExcelAddResultAmountMax(builder, resultAmountMax):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(resultAmountMax), 0)

def AddResultAmountMax(builder, resultAmountMax):
    RecipeCraftExcelAddResultAmountMax(builder, resultAmountMax)

def RecipeCraftExcelStartResultAmountMaxVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartResultAmountMaxVector(builder, numElems):
    return RecipeCraftExcelStartResultAmountMaxVector(builder, numElems)

def RecipeCraftExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return RecipeCraftExcelEnd(builder)

try:
    from typing import List
except:
    pass

class RecipeCraftExcelT(object):

    # RecipeCraftExcelT
    def __init__(
        self,
        id = 0,
        devName = None,
        recipeType = 0,
        recipeIngredientId = 0,
        recipeIngredientDevName = None,
        parcelType = None,
        parcelId = None,
        parcelDevName = None,
        resultAmountMin = None,
        resultAmountMax = None,
    ):
        self.id = id  # type: int
        self.devName = devName  # type: Optional[str]
        self.recipeType = recipeType  # type: int
        self.recipeIngredientId = recipeIngredientId  # type: int
        self.recipeIngredientDevName = recipeIngredientDevName  # type: Optional[str]
        self.parcelType = parcelType  # type: Optional[List[int]]
        self.parcelId = parcelId  # type: Optional[List[int]]
        self.parcelDevName = parcelDevName  # type: Optional[List[Optional[str]]]
        self.resultAmountMin = resultAmountMin  # type: Optional[List[int]]
        self.resultAmountMax = resultAmountMax  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        recipeCraftExcel = RecipeCraftExcel()
        recipeCraftExcel.Init(buf, pos)
        return cls.InitFromObj(recipeCraftExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, recipeCraftExcel):
        x = RecipeCraftExcelT()
        x._UnPack(recipeCraftExcel)
        return x

    # RecipeCraftExcelT
    def _UnPack(self, recipeCraftExcel):
        if recipeCraftExcel is None:
            return
        self.id = recipeCraftExcel.Id()
        self.devName = recipeCraftExcel.DevName()
        self.recipeType = recipeCraftExcel.RecipeType()
        self.recipeIngredientId = recipeCraftExcel.RecipeIngredientId()
        self.recipeIngredientDevName = recipeCraftExcel.RecipeIngredientDevName()
        if not recipeCraftExcel.ParcelTypeIsNone():
            if np is None:
                self.parcelType = []
                for i in range(recipeCraftExcel.ParcelTypeLength()):
                    self.parcelType.append(recipeCraftExcel.ParcelType(i))
            else:
                self.parcelType = recipeCraftExcel.ParcelTypeAsNumpy()
        if not recipeCraftExcel.ParcelIdIsNone():
            if np is None:
                self.parcelId = []
                for i in range(recipeCraftExcel.ParcelIdLength()):
                    self.parcelId.append(recipeCraftExcel.ParcelId(i))
            else:
                self.parcelId = recipeCraftExcel.ParcelIdAsNumpy()
        if not recipeCraftExcel.ParcelDevNameIsNone():
            self.parcelDevName = []
            for i in range(recipeCraftExcel.ParcelDevNameLength()):
                self.parcelDevName.append(recipeCraftExcel.ParcelDevName(i))
        if not recipeCraftExcel.ResultAmountMinIsNone():
            if np is None:
                self.resultAmountMin = []
                for i in range(recipeCraftExcel.ResultAmountMinLength()):
                    self.resultAmountMin.append(recipeCraftExcel.ResultAmountMin(i))
            else:
                self.resultAmountMin = recipeCraftExcel.ResultAmountMinAsNumpy()
        if not recipeCraftExcel.ResultAmountMaxIsNone():
            if np is None:
                self.resultAmountMax = []
                for i in range(recipeCraftExcel.ResultAmountMaxLength()):
                    self.resultAmountMax.append(recipeCraftExcel.ResultAmountMax(i))
            else:
                self.resultAmountMax = recipeCraftExcel.ResultAmountMaxAsNumpy()

    # RecipeCraftExcelT
    def Pack(self, builder):
        if self.devName is not None:
            devName = builder.CreateString(self.devName)
        if self.recipeIngredientDevName is not None:
            recipeIngredientDevName = builder.CreateString(self.recipeIngredientDevName)
        if self.parcelType is not None:
            if np is not None and type(self.parcelType) is np.ndarray:
                parcelType = builder.CreateNumpyVector(self.parcelType)
            else:
                RecipeCraftExcelStartParcelTypeVector(builder, len(self.parcelType))
                for i in reversed(range(len(self.parcelType))):
                    builder.PrependInt32(self.parcelType[i])
                parcelType = builder.EndVector()
        if self.parcelId is not None:
            if np is not None and type(self.parcelId) is np.ndarray:
                parcelId = builder.CreateNumpyVector(self.parcelId)
            else:
                RecipeCraftExcelStartParcelIdVector(builder, len(self.parcelId))
                for i in reversed(range(len(self.parcelId))):
                    builder.PrependInt64(self.parcelId[i])
                parcelId = builder.EndVector()
        if self.parcelDevName is not None:
            parcelDevNamelist = []
            for i in range(len(self.parcelDevName)):
                parcelDevNamelist.append(builder.CreateString(self.parcelDevName[i]))
            RecipeCraftExcelStartParcelDevNameVector(builder, len(self.parcelDevName))
            for i in reversed(range(len(self.parcelDevName))):
                builder.PrependUOffsetTRelative(parcelDevNamelist[i])
            parcelDevName = builder.EndVector()
        if self.resultAmountMin is not None:
            if np is not None and type(self.resultAmountMin) is np.ndarray:
                resultAmountMin = builder.CreateNumpyVector(self.resultAmountMin)
            else:
                RecipeCraftExcelStartResultAmountMinVector(builder, len(self.resultAmountMin))
                for i in reversed(range(len(self.resultAmountMin))):
                    builder.PrependInt64(self.resultAmountMin[i])
                resultAmountMin = builder.EndVector()
        if self.resultAmountMax is not None:
            if np is not None and type(self.resultAmountMax) is np.ndarray:
                resultAmountMax = builder.CreateNumpyVector(self.resultAmountMax)
            else:
                RecipeCraftExcelStartResultAmountMaxVector(builder, len(self.resultAmountMax))
                for i in reversed(range(len(self.resultAmountMax))):
                    builder.PrependInt64(self.resultAmountMax[i])
                resultAmountMax = builder.EndVector()
        RecipeCraftExcelStart(builder)
        RecipeCraftExcelAddId(builder, self.id)
        if self.devName is not None:
            RecipeCraftExcelAddDevName(builder, devName)
        RecipeCraftExcelAddRecipeType(builder, self.recipeType)
        RecipeCraftExcelAddRecipeIngredientId(builder, self.recipeIngredientId)
        if self.recipeIngredientDevName is not None:
            RecipeCraftExcelAddRecipeIngredientDevName(builder, recipeIngredientDevName)
        if self.parcelType is not None:
            RecipeCraftExcelAddParcelType(builder, parcelType)
        if self.parcelId is not None:
            RecipeCraftExcelAddParcelId(builder, parcelId)
        if self.parcelDevName is not None:
            RecipeCraftExcelAddParcelDevName(builder, parcelDevName)
        if self.resultAmountMin is not None:
            RecipeCraftExcelAddResultAmountMin(builder, resultAmountMin)
        if self.resultAmountMax is not None:
            RecipeCraftExcelAddResultAmountMax(builder, resultAmountMax)
        recipeCraftExcel = RecipeCraftExcelEnd(builder)
        return recipeCraftExcel

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(RecipeCraftExcelT, 'RecipeCraftExcel', (('id', 'int64', False), ('devName', 'string', False), ('recipeType', 'int32', False), ('recipeIngredientId', 'int64', False), ('recipeIngredientDevName', 'string', False), ('parcelType', 'int32', True), ('parcelId', 'int64', True), ('parcelDevName', 'string', True), ('resultAmountMin', 'int64', True), ('resultAmountMax', 'int64', True)))
