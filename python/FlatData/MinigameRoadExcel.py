# automatically generated by the FlatBuffers compiler, do not modify

# namespace: FlatData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class MinigameRoadExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MinigameRoadExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMinigameRoadExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # MinigameRoadExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # MinigameRoadExcel
    def None_(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # MinigameRoadExcel
    def None_AsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # MinigameRoadExcel
    def None_Length(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MinigameRoadExcel
    def None_IsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def MinigameRoadExcelStart(builder):
    builder.StartObject(1)

def Start(builder):
    MinigameRoadExcelStart(builder)

def MinigameRoadExcelAddNone_(builder, none):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(none), 0)

def AddNone_(builder, none):
    MinigameRoadExcelAddNone_(builder, none)

def MinigameRoadExcelStartNone_Vector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartNone_Vector(builder, numElems):
    return MinigameRoadExcelStartNone_Vector(builder, numElems)

def MinigameRoadExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return MinigameRoadExcelEnd(builder)

try:
    from typing import List
except:
    pass

class MinigameRoadExcelT(object):

    # MinigameRoadExcelT
    def __init__(
        self,
        none = None,
    ):
        self.none = none  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        minigameRoadExcel = MinigameRoadExcel()
        minigameRoadExcel.Init(buf, pos)
        return cls.InitFromObj(minigameRoadExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, minigameRoadExcel):
        x = MinigameRoadExcelT()
        x._UnPack(minigameRoadExcel)
        return x

    # MinigameRoadExcelT
    def _UnPack(self, minigameRoadExcel):
        if minigameRoadExcel is None:
            return
        if not minigameRoadExcel.None_IsNone():
            if np is None:
                self.none = []
                for i in range(minigameRoadExcel.None_Length()):
                    self.none.append(minigameRoadExcel.None_(i))
            else:
                self.none = minigameRoadExcel.None_AsNumpy()

    # MinigameRoadExcelT
    def Pack(self, builder):
        if self.none is not None:
            if np is not None and type(self.none) is np.ndarray:
                none = builder.CreateNumpyVector(self.none)
            else:
                MinigameRoadExcelStartNone_Vector(builder, len(self.none))
                for i in reversed(range(len(self.none))):
                    builder.PrependInt32(self.none[i])
                none = builder.EndVector()
        MinigameRoadExcelStart(builder)
        if self.none is not None:
            MinigameRoadExcelAddNone_(builder, none)
        minigameRoadExcel = MinigameRoadExcelEnd(builder)
        return minigameRoadExcel

# plana-flatbuffer: object-api conversion
from FlatData._conversion import install_object_api as _install_object_api
_install_object_api(MinigameRoadExcelT, 'MinigameRoadExcel', (('none', 'int32', True),))
