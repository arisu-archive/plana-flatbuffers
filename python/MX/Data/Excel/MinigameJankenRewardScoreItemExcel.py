# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class MinigameJankenRewardScoreItemExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MinigameJankenRewardScoreItemExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMinigameJankenRewardScoreItemExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # MinigameJankenRewardScoreItemExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # MinigameJankenRewardScoreItemExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # MinigameJankenRewardScoreItemExcel
    def EventContentId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # MinigameJankenRewardScoreItemExcel
    def ParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # MinigameJankenRewardScoreItemExcel
    def ParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # MinigameJankenRewardScoreItemExcel
    def ParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MinigameJankenRewardScoreItemExcel
    def ParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # MinigameJankenRewardScoreItemExcel
    def ParcelUniqueId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # MinigameJankenRewardScoreItemExcel
    def ParcelUniqueIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # MinigameJankenRewardScoreItemExcel
    def ParcelUniqueIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MinigameJankenRewardScoreItemExcel
    def ParcelUniqueIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # MinigameJankenRewardScoreItemExcel
    def Amount(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # MinigameJankenRewardScoreItemExcel
    def AmountAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # MinigameJankenRewardScoreItemExcel
    def AmountLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MinigameJankenRewardScoreItemExcel
    def AmountIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def MinigameJankenRewardScoreItemExcelStart(builder):
    builder.StartObject(5)

def Start(builder):
    MinigameJankenRewardScoreItemExcelStart(builder)

def MinigameJankenRewardScoreItemExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    MinigameJankenRewardScoreItemExcelAddId(builder, id)

def MinigameJankenRewardScoreItemExcelAddEventContentId(builder, eventContentId):
    builder.PrependInt64Slot(1, eventContentId, 0)

def AddEventContentId(builder, eventContentId):
    MinigameJankenRewardScoreItemExcelAddEventContentId(builder, eventContentId)

def MinigameJankenRewardScoreItemExcelAddParcelType(builder, parcelType):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(parcelType), 0)

def AddParcelType(builder, parcelType):
    MinigameJankenRewardScoreItemExcelAddParcelType(builder, parcelType)

def MinigameJankenRewardScoreItemExcelStartParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartParcelTypeVector(builder, numElems):
    return MinigameJankenRewardScoreItemExcelStartParcelTypeVector(builder, numElems)

def MinigameJankenRewardScoreItemExcelAddParcelUniqueId(builder, parcelUniqueId):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(parcelUniqueId), 0)

def AddParcelUniqueId(builder, parcelUniqueId):
    MinigameJankenRewardScoreItemExcelAddParcelUniqueId(builder, parcelUniqueId)

def MinigameJankenRewardScoreItemExcelStartParcelUniqueIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartParcelUniqueIdVector(builder, numElems):
    return MinigameJankenRewardScoreItemExcelStartParcelUniqueIdVector(builder, numElems)

def MinigameJankenRewardScoreItemExcelAddAmount(builder, amount):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(amount), 0)

def AddAmount(builder, amount):
    MinigameJankenRewardScoreItemExcelAddAmount(builder, amount)

def MinigameJankenRewardScoreItemExcelStartAmountVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartAmountVector(builder, numElems):
    return MinigameJankenRewardScoreItemExcelStartAmountVector(builder, numElems)

def MinigameJankenRewardScoreItemExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return MinigameJankenRewardScoreItemExcelEnd(builder)

try:
    from typing import List
except:
    pass

class MinigameJankenRewardScoreItemExcelT(object):

    # MinigameJankenRewardScoreItemExcelT
    def __init__(
        self,
        id = 0,
        eventContentId = 0,
        parcelType = None,
        parcelUniqueId = None,
        amount = None,
    ):
        self.id = id  # type: int
        self.eventContentId = eventContentId  # type: int
        self.parcelType = parcelType  # type: Optional[List[int]]
        self.parcelUniqueId = parcelUniqueId  # type: Optional[List[int]]
        self.amount = amount  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        minigameJankenRewardScoreItemExcel = MinigameJankenRewardScoreItemExcel()
        minigameJankenRewardScoreItemExcel.Init(buf, pos)
        return cls.InitFromObj(minigameJankenRewardScoreItemExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, minigameJankenRewardScoreItemExcel):
        x = MinigameJankenRewardScoreItemExcelT()
        x._UnPack(minigameJankenRewardScoreItemExcel)
        return x

    # MinigameJankenRewardScoreItemExcelT
    def _UnPack(self, minigameJankenRewardScoreItemExcel):
        if minigameJankenRewardScoreItemExcel is None:
            return
        self.id = minigameJankenRewardScoreItemExcel.Id()
        self.eventContentId = minigameJankenRewardScoreItemExcel.EventContentId()
        if not minigameJankenRewardScoreItemExcel.ParcelTypeIsNone():
            if np is None:
                self.parcelType = []
                for i in range(minigameJankenRewardScoreItemExcel.ParcelTypeLength()):
                    self.parcelType.append(minigameJankenRewardScoreItemExcel.ParcelType(i))
            else:
                self.parcelType = minigameJankenRewardScoreItemExcel.ParcelTypeAsNumpy()
        if not minigameJankenRewardScoreItemExcel.ParcelUniqueIdIsNone():
            if np is None:
                self.parcelUniqueId = []
                for i in range(minigameJankenRewardScoreItemExcel.ParcelUniqueIdLength()):
                    self.parcelUniqueId.append(minigameJankenRewardScoreItemExcel.ParcelUniqueId(i))
            else:
                self.parcelUniqueId = minigameJankenRewardScoreItemExcel.ParcelUniqueIdAsNumpy()
        if not minigameJankenRewardScoreItemExcel.AmountIsNone():
            if np is None:
                self.amount = []
                for i in range(minigameJankenRewardScoreItemExcel.AmountLength()):
                    self.amount.append(minigameJankenRewardScoreItemExcel.Amount(i))
            else:
                self.amount = minigameJankenRewardScoreItemExcel.AmountAsNumpy()

    # MinigameJankenRewardScoreItemExcelT
    def Pack(self, builder):
        if self.parcelType is not None:
            if np is not None and type(self.parcelType) is np.ndarray:
                parcelType = builder.CreateNumpyVector(self.parcelType)
            else:
                MinigameJankenRewardScoreItemExcelStartParcelTypeVector(builder, len(self.parcelType))
                for i in reversed(range(len(self.parcelType))):
                    builder.PrependInt32(self.parcelType[i])
                parcelType = builder.EndVector()
        if self.parcelUniqueId is not None:
            if np is not None and type(self.parcelUniqueId) is np.ndarray:
                parcelUniqueId = builder.CreateNumpyVector(self.parcelUniqueId)
            else:
                MinigameJankenRewardScoreItemExcelStartParcelUniqueIdVector(builder, len(self.parcelUniqueId))
                for i in reversed(range(len(self.parcelUniqueId))):
                    builder.PrependInt64(self.parcelUniqueId[i])
                parcelUniqueId = builder.EndVector()
        if self.amount is not None:
            if np is not None and type(self.amount) is np.ndarray:
                amount = builder.CreateNumpyVector(self.amount)
            else:
                MinigameJankenRewardScoreItemExcelStartAmountVector(builder, len(self.amount))
                for i in reversed(range(len(self.amount))):
                    builder.PrependInt64(self.amount[i])
                amount = builder.EndVector()
        MinigameJankenRewardScoreItemExcelStart(builder)
        MinigameJankenRewardScoreItemExcelAddId(builder, self.id)
        MinigameJankenRewardScoreItemExcelAddEventContentId(builder, self.eventContentId)
        if self.parcelType is not None:
            MinigameJankenRewardScoreItemExcelAddParcelType(builder, parcelType)
        if self.parcelUniqueId is not None:
            MinigameJankenRewardScoreItemExcelAddParcelUniqueId(builder, parcelUniqueId)
        if self.amount is not None:
            MinigameJankenRewardScoreItemExcelAddAmount(builder, amount)
        minigameJankenRewardScoreItemExcel = MinigameJankenRewardScoreItemExcelEnd(builder)
        return minigameJankenRewardScoreItemExcel
