# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class CharacterStatsDetailExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CharacterStatsDetailExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCharacterStatsDetailExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # CharacterStatsDetailExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CharacterStatsDetailExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # CharacterStatsDetailExcel
    def DetailShowStats(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # CharacterStatsDetailExcel
    def DetailShowStatsAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # CharacterStatsDetailExcel
    def DetailShowStatsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CharacterStatsDetailExcel
    def DetailShowStatsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # CharacterStatsDetailExcel
    def IsStatsPercent(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.BoolFlags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # CharacterStatsDetailExcel
    def IsStatsPercentAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.BoolFlags, o)
        return 0

    # CharacterStatsDetailExcel
    def IsStatsPercentLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CharacterStatsDetailExcel
    def IsStatsPercentIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def CharacterStatsDetailExcelStart(builder):
    builder.StartObject(3)

def Start(builder):
    CharacterStatsDetailExcelStart(builder)

def CharacterStatsDetailExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    CharacterStatsDetailExcelAddId(builder, id)

def CharacterStatsDetailExcelAddDetailShowStats(builder, detailShowStats):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(detailShowStats), 0)

def AddDetailShowStats(builder, detailShowStats):
    CharacterStatsDetailExcelAddDetailShowStats(builder, detailShowStats)

def CharacterStatsDetailExcelStartDetailShowStatsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDetailShowStatsVector(builder, numElems):
    return CharacterStatsDetailExcelStartDetailShowStatsVector(builder, numElems)

def CharacterStatsDetailExcelAddIsStatsPercent(builder, isStatsPercent):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(isStatsPercent), 0)

def AddIsStatsPercent(builder, isStatsPercent):
    CharacterStatsDetailExcelAddIsStatsPercent(builder, isStatsPercent)

def CharacterStatsDetailExcelStartIsStatsPercentVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartIsStatsPercentVector(builder, numElems):
    return CharacterStatsDetailExcelStartIsStatsPercentVector(builder, numElems)

def CharacterStatsDetailExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return CharacterStatsDetailExcelEnd(builder)

try:
    from typing import List
except:
    pass

class CharacterStatsDetailExcelT(object):

    # CharacterStatsDetailExcelT
    def __init__(
        self,
        id = 0,
        detailShowStats = None,
        isStatsPercent = None,
    ):
        self.id = id  # type: int
        self.detailShowStats = detailShowStats  # type: Optional[List[int]]
        self.isStatsPercent = isStatsPercent  # type: Optional[List[bool]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        characterStatsDetailExcel = CharacterStatsDetailExcel()
        characterStatsDetailExcel.Init(buf, pos)
        return cls.InitFromObj(characterStatsDetailExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, characterStatsDetailExcel):
        x = CharacterStatsDetailExcelT()
        x._UnPack(characterStatsDetailExcel)
        return x

    # CharacterStatsDetailExcelT
    def _UnPack(self, characterStatsDetailExcel):
        if characterStatsDetailExcel is None:
            return
        self.id = characterStatsDetailExcel.Id()
        if not characterStatsDetailExcel.DetailShowStatsIsNone():
            if np is None:
                self.detailShowStats = []
                for i in range(characterStatsDetailExcel.DetailShowStatsLength()):
                    self.detailShowStats.append(characterStatsDetailExcel.DetailShowStats(i))
            else:
                self.detailShowStats = characterStatsDetailExcel.DetailShowStatsAsNumpy()
        if not characterStatsDetailExcel.IsStatsPercentIsNone():
            if np is None:
                self.isStatsPercent = []
                for i in range(characterStatsDetailExcel.IsStatsPercentLength()):
                    self.isStatsPercent.append(characterStatsDetailExcel.IsStatsPercent(i))
            else:
                self.isStatsPercent = characterStatsDetailExcel.IsStatsPercentAsNumpy()

    # CharacterStatsDetailExcelT
    def Pack(self, builder):
        if self.detailShowStats is not None:
            if np is not None and type(self.detailShowStats) is np.ndarray:
                detailShowStats = builder.CreateNumpyVector(self.detailShowStats)
            else:
                CharacterStatsDetailExcelStartDetailShowStatsVector(builder, len(self.detailShowStats))
                for i in reversed(range(len(self.detailShowStats))):
                    builder.PrependInt32(self.detailShowStats[i])
                detailShowStats = builder.EndVector()
        if self.isStatsPercent is not None:
            if np is not None and type(self.isStatsPercent) is np.ndarray:
                isStatsPercent = builder.CreateNumpyVector(self.isStatsPercent)
            else:
                CharacterStatsDetailExcelStartIsStatsPercentVector(builder, len(self.isStatsPercent))
                for i in reversed(range(len(self.isStatsPercent))):
                    builder.PrependBool(self.isStatsPercent[i])
                isStatsPercent = builder.EndVector()
        CharacterStatsDetailExcelStart(builder)
        CharacterStatsDetailExcelAddId(builder, self.id)
        if self.detailShowStats is not None:
            CharacterStatsDetailExcelAddDetailShowStats(builder, detailShowStats)
        if self.isStatsPercent is not None:
            CharacterStatsDetailExcelAddIsStatsPercent(builder, isStatsPercent)
        characterStatsDetailExcel = CharacterStatsDetailExcelEnd(builder)
        return characterStatsDetailExcel
