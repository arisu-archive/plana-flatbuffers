# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class CheatCodeListExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CheatCodeListExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCheatCodeListExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # CheatCodeListExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CheatCodeListExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # CheatCodeListExcel
    def CheatCode(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # CheatCodeListExcel
    def CheatCodeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CheatCodeListExcel
    def CheatCodeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # CheatCodeListExcel
    def InputTitle(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # CheatCodeListExcel
    def InputTitleLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CheatCodeListExcel
    def InputTitleIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # CheatCodeListExcel
    def Desc(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def CheatCodeListExcelStart(builder):
    builder.StartObject(4)

def Start(builder):
    CheatCodeListExcelStart(builder)

def CheatCodeListExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    CheatCodeListExcelAddId(builder, id)

def CheatCodeListExcelAddCheatCode(builder, cheatCode):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(cheatCode), 0)

def AddCheatCode(builder, cheatCode):
    CheatCodeListExcelAddCheatCode(builder, cheatCode)

def CheatCodeListExcelStartCheatCodeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartCheatCodeVector(builder, numElems):
    return CheatCodeListExcelStartCheatCodeVector(builder, numElems)

def CheatCodeListExcelAddInputTitle(builder, inputTitle):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(inputTitle), 0)

def AddInputTitle(builder, inputTitle):
    CheatCodeListExcelAddInputTitle(builder, inputTitle)

def CheatCodeListExcelStartInputTitleVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartInputTitleVector(builder, numElems):
    return CheatCodeListExcelStartInputTitleVector(builder, numElems)

def CheatCodeListExcelAddDesc(builder, desc):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(desc), 0)

def AddDesc(builder, desc):
    CheatCodeListExcelAddDesc(builder, desc)

def CheatCodeListExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return CheatCodeListExcelEnd(builder)

try:
    from typing import List
except:
    pass

class CheatCodeListExcelT(object):

    # CheatCodeListExcelT
    def __init__(
        self,
        id = 0,
        cheatCode = None,
        inputTitle = None,
        desc = None,
    ):
        self.id = id  # type: int
        self.cheatCode = cheatCode  # type: Optional[List[Optional[str]]]
        self.inputTitle = inputTitle  # type: Optional[List[Optional[str]]]
        self.desc = desc  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        cheatCodeListExcel = CheatCodeListExcel()
        cheatCodeListExcel.Init(buf, pos)
        return cls.InitFromObj(cheatCodeListExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, cheatCodeListExcel):
        x = CheatCodeListExcelT()
        x._UnPack(cheatCodeListExcel)
        return x

    # CheatCodeListExcelT
    def _UnPack(self, cheatCodeListExcel):
        if cheatCodeListExcel is None:
            return
        self.id = cheatCodeListExcel.Id()
        if not cheatCodeListExcel.CheatCodeIsNone():
            self.cheatCode = []
            for i in range(cheatCodeListExcel.CheatCodeLength()):
                self.cheatCode.append(cheatCodeListExcel.CheatCode(i))
        if not cheatCodeListExcel.InputTitleIsNone():
            self.inputTitle = []
            for i in range(cheatCodeListExcel.InputTitleLength()):
                self.inputTitle.append(cheatCodeListExcel.InputTitle(i))
        self.desc = cheatCodeListExcel.Desc()

    # CheatCodeListExcelT
    def Pack(self, builder):
        if self.cheatCode is not None:
            cheatCodelist = []
            for i in range(len(self.cheatCode)):
                cheatCodelist.append(builder.CreateString(self.cheatCode[i]))
            CheatCodeListExcelStartCheatCodeVector(builder, len(self.cheatCode))
            for i in reversed(range(len(self.cheatCode))):
                builder.PrependUOffsetTRelative(cheatCodelist[i])
            cheatCode = builder.EndVector()
        if self.inputTitle is not None:
            inputTitlelist = []
            for i in range(len(self.inputTitle)):
                inputTitlelist.append(builder.CreateString(self.inputTitle[i]))
            CheatCodeListExcelStartInputTitleVector(builder, len(self.inputTitle))
            for i in reversed(range(len(self.inputTitle))):
                builder.PrependUOffsetTRelative(inputTitlelist[i])
            inputTitle = builder.EndVector()
        if self.desc is not None:
            desc = builder.CreateString(self.desc)
        CheatCodeListExcelStart(builder)
        CheatCodeListExcelAddId(builder, self.id)
        if self.cheatCode is not None:
            CheatCodeListExcelAddCheatCode(builder, cheatCode)
        if self.inputTitle is not None:
            CheatCodeListExcelAddInputTitle(builder, inputTitle)
        if self.desc is not None:
            CheatCodeListExcelAddDesc(builder, desc)
        cheatCodeListExcel = CheatCodeListExcelEnd(builder)
        return cheatCodeListExcel
