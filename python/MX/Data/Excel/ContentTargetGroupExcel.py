# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ContentTargetGroupExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ContentTargetGroupExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsContentTargetGroupExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ContentTargetGroupExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ContentTargetGroupExcel
    def TargetGroup(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ContentTargetGroupExcel
    def AccountType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # ContentTargetGroupExcel
    def AccountTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # ContentTargetGroupExcel
    def AccountTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ContentTargetGroupExcel
    def AccountTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def ContentTargetGroupExcelStart(builder):
    builder.StartObject(2)

def Start(builder):
    ContentTargetGroupExcelStart(builder)

def ContentTargetGroupExcelAddTargetGroup(builder, targetGroup):
    builder.PrependInt32Slot(0, targetGroup, 0)

def AddTargetGroup(builder, targetGroup):
    ContentTargetGroupExcelAddTargetGroup(builder, targetGroup)

def ContentTargetGroupExcelAddAccountType(builder, accountType):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(accountType), 0)

def AddAccountType(builder, accountType):
    ContentTargetGroupExcelAddAccountType(builder, accountType)

def ContentTargetGroupExcelStartAccountTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartAccountTypeVector(builder, numElems):
    return ContentTargetGroupExcelStartAccountTypeVector(builder, numElems)

def ContentTargetGroupExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return ContentTargetGroupExcelEnd(builder)

try:
    from typing import List
except:
    pass

class ContentTargetGroupExcelT(object):

    # ContentTargetGroupExcelT
    def __init__(
        self,
        targetGroup = 0,
        accountType = None,
    ):
        self.targetGroup = targetGroup  # type: int
        self.accountType = accountType  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        contentTargetGroupExcel = ContentTargetGroupExcel()
        contentTargetGroupExcel.Init(buf, pos)
        return cls.InitFromObj(contentTargetGroupExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, contentTargetGroupExcel):
        x = ContentTargetGroupExcelT()
        x._UnPack(contentTargetGroupExcel)
        return x

    # ContentTargetGroupExcelT
    def _UnPack(self, contentTargetGroupExcel):
        if contentTargetGroupExcel is None:
            return
        self.targetGroup = contentTargetGroupExcel.TargetGroup()
        if not contentTargetGroupExcel.AccountTypeIsNone():
            if np is None:
                self.accountType = []
                for i in range(contentTargetGroupExcel.AccountTypeLength()):
                    self.accountType.append(contentTargetGroupExcel.AccountType(i))
            else:
                self.accountType = contentTargetGroupExcel.AccountTypeAsNumpy()

    # ContentTargetGroupExcelT
    def Pack(self, builder):
        if self.accountType is not None:
            if np is not None and type(self.accountType) is np.ndarray:
                accountType = builder.CreateNumpyVector(self.accountType)
            else:
                ContentTargetGroupExcelStartAccountTypeVector(builder, len(self.accountType))
                for i in reversed(range(len(self.accountType))):
                    builder.PrependInt32(self.accountType[i])
                accountType = builder.EndVector()
        ContentTargetGroupExcelStart(builder)
        ContentTargetGroupExcelAddTargetGroup(builder, self.targetGroup)
        if self.accountType is not None:
            ContentTargetGroupExcelAddAccountType(builder, accountType)
        contentTargetGroupExcel = ContentTargetGroupExcelEnd(builder)
        return contentTargetGroupExcel
