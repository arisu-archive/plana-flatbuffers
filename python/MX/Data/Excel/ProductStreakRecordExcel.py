# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ProductStreakRecordExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ProductStreakRecordExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsProductStreakRecordExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ProductStreakRecordExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ProductStreakRecordExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductStreakRecordExcel
    def ProductId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ProductStreakRecordExcel
    def StoreType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ProductStreakRecordExcel
    def Price(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductStreakRecordExcel
    def PurchaseCountLimit(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductStreakRecordExcel
    def StreakRecordId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

def ProductStreakRecordExcelStart(builder):
    builder.StartObject(6)

def Start(builder):
    ProductStreakRecordExcelStart(builder)

def ProductStreakRecordExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    ProductStreakRecordExcelAddId(builder, id)

def ProductStreakRecordExcelAddProductId(builder, productId):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(productId), 0)

def AddProductId(builder, productId):
    ProductStreakRecordExcelAddProductId(builder, productId)

def ProductStreakRecordExcelAddStoreType(builder, storeType):
    builder.PrependInt32Slot(2, storeType, 0)

def AddStoreType(builder, storeType):
    ProductStreakRecordExcelAddStoreType(builder, storeType)

def ProductStreakRecordExcelAddPrice(builder, price):
    builder.PrependInt64Slot(3, price, 0)

def AddPrice(builder, price):
    ProductStreakRecordExcelAddPrice(builder, price)

def ProductStreakRecordExcelAddPurchaseCountLimit(builder, purchaseCountLimit):
    builder.PrependInt64Slot(4, purchaseCountLimit, 0)

def AddPurchaseCountLimit(builder, purchaseCountLimit):
    ProductStreakRecordExcelAddPurchaseCountLimit(builder, purchaseCountLimit)

def ProductStreakRecordExcelAddStreakRecordId(builder, streakRecordId):
    builder.PrependInt64Slot(5, streakRecordId, 0)

def AddStreakRecordId(builder, streakRecordId):
    ProductStreakRecordExcelAddStreakRecordId(builder, streakRecordId)

def ProductStreakRecordExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return ProductStreakRecordExcelEnd(builder)


class ProductStreakRecordExcelT(object):

    # ProductStreakRecordExcelT
    def __init__(
        self,
        id = 0,
        productId = None,
        storeType = 0,
        price = 0,
        purchaseCountLimit = 0,
        streakRecordId = 0,
    ):
        self.id = id  # type: int
        self.productId = productId  # type: Optional[str]
        self.storeType = storeType  # type: int
        self.price = price  # type: int
        self.purchaseCountLimit = purchaseCountLimit  # type: int
        self.streakRecordId = streakRecordId  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        productStreakRecordExcel = ProductStreakRecordExcel()
        productStreakRecordExcel.Init(buf, pos)
        return cls.InitFromObj(productStreakRecordExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, productStreakRecordExcel):
        x = ProductStreakRecordExcelT()
        x._UnPack(productStreakRecordExcel)
        return x

    # ProductStreakRecordExcelT
    def _UnPack(self, productStreakRecordExcel):
        if productStreakRecordExcel is None:
            return
        self.id = productStreakRecordExcel.Id()
        self.productId = productStreakRecordExcel.ProductId()
        self.storeType = productStreakRecordExcel.StoreType()
        self.price = productStreakRecordExcel.Price()
        self.purchaseCountLimit = productStreakRecordExcel.PurchaseCountLimit()
        self.streakRecordId = productStreakRecordExcel.StreakRecordId()

    # ProductStreakRecordExcelT
    def Pack(self, builder):
        if self.productId is not None:
            productId = builder.CreateString(self.productId)
        ProductStreakRecordExcelStart(builder)
        ProductStreakRecordExcelAddId(builder, self.id)
        if self.productId is not None:
            ProductStreakRecordExcelAddProductId(builder, productId)
        ProductStreakRecordExcelAddStoreType(builder, self.storeType)
        ProductStreakRecordExcelAddPrice(builder, self.price)
        ProductStreakRecordExcelAddPurchaseCountLimit(builder, self.purchaseCountLimit)
        ProductStreakRecordExcelAddStreakRecordId(builder, self.streakRecordId)
        productStreakRecordExcel = ProductStreakRecordExcelEnd(builder)
        return productStreakRecordExcel
