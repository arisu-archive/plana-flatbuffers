# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ProductDailyRecordRewardExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ProductDailyRecordRewardExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsProductDailyRecordRewardExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ProductDailyRecordRewardExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ProductDailyRecordRewardExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductDailyRecordRewardExcel
    def Day(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ProductDailyRecordRewardExcel
    def RewardParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # ProductDailyRecordRewardExcel
    def RewardParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # ProductDailyRecordRewardExcel
    def RewardParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductDailyRecordRewardExcel
    def RewardParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # ProductDailyRecordRewardExcel
    def RewardId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductDailyRecordRewardExcel
    def RewardIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductDailyRecordRewardExcel
    def RewardIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductDailyRecordRewardExcel
    def RewardIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # ProductDailyRecordRewardExcel
    def RewardAmount(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductDailyRecordRewardExcel
    def RewardAmountAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductDailyRecordRewardExcel
    def RewardAmountLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductDailyRecordRewardExcel
    def RewardAmountIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def ProductDailyRecordRewardExcelStart(builder):
    builder.StartObject(5)

def Start(builder):
    ProductDailyRecordRewardExcelStart(builder)

def ProductDailyRecordRewardExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    ProductDailyRecordRewardExcelAddId(builder, id)

def ProductDailyRecordRewardExcelAddDay(builder, day):
    builder.PrependInt32Slot(1, day, 0)

def AddDay(builder, day):
    ProductDailyRecordRewardExcelAddDay(builder, day)

def ProductDailyRecordRewardExcelAddRewardParcelType(builder, rewardParcelType):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(rewardParcelType), 0)

def AddRewardParcelType(builder, rewardParcelType):
    ProductDailyRecordRewardExcelAddRewardParcelType(builder, rewardParcelType)

def ProductDailyRecordRewardExcelStartRewardParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRewardParcelTypeVector(builder, numElems):
    return ProductDailyRecordRewardExcelStartRewardParcelTypeVector(builder, numElems)

def ProductDailyRecordRewardExcelAddRewardId(builder, rewardId):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(rewardId), 0)

def AddRewardId(builder, rewardId):
    ProductDailyRecordRewardExcelAddRewardId(builder, rewardId)

def ProductDailyRecordRewardExcelStartRewardIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartRewardIdVector(builder, numElems):
    return ProductDailyRecordRewardExcelStartRewardIdVector(builder, numElems)

def ProductDailyRecordRewardExcelAddRewardAmount(builder, rewardAmount):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(rewardAmount), 0)

def AddRewardAmount(builder, rewardAmount):
    ProductDailyRecordRewardExcelAddRewardAmount(builder, rewardAmount)

def ProductDailyRecordRewardExcelStartRewardAmountVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartRewardAmountVector(builder, numElems):
    return ProductDailyRecordRewardExcelStartRewardAmountVector(builder, numElems)

def ProductDailyRecordRewardExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return ProductDailyRecordRewardExcelEnd(builder)

try:
    from typing import List
except:
    pass

class ProductDailyRecordRewardExcelT(object):

    # ProductDailyRecordRewardExcelT
    def __init__(
        self,
        id = 0,
        day = 0,
        rewardParcelType = None,
        rewardId = None,
        rewardAmount = None,
    ):
        self.id = id  # type: int
        self.day = day  # type: int
        self.rewardParcelType = rewardParcelType  # type: Optional[List[int]]
        self.rewardId = rewardId  # type: Optional[List[int]]
        self.rewardAmount = rewardAmount  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        productDailyRecordRewardExcel = ProductDailyRecordRewardExcel()
        productDailyRecordRewardExcel.Init(buf, pos)
        return cls.InitFromObj(productDailyRecordRewardExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, productDailyRecordRewardExcel):
        x = ProductDailyRecordRewardExcelT()
        x._UnPack(productDailyRecordRewardExcel)
        return x

    # ProductDailyRecordRewardExcelT
    def _UnPack(self, productDailyRecordRewardExcel):
        if productDailyRecordRewardExcel is None:
            return
        self.id = productDailyRecordRewardExcel.Id()
        self.day = productDailyRecordRewardExcel.Day()
        if not productDailyRecordRewardExcel.RewardParcelTypeIsNone():
            if np is None:
                self.rewardParcelType = []
                for i in range(productDailyRecordRewardExcel.RewardParcelTypeLength()):
                    self.rewardParcelType.append(productDailyRecordRewardExcel.RewardParcelType(i))
            else:
                self.rewardParcelType = productDailyRecordRewardExcel.RewardParcelTypeAsNumpy()
        if not productDailyRecordRewardExcel.RewardIdIsNone():
            if np is None:
                self.rewardId = []
                for i in range(productDailyRecordRewardExcel.RewardIdLength()):
                    self.rewardId.append(productDailyRecordRewardExcel.RewardId(i))
            else:
                self.rewardId = productDailyRecordRewardExcel.RewardIdAsNumpy()
        if not productDailyRecordRewardExcel.RewardAmountIsNone():
            if np is None:
                self.rewardAmount = []
                for i in range(productDailyRecordRewardExcel.RewardAmountLength()):
                    self.rewardAmount.append(productDailyRecordRewardExcel.RewardAmount(i))
            else:
                self.rewardAmount = productDailyRecordRewardExcel.RewardAmountAsNumpy()

    # ProductDailyRecordRewardExcelT
    def Pack(self, builder):
        if self.rewardParcelType is not None:
            if np is not None and type(self.rewardParcelType) is np.ndarray:
                rewardParcelType = builder.CreateNumpyVector(self.rewardParcelType)
            else:
                ProductDailyRecordRewardExcelStartRewardParcelTypeVector(builder, len(self.rewardParcelType))
                for i in reversed(range(len(self.rewardParcelType))):
                    builder.PrependInt32(self.rewardParcelType[i])
                rewardParcelType = builder.EndVector()
        if self.rewardId is not None:
            if np is not None and type(self.rewardId) is np.ndarray:
                rewardId = builder.CreateNumpyVector(self.rewardId)
            else:
                ProductDailyRecordRewardExcelStartRewardIdVector(builder, len(self.rewardId))
                for i in reversed(range(len(self.rewardId))):
                    builder.PrependInt64(self.rewardId[i])
                rewardId = builder.EndVector()
        if self.rewardAmount is not None:
            if np is not None and type(self.rewardAmount) is np.ndarray:
                rewardAmount = builder.CreateNumpyVector(self.rewardAmount)
            else:
                ProductDailyRecordRewardExcelStartRewardAmountVector(builder, len(self.rewardAmount))
                for i in reversed(range(len(self.rewardAmount))):
                    builder.PrependInt64(self.rewardAmount[i])
                rewardAmount = builder.EndVector()
        ProductDailyRecordRewardExcelStart(builder)
        ProductDailyRecordRewardExcelAddId(builder, self.id)
        ProductDailyRecordRewardExcelAddDay(builder, self.day)
        if self.rewardParcelType is not None:
            ProductDailyRecordRewardExcelAddRewardParcelType(builder, rewardParcelType)
        if self.rewardId is not None:
            ProductDailyRecordRewardExcelAddRewardId(builder, rewardId)
        if self.rewardAmount is not None:
            ProductDailyRecordRewardExcelAddRewardAmount(builder, rewardAmount)
        productDailyRecordRewardExcel = ProductDailyRecordRewardExcelEnd(builder)
        return productDailyRecordRewardExcel
