# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class StreakRecordInfoExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = StreakRecordInfoExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsStreakRecordInfoExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # StreakRecordInfoExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # StreakRecordInfoExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # StreakRecordInfoExcel
    def DaySize(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # StreakRecordInfoExcel
    def StartDate(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # StreakRecordInfoExcel
    def EndDate(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # StreakRecordInfoExcel
    def FreeRewardGroupId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # StreakRecordInfoExcel
    def PurchaseRewardGroupId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # StreakRecordInfoExcel
    def TitleImagePath(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # StreakRecordInfoExcel
    def DecorationImagePath(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # StreakRecordInfoExcel
    def RestoreCostParcelType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # StreakRecordInfoExcel
    def RestoreCostParcelId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # StreakRecordInfoExcel
    def RestoreCostParcelAmount(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # StreakRecordInfoExcel
    def CostIncreaseDayMax(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

def StreakRecordInfoExcelStart(builder):
    builder.StartObject(12)

def Start(builder):
    StreakRecordInfoExcelStart(builder)

def StreakRecordInfoExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    StreakRecordInfoExcelAddId(builder, id)

def StreakRecordInfoExcelAddDaySize(builder, daySize):
    builder.PrependInt64Slot(1, daySize, 0)

def AddDaySize(builder, daySize):
    StreakRecordInfoExcelAddDaySize(builder, daySize)

def StreakRecordInfoExcelAddStartDate(builder, startDate):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(startDate), 0)

def AddStartDate(builder, startDate):
    StreakRecordInfoExcelAddStartDate(builder, startDate)

def StreakRecordInfoExcelAddEndDate(builder, endDate):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(endDate), 0)

def AddEndDate(builder, endDate):
    StreakRecordInfoExcelAddEndDate(builder, endDate)

def StreakRecordInfoExcelAddFreeRewardGroupId(builder, freeRewardGroupId):
    builder.PrependInt64Slot(4, freeRewardGroupId, 0)

def AddFreeRewardGroupId(builder, freeRewardGroupId):
    StreakRecordInfoExcelAddFreeRewardGroupId(builder, freeRewardGroupId)

def StreakRecordInfoExcelAddPurchaseRewardGroupId(builder, purchaseRewardGroupId):
    builder.PrependInt64Slot(5, purchaseRewardGroupId, 0)

def AddPurchaseRewardGroupId(builder, purchaseRewardGroupId):
    StreakRecordInfoExcelAddPurchaseRewardGroupId(builder, purchaseRewardGroupId)

def StreakRecordInfoExcelAddTitleImagePath(builder, titleImagePath):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(titleImagePath), 0)

def AddTitleImagePath(builder, titleImagePath):
    StreakRecordInfoExcelAddTitleImagePath(builder, titleImagePath)

def StreakRecordInfoExcelAddDecorationImagePath(builder, decorationImagePath):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(decorationImagePath), 0)

def AddDecorationImagePath(builder, decorationImagePath):
    StreakRecordInfoExcelAddDecorationImagePath(builder, decorationImagePath)

def StreakRecordInfoExcelAddRestoreCostParcelType(builder, restoreCostParcelType):
    builder.PrependInt32Slot(8, restoreCostParcelType, 0)

def AddRestoreCostParcelType(builder, restoreCostParcelType):
    StreakRecordInfoExcelAddRestoreCostParcelType(builder, restoreCostParcelType)

def StreakRecordInfoExcelAddRestoreCostParcelId(builder, restoreCostParcelId):
    builder.PrependInt64Slot(9, restoreCostParcelId, 0)

def AddRestoreCostParcelId(builder, restoreCostParcelId):
    StreakRecordInfoExcelAddRestoreCostParcelId(builder, restoreCostParcelId)

def StreakRecordInfoExcelAddRestoreCostParcelAmount(builder, restoreCostParcelAmount):
    builder.PrependInt64Slot(10, restoreCostParcelAmount, 0)

def AddRestoreCostParcelAmount(builder, restoreCostParcelAmount):
    StreakRecordInfoExcelAddRestoreCostParcelAmount(builder, restoreCostParcelAmount)

def StreakRecordInfoExcelAddCostIncreaseDayMax(builder, costIncreaseDayMax):
    builder.PrependInt64Slot(11, costIncreaseDayMax, 0)

def AddCostIncreaseDayMax(builder, costIncreaseDayMax):
    StreakRecordInfoExcelAddCostIncreaseDayMax(builder, costIncreaseDayMax)

def StreakRecordInfoExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return StreakRecordInfoExcelEnd(builder)


class StreakRecordInfoExcelT(object):

    # StreakRecordInfoExcelT
    def __init__(
        self,
        id = 0,
        daySize = 0,
        startDate = None,
        endDate = None,
        freeRewardGroupId = 0,
        purchaseRewardGroupId = 0,
        titleImagePath = None,
        decorationImagePath = None,
        restoreCostParcelType = 0,
        restoreCostParcelId = 0,
        restoreCostParcelAmount = 0,
        costIncreaseDayMax = 0,
    ):
        self.id = id  # type: int
        self.daySize = daySize  # type: int
        self.startDate = startDate  # type: Optional[str]
        self.endDate = endDate  # type: Optional[str]
        self.freeRewardGroupId = freeRewardGroupId  # type: int
        self.purchaseRewardGroupId = purchaseRewardGroupId  # type: int
        self.titleImagePath = titleImagePath  # type: Optional[str]
        self.decorationImagePath = decorationImagePath  # type: Optional[str]
        self.restoreCostParcelType = restoreCostParcelType  # type: int
        self.restoreCostParcelId = restoreCostParcelId  # type: int
        self.restoreCostParcelAmount = restoreCostParcelAmount  # type: int
        self.costIncreaseDayMax = costIncreaseDayMax  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        streakRecordInfoExcel = StreakRecordInfoExcel()
        streakRecordInfoExcel.Init(buf, pos)
        return cls.InitFromObj(streakRecordInfoExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, streakRecordInfoExcel):
        x = StreakRecordInfoExcelT()
        x._UnPack(streakRecordInfoExcel)
        return x

    # StreakRecordInfoExcelT
    def _UnPack(self, streakRecordInfoExcel):
        if streakRecordInfoExcel is None:
            return
        self.id = streakRecordInfoExcel.Id()
        self.daySize = streakRecordInfoExcel.DaySize()
        self.startDate = streakRecordInfoExcel.StartDate()
        self.endDate = streakRecordInfoExcel.EndDate()
        self.freeRewardGroupId = streakRecordInfoExcel.FreeRewardGroupId()
        self.purchaseRewardGroupId = streakRecordInfoExcel.PurchaseRewardGroupId()
        self.titleImagePath = streakRecordInfoExcel.TitleImagePath()
        self.decorationImagePath = streakRecordInfoExcel.DecorationImagePath()
        self.restoreCostParcelType = streakRecordInfoExcel.RestoreCostParcelType()
        self.restoreCostParcelId = streakRecordInfoExcel.RestoreCostParcelId()
        self.restoreCostParcelAmount = streakRecordInfoExcel.RestoreCostParcelAmount()
        self.costIncreaseDayMax = streakRecordInfoExcel.CostIncreaseDayMax()

    # StreakRecordInfoExcelT
    def Pack(self, builder):
        if self.startDate is not None:
            startDate = builder.CreateString(self.startDate)
        if self.endDate is not None:
            endDate = builder.CreateString(self.endDate)
        if self.titleImagePath is not None:
            titleImagePath = builder.CreateString(self.titleImagePath)
        if self.decorationImagePath is not None:
            decorationImagePath = builder.CreateString(self.decorationImagePath)
        StreakRecordInfoExcelStart(builder)
        StreakRecordInfoExcelAddId(builder, self.id)
        StreakRecordInfoExcelAddDaySize(builder, self.daySize)
        if self.startDate is not None:
            StreakRecordInfoExcelAddStartDate(builder, startDate)
        if self.endDate is not None:
            StreakRecordInfoExcelAddEndDate(builder, endDate)
        StreakRecordInfoExcelAddFreeRewardGroupId(builder, self.freeRewardGroupId)
        StreakRecordInfoExcelAddPurchaseRewardGroupId(builder, self.purchaseRewardGroupId)
        if self.titleImagePath is not None:
            StreakRecordInfoExcelAddTitleImagePath(builder, titleImagePath)
        if self.decorationImagePath is not None:
            StreakRecordInfoExcelAddDecorationImagePath(builder, decorationImagePath)
        StreakRecordInfoExcelAddRestoreCostParcelType(builder, self.restoreCostParcelType)
        StreakRecordInfoExcelAddRestoreCostParcelId(builder, self.restoreCostParcelId)
        StreakRecordInfoExcelAddRestoreCostParcelAmount(builder, self.restoreCostParcelAmount)
        StreakRecordInfoExcelAddCostIncreaseDayMax(builder, self.costIncreaseDayMax)
        streakRecordInfoExcel = StreakRecordInfoExcelEnd(builder)
        return streakRecordInfoExcel
