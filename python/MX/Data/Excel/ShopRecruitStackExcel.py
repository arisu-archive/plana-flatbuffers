# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ShopRecruitStackExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ShopRecruitStackExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsShopRecruitStackExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ShopRecruitStackExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ShopRecruitStackExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ShopRecruitStackExcel
    def RecruitStackItemId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ShopRecruitStackExcel
    def HalfStack(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ShopRecruitStackExcel
    def FullStack(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ShopRecruitStackExcel
    def StackColor(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # ShopRecruitStackExcel
    def StackColorLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ShopRecruitStackExcel
    def StackColorIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def ShopRecruitStackExcelStart(builder):
    builder.StartObject(5)

def Start(builder):
    ShopRecruitStackExcelStart(builder)

def ShopRecruitStackExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    ShopRecruitStackExcelAddId(builder, id)

def ShopRecruitStackExcelAddRecruitStackItemId(builder, recruitStackItemId):
    builder.PrependInt64Slot(1, recruitStackItemId, 0)

def AddRecruitStackItemId(builder, recruitStackItemId):
    ShopRecruitStackExcelAddRecruitStackItemId(builder, recruitStackItemId)

def ShopRecruitStackExcelAddHalfStack(builder, halfStack):
    builder.PrependInt64Slot(2, halfStack, 0)

def AddHalfStack(builder, halfStack):
    ShopRecruitStackExcelAddHalfStack(builder, halfStack)

def ShopRecruitStackExcelAddFullStack(builder, fullStack):
    builder.PrependInt64Slot(3, fullStack, 0)

def AddFullStack(builder, fullStack):
    ShopRecruitStackExcelAddFullStack(builder, fullStack)

def ShopRecruitStackExcelAddStackColor(builder, stackColor):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(stackColor), 0)

def AddStackColor(builder, stackColor):
    ShopRecruitStackExcelAddStackColor(builder, stackColor)

def ShopRecruitStackExcelStartStackColorVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartStackColorVector(builder, numElems):
    return ShopRecruitStackExcelStartStackColorVector(builder, numElems)

def ShopRecruitStackExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return ShopRecruitStackExcelEnd(builder)

try:
    from typing import List
except:
    pass

class ShopRecruitStackExcelT(object):

    # ShopRecruitStackExcelT
    def __init__(
        self,
        id = 0,
        recruitStackItemId = 0,
        halfStack = 0,
        fullStack = 0,
        stackColor = None,
    ):
        self.id = id  # type: int
        self.recruitStackItemId = recruitStackItemId  # type: int
        self.halfStack = halfStack  # type: int
        self.fullStack = fullStack  # type: int
        self.stackColor = stackColor  # type: Optional[List[Optional[str]]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        shopRecruitStackExcel = ShopRecruitStackExcel()
        shopRecruitStackExcel.Init(buf, pos)
        return cls.InitFromObj(shopRecruitStackExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, shopRecruitStackExcel):
        x = ShopRecruitStackExcelT()
        x._UnPack(shopRecruitStackExcel)
        return x

    # ShopRecruitStackExcelT
    def _UnPack(self, shopRecruitStackExcel):
        if shopRecruitStackExcel is None:
            return
        self.id = shopRecruitStackExcel.Id()
        self.recruitStackItemId = shopRecruitStackExcel.RecruitStackItemId()
        self.halfStack = shopRecruitStackExcel.HalfStack()
        self.fullStack = shopRecruitStackExcel.FullStack()
        if not shopRecruitStackExcel.StackColorIsNone():
            self.stackColor = []
            for i in range(shopRecruitStackExcel.StackColorLength()):
                self.stackColor.append(shopRecruitStackExcel.StackColor(i))

    # ShopRecruitStackExcelT
    def Pack(self, builder):
        if self.stackColor is not None:
            stackColorlist = []
            for i in range(len(self.stackColor)):
                stackColorlist.append(builder.CreateString(self.stackColor[i]))
            ShopRecruitStackExcelStartStackColorVector(builder, len(self.stackColor))
            for i in reversed(range(len(self.stackColor))):
                builder.PrependUOffsetTRelative(stackColorlist[i])
            stackColor = builder.EndVector()
        ShopRecruitStackExcelStart(builder)
        ShopRecruitStackExcelAddId(builder, self.id)
        ShopRecruitStackExcelAddRecruitStackItemId(builder, self.recruitStackItemId)
        ShopRecruitStackExcelAddHalfStack(builder, self.halfStack)
        ShopRecruitStackExcelAddFullStack(builder, self.fullStack)
        if self.stackColor is not None:
            ShopRecruitStackExcelAddStackColor(builder, stackColor)
        shopRecruitStackExcel = ShopRecruitStackExcelEnd(builder)
        return shopRecruitStackExcel
