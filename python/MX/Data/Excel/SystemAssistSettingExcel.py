# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SystemAssistSettingExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SystemAssistSettingExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSystemAssistSettingExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # SystemAssistSettingExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SystemAssistSettingExcel
    def BattleType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # SystemAssistSettingExcel
    def StageId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # SystemAssistSettingExcel
    def CharacterSettingId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # SystemAssistSettingExcel
    def CharacterId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SystemAssistSettingExcel
    def CharacterIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # SystemAssistSettingExcel
    def CharacterIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SystemAssistSettingExcel
    def CharacterIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def SystemAssistSettingExcelStart(builder):
    builder.StartObject(4)

def Start(builder):
    SystemAssistSettingExcelStart(builder)

def SystemAssistSettingExcelAddBattleType(builder, battleType):
    builder.PrependInt32Slot(0, battleType, 0)

def AddBattleType(builder, battleType):
    SystemAssistSettingExcelAddBattleType(builder, battleType)

def SystemAssistSettingExcelAddStageId(builder, stageId):
    builder.PrependInt64Slot(1, stageId, 0)

def AddStageId(builder, stageId):
    SystemAssistSettingExcelAddStageId(builder, stageId)

def SystemAssistSettingExcelAddCharacterSettingId(builder, characterSettingId):
    builder.PrependInt64Slot(2, characterSettingId, 0)

def AddCharacterSettingId(builder, characterSettingId):
    SystemAssistSettingExcelAddCharacterSettingId(builder, characterSettingId)

def SystemAssistSettingExcelAddCharacterId(builder, characterId):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(characterId), 0)

def AddCharacterId(builder, characterId):
    SystemAssistSettingExcelAddCharacterId(builder, characterId)

def SystemAssistSettingExcelStartCharacterIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartCharacterIdVector(builder, numElems):
    return SystemAssistSettingExcelStartCharacterIdVector(builder, numElems)

def SystemAssistSettingExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return SystemAssistSettingExcelEnd(builder)

try:
    from typing import List
except:
    pass

class SystemAssistSettingExcelT(object):

    # SystemAssistSettingExcelT
    def __init__(
        self,
        battleType = 0,
        stageId = 0,
        characterSettingId = 0,
        characterId = None,
    ):
        self.battleType = battleType  # type: int
        self.stageId = stageId  # type: int
        self.characterSettingId = characterSettingId  # type: int
        self.characterId = characterId  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        systemAssistSettingExcel = SystemAssistSettingExcel()
        systemAssistSettingExcel.Init(buf, pos)
        return cls.InitFromObj(systemAssistSettingExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, systemAssistSettingExcel):
        x = SystemAssistSettingExcelT()
        x._UnPack(systemAssistSettingExcel)
        return x

    # SystemAssistSettingExcelT
    def _UnPack(self, systemAssistSettingExcel):
        if systemAssistSettingExcel is None:
            return
        self.battleType = systemAssistSettingExcel.BattleType()
        self.stageId = systemAssistSettingExcel.StageId()
        self.characterSettingId = systemAssistSettingExcel.CharacterSettingId()
        if not systemAssistSettingExcel.CharacterIdIsNone():
            if np is None:
                self.characterId = []
                for i in range(systemAssistSettingExcel.CharacterIdLength()):
                    self.characterId.append(systemAssistSettingExcel.CharacterId(i))
            else:
                self.characterId = systemAssistSettingExcel.CharacterIdAsNumpy()

    # SystemAssistSettingExcelT
    def Pack(self, builder):
        if self.characterId is not None:
            if np is not None and type(self.characterId) is np.ndarray:
                characterId = builder.CreateNumpyVector(self.characterId)
            else:
                SystemAssistSettingExcelStartCharacterIdVector(builder, len(self.characterId))
                for i in reversed(range(len(self.characterId))):
                    builder.PrependInt64(self.characterId[i])
                characterId = builder.EndVector()
        SystemAssistSettingExcelStart(builder)
        SystemAssistSettingExcelAddBattleType(builder, self.battleType)
        SystemAssistSettingExcelAddStageId(builder, self.stageId)
        SystemAssistSettingExcelAddCharacterSettingId(builder, self.characterSettingId)
        if self.characterId is not None:
            SystemAssistSettingExcelAddCharacterId(builder, characterId)
        systemAssistSettingExcel = SystemAssistSettingExcelEnd(builder)
        return systemAssistSettingExcel
