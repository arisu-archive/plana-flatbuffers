# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class EngraveTreeExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EngraveTreeExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEngraveTreeExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # EngraveTreeExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # EngraveTreeExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # EngraveTreeExcel
    def NodeIndex(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # EngraveTreeExcel
    def IsUnlock(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # EngraveTreeExcel
    def PreNodeIndex(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # EngraveTreeExcel
    def PreNodeIndexAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # EngraveTreeExcel
    def PreNodeIndexLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # EngraveTreeExcel
    def PreNodeIndexIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # EngraveTreeExcel
    def GradeType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # EngraveTreeExcel
    def LocalizeEtcId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # EngraveTreeExcel
    def Recipe(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # EngraveTreeExcel
    def MaxLevel(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # EngraveTreeExcel
    def LearnSkillSlot(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # EngraveTreeExcel
    def StatType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # EngraveTreeExcel
    def StatTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # EngraveTreeExcel
    def StatTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # EngraveTreeExcel
    def StatTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # EngraveTreeExcel
    def StatValue(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # EngraveTreeExcel
    def StatValueAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # EngraveTreeExcel
    def StatValueLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # EngraveTreeExcel
    def StatValueIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

def EngraveTreeExcelStart(builder):
    builder.StartObject(11)

def Start(builder):
    EngraveTreeExcelStart(builder)

def EngraveTreeExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    EngraveTreeExcelAddId(builder, id)

def EngraveTreeExcelAddNodeIndex(builder, nodeIndex):
    builder.PrependInt32Slot(1, nodeIndex, 0)

def AddNodeIndex(builder, nodeIndex):
    EngraveTreeExcelAddNodeIndex(builder, nodeIndex)

def EngraveTreeExcelAddIsUnlock(builder, isUnlock):
    builder.PrependBoolSlot(2, isUnlock, 0)

def AddIsUnlock(builder, isUnlock):
    EngraveTreeExcelAddIsUnlock(builder, isUnlock)

def EngraveTreeExcelAddPreNodeIndex(builder, preNodeIndex):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(preNodeIndex), 0)

def AddPreNodeIndex(builder, preNodeIndex):
    EngraveTreeExcelAddPreNodeIndex(builder, preNodeIndex)

def EngraveTreeExcelStartPreNodeIndexVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartPreNodeIndexVector(builder, numElems):
    return EngraveTreeExcelStartPreNodeIndexVector(builder, numElems)

def EngraveTreeExcelAddGradeType(builder, gradeType):
    builder.PrependInt32Slot(4, gradeType, 0)

def AddGradeType(builder, gradeType):
    EngraveTreeExcelAddGradeType(builder, gradeType)

def EngraveTreeExcelAddLocalizeEtcId(builder, localizeEtcId):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(localizeEtcId), 0)

def AddLocalizeEtcId(builder, localizeEtcId):
    EngraveTreeExcelAddLocalizeEtcId(builder, localizeEtcId)

def EngraveTreeExcelAddRecipe(builder, recipe):
    builder.PrependInt64Slot(6, recipe, 0)

def AddRecipe(builder, recipe):
    EngraveTreeExcelAddRecipe(builder, recipe)

def EngraveTreeExcelAddMaxLevel(builder, maxLevel):
    builder.PrependInt64Slot(7, maxLevel, 0)

def AddMaxLevel(builder, maxLevel):
    EngraveTreeExcelAddMaxLevel(builder, maxLevel)

def EngraveTreeExcelAddLearnSkillSlot(builder, learnSkillSlot):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(learnSkillSlot), 0)

def AddLearnSkillSlot(builder, learnSkillSlot):
    EngraveTreeExcelAddLearnSkillSlot(builder, learnSkillSlot)

def EngraveTreeExcelAddStatType(builder, statType):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(statType), 0)

def AddStatType(builder, statType):
    EngraveTreeExcelAddStatType(builder, statType)

def EngraveTreeExcelStartStatTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartStatTypeVector(builder, numElems):
    return EngraveTreeExcelStartStatTypeVector(builder, numElems)

def EngraveTreeExcelAddStatValue(builder, statValue):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(statValue), 0)

def AddStatValue(builder, statValue):
    EngraveTreeExcelAddStatValue(builder, statValue)

def EngraveTreeExcelStartStatValueVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartStatValueVector(builder, numElems):
    return EngraveTreeExcelStartStatValueVector(builder, numElems)

def EngraveTreeExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return EngraveTreeExcelEnd(builder)

try:
    from typing import List
except:
    pass

class EngraveTreeExcelT(object):

    # EngraveTreeExcelT
    def __init__(
        self,
        id = 0,
        nodeIndex = 0,
        isUnlock = False,
        preNodeIndex = None,
        gradeType = 0,
        localizeEtcId = None,
        recipe = 0,
        maxLevel = 0,
        learnSkillSlot = None,
        statType = None,
        statValue = None,
    ):
        self.id = id  # type: int
        self.nodeIndex = nodeIndex  # type: int
        self.isUnlock = isUnlock  # type: bool
        self.preNodeIndex = preNodeIndex  # type: Optional[List[int]]
        self.gradeType = gradeType  # type: int
        self.localizeEtcId = localizeEtcId  # type: Optional[str]
        self.recipe = recipe  # type: int
        self.maxLevel = maxLevel  # type: int
        self.learnSkillSlot = learnSkillSlot  # type: Optional[str]
        self.statType = statType  # type: Optional[List[int]]
        self.statValue = statValue  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        engraveTreeExcel = EngraveTreeExcel()
        engraveTreeExcel.Init(buf, pos)
        return cls.InitFromObj(engraveTreeExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, engraveTreeExcel):
        x = EngraveTreeExcelT()
        x._UnPack(engraveTreeExcel)
        return x

    # EngraveTreeExcelT
    def _UnPack(self, engraveTreeExcel):
        if engraveTreeExcel is None:
            return
        self.id = engraveTreeExcel.Id()
        self.nodeIndex = engraveTreeExcel.NodeIndex()
        self.isUnlock = engraveTreeExcel.IsUnlock()
        if not engraveTreeExcel.PreNodeIndexIsNone():
            if np is None:
                self.preNodeIndex = []
                for i in range(engraveTreeExcel.PreNodeIndexLength()):
                    self.preNodeIndex.append(engraveTreeExcel.PreNodeIndex(i))
            else:
                self.preNodeIndex = engraveTreeExcel.PreNodeIndexAsNumpy()
        self.gradeType = engraveTreeExcel.GradeType()
        self.localizeEtcId = engraveTreeExcel.LocalizeEtcId()
        self.recipe = engraveTreeExcel.Recipe()
        self.maxLevel = engraveTreeExcel.MaxLevel()
        self.learnSkillSlot = engraveTreeExcel.LearnSkillSlot()
        if not engraveTreeExcel.StatTypeIsNone():
            if np is None:
                self.statType = []
                for i in range(engraveTreeExcel.StatTypeLength()):
                    self.statType.append(engraveTreeExcel.StatType(i))
            else:
                self.statType = engraveTreeExcel.StatTypeAsNumpy()
        if not engraveTreeExcel.StatValueIsNone():
            if np is None:
                self.statValue = []
                for i in range(engraveTreeExcel.StatValueLength()):
                    self.statValue.append(engraveTreeExcel.StatValue(i))
            else:
                self.statValue = engraveTreeExcel.StatValueAsNumpy()

    # EngraveTreeExcelT
    def Pack(self, builder):
        if self.preNodeIndex is not None:
            if np is not None and type(self.preNodeIndex) is np.ndarray:
                preNodeIndex = builder.CreateNumpyVector(self.preNodeIndex)
            else:
                EngraveTreeExcelStartPreNodeIndexVector(builder, len(self.preNodeIndex))
                for i in reversed(range(len(self.preNodeIndex))):
                    builder.PrependInt32(self.preNodeIndex[i])
                preNodeIndex = builder.EndVector()
        if self.localizeEtcId is not None:
            localizeEtcId = builder.CreateString(self.localizeEtcId)
        if self.learnSkillSlot is not None:
            learnSkillSlot = builder.CreateString(self.learnSkillSlot)
        if self.statType is not None:
            if np is not None and type(self.statType) is np.ndarray:
                statType = builder.CreateNumpyVector(self.statType)
            else:
                EngraveTreeExcelStartStatTypeVector(builder, len(self.statType))
                for i in reversed(range(len(self.statType))):
                    builder.PrependInt32(self.statType[i])
                statType = builder.EndVector()
        if self.statValue is not None:
            if np is not None and type(self.statValue) is np.ndarray:
                statValue = builder.CreateNumpyVector(self.statValue)
            else:
                EngraveTreeExcelStartStatValueVector(builder, len(self.statValue))
                for i in reversed(range(len(self.statValue))):
                    builder.PrependInt64(self.statValue[i])
                statValue = builder.EndVector()
        EngraveTreeExcelStart(builder)
        EngraveTreeExcelAddId(builder, self.id)
        EngraveTreeExcelAddNodeIndex(builder, self.nodeIndex)
        EngraveTreeExcelAddIsUnlock(builder, self.isUnlock)
        if self.preNodeIndex is not None:
            EngraveTreeExcelAddPreNodeIndex(builder, preNodeIndex)
        EngraveTreeExcelAddGradeType(builder, self.gradeType)
        if self.localizeEtcId is not None:
            EngraveTreeExcelAddLocalizeEtcId(builder, localizeEtcId)
        EngraveTreeExcelAddRecipe(builder, self.recipe)
        EngraveTreeExcelAddMaxLevel(builder, self.maxLevel)
        if self.learnSkillSlot is not None:
            EngraveTreeExcelAddLearnSkillSlot(builder, learnSkillSlot)
        if self.statType is not None:
            EngraveTreeExcelAddStatType(builder, statType)
        if self.statValue is not None:
            EngraveTreeExcelAddStatValue(builder, statValue)
        engraveTreeExcel = EngraveTreeExcelEnd(builder)
        return engraveTreeExcel
