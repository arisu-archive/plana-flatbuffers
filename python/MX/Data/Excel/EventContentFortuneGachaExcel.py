# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class EventContentFortuneGachaExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = EventContentFortuneGachaExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsEventContentFortuneGachaExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # EventContentFortuneGachaExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # EventContentFortuneGachaExcel
    def FortuneGachaGroupId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # EventContentFortuneGachaExcel
    def LocalizeEtcId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # EventContentFortuneGachaExcel
    def NameImagePath(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # EventContentFortuneGachaExcel
    def IconPath(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def EventContentFortuneGachaExcelStart(builder):
    builder.StartObject(4)

def Start(builder):
    EventContentFortuneGachaExcelStart(builder)

def EventContentFortuneGachaExcelAddFortuneGachaGroupId(builder, fortuneGachaGroupId):
    builder.PrependInt32Slot(0, fortuneGachaGroupId, 0)

def AddFortuneGachaGroupId(builder, fortuneGachaGroupId):
    EventContentFortuneGachaExcelAddFortuneGachaGroupId(builder, fortuneGachaGroupId)

def EventContentFortuneGachaExcelAddLocalizeEtcId(builder, localizeEtcId):
    builder.PrependUint32Slot(1, localizeEtcId, 0)

def AddLocalizeEtcId(builder, localizeEtcId):
    EventContentFortuneGachaExcelAddLocalizeEtcId(builder, localizeEtcId)

def EventContentFortuneGachaExcelAddNameImagePath(builder, nameImagePath):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(nameImagePath), 0)

def AddNameImagePath(builder, nameImagePath):
    EventContentFortuneGachaExcelAddNameImagePath(builder, nameImagePath)

def EventContentFortuneGachaExcelAddIconPath(builder, iconPath):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(iconPath), 0)

def AddIconPath(builder, iconPath):
    EventContentFortuneGachaExcelAddIconPath(builder, iconPath)

def EventContentFortuneGachaExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return EventContentFortuneGachaExcelEnd(builder)


class EventContentFortuneGachaExcelT(object):

    # EventContentFortuneGachaExcelT
    def __init__(
        self,
        fortuneGachaGroupId = 0,
        localizeEtcId = 0,
        nameImagePath = None,
        iconPath = None,
    ):
        self.fortuneGachaGroupId = fortuneGachaGroupId  # type: int
        self.localizeEtcId = localizeEtcId  # type: int
        self.nameImagePath = nameImagePath  # type: Optional[str]
        self.iconPath = iconPath  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        eventContentFortuneGachaExcel = EventContentFortuneGachaExcel()
        eventContentFortuneGachaExcel.Init(buf, pos)
        return cls.InitFromObj(eventContentFortuneGachaExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, eventContentFortuneGachaExcel):
        x = EventContentFortuneGachaExcelT()
        x._UnPack(eventContentFortuneGachaExcel)
        return x

    # EventContentFortuneGachaExcelT
    def _UnPack(self, eventContentFortuneGachaExcel):
        if eventContentFortuneGachaExcel is None:
            return
        self.fortuneGachaGroupId = eventContentFortuneGachaExcel.FortuneGachaGroupId()
        self.localizeEtcId = eventContentFortuneGachaExcel.LocalizeEtcId()
        self.nameImagePath = eventContentFortuneGachaExcel.NameImagePath()
        self.iconPath = eventContentFortuneGachaExcel.IconPath()

    # EventContentFortuneGachaExcelT
    def Pack(self, builder):
        if self.nameImagePath is not None:
            nameImagePath = builder.CreateString(self.nameImagePath)
        if self.iconPath is not None:
            iconPath = builder.CreateString(self.iconPath)
        EventContentFortuneGachaExcelStart(builder)
        EventContentFortuneGachaExcelAddFortuneGachaGroupId(builder, self.fortuneGachaGroupId)
        EventContentFortuneGachaExcelAddLocalizeEtcId(builder, self.localizeEtcId)
        if self.nameImagePath is not None:
            EventContentFortuneGachaExcelAddNameImagePath(builder, nameImagePath)
        if self.iconPath is not None:
            EventContentFortuneGachaExcelAddIconPath(builder, iconPath)
        eventContentFortuneGachaExcel = EventContentFortuneGachaExcelEnd(builder)
        return eventContentFortuneGachaExcel
