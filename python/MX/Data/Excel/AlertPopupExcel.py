# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class AlertPopupExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = AlertPopupExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAlertPopupExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # AlertPopupExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # AlertPopupExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # AlertPopupExcel
    def CheckConfirmAble(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # AlertPopupExcel
    def SystemPopupTitle(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # AlertPopupExcel
    def SystemPopupDescription(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # AlertPopupExcel
    def SpoilerPopupTitle(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # AlertPopupExcel
    def SpoilerPopupDescription(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # AlertPopupExcel
    def PopupType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

def AlertPopupExcelStart(builder):
    builder.StartObject(7)

def Start(builder):
    AlertPopupExcelStart(builder)

def AlertPopupExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    AlertPopupExcelAddId(builder, id)

def AlertPopupExcelAddCheckConfirmAble(builder, checkConfirmAble):
    builder.PrependBoolSlot(1, checkConfirmAble, 0)

def AddCheckConfirmAble(builder, checkConfirmAble):
    AlertPopupExcelAddCheckConfirmAble(builder, checkConfirmAble)

def AlertPopupExcelAddSystemPopupTitle(builder, systemPopupTitle):
    builder.PrependUint32Slot(2, systemPopupTitle, 0)

def AddSystemPopupTitle(builder, systemPopupTitle):
    AlertPopupExcelAddSystemPopupTitle(builder, systemPopupTitle)

def AlertPopupExcelAddSystemPopupDescription(builder, systemPopupDescription):
    builder.PrependUint32Slot(3, systemPopupDescription, 0)

def AddSystemPopupDescription(builder, systemPopupDescription):
    AlertPopupExcelAddSystemPopupDescription(builder, systemPopupDescription)

def AlertPopupExcelAddSpoilerPopupTitle(builder, spoilerPopupTitle):
    builder.PrependUint32Slot(4, spoilerPopupTitle, 0)

def AddSpoilerPopupTitle(builder, spoilerPopupTitle):
    AlertPopupExcelAddSpoilerPopupTitle(builder, spoilerPopupTitle)

def AlertPopupExcelAddSpoilerPopupDescription(builder, spoilerPopupDescription):
    builder.PrependUint32Slot(5, spoilerPopupDescription, 0)

def AddSpoilerPopupDescription(builder, spoilerPopupDescription):
    AlertPopupExcelAddSpoilerPopupDescription(builder, spoilerPopupDescription)

def AlertPopupExcelAddPopupType(builder, popupType):
    builder.PrependInt32Slot(6, popupType, 0)

def AddPopupType(builder, popupType):
    AlertPopupExcelAddPopupType(builder, popupType)

def AlertPopupExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return AlertPopupExcelEnd(builder)


class AlertPopupExcelT(object):

    # AlertPopupExcelT
    def __init__(
        self,
        id = 0,
        checkConfirmAble = False,
        systemPopupTitle = 0,
        systemPopupDescription = 0,
        spoilerPopupTitle = 0,
        spoilerPopupDescription = 0,
        popupType = 0,
    ):
        self.id = id  # type: int
        self.checkConfirmAble = checkConfirmAble  # type: bool
        self.systemPopupTitle = systemPopupTitle  # type: int
        self.systemPopupDescription = systemPopupDescription  # type: int
        self.spoilerPopupTitle = spoilerPopupTitle  # type: int
        self.spoilerPopupDescription = spoilerPopupDescription  # type: int
        self.popupType = popupType  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        alertPopupExcel = AlertPopupExcel()
        alertPopupExcel.Init(buf, pos)
        return cls.InitFromObj(alertPopupExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, alertPopupExcel):
        x = AlertPopupExcelT()
        x._UnPack(alertPopupExcel)
        return x

    # AlertPopupExcelT
    def _UnPack(self, alertPopupExcel):
        if alertPopupExcel is None:
            return
        self.id = alertPopupExcel.Id()
        self.checkConfirmAble = alertPopupExcel.CheckConfirmAble()
        self.systemPopupTitle = alertPopupExcel.SystemPopupTitle()
        self.systemPopupDescription = alertPopupExcel.SystemPopupDescription()
        self.spoilerPopupTitle = alertPopupExcel.SpoilerPopupTitle()
        self.spoilerPopupDescription = alertPopupExcel.SpoilerPopupDescription()
        self.popupType = alertPopupExcel.PopupType()

    # AlertPopupExcelT
    def Pack(self, builder):
        AlertPopupExcelStart(builder)
        AlertPopupExcelAddId(builder, self.id)
        AlertPopupExcelAddCheckConfirmAble(builder, self.checkConfirmAble)
        AlertPopupExcelAddSystemPopupTitle(builder, self.systemPopupTitle)
        AlertPopupExcelAddSystemPopupDescription(builder, self.systemPopupDescription)
        AlertPopupExcelAddSpoilerPopupTitle(builder, self.spoilerPopupTitle)
        AlertPopupExcelAddSpoilerPopupDescription(builder, self.spoilerPopupDescription)
        AlertPopupExcelAddPopupType(builder, self.popupType)
        alertPopupExcel = AlertPopupExcelEnd(builder)
        return alertPopupExcel
