# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class SeasonRecordRewardExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SeasonRecordRewardExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSeasonRecordRewardExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # SeasonRecordRewardExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SeasonRecordRewardExcel
    def GroupId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # SeasonRecordRewardExcel
    def Day(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # SeasonRecordRewardExcel
    def RewardParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # SeasonRecordRewardExcel
    def RewardParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # SeasonRecordRewardExcel
    def RewardParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SeasonRecordRewardExcel
    def RewardParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # SeasonRecordRewardExcel
    def RewardParcelId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SeasonRecordRewardExcel
    def RewardParcelIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # SeasonRecordRewardExcel
    def RewardParcelIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SeasonRecordRewardExcel
    def RewardParcelIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # SeasonRecordRewardExcel
    def RewardAmount(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # SeasonRecordRewardExcel
    def RewardAmountAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # SeasonRecordRewardExcel
    def RewardAmountLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SeasonRecordRewardExcel
    def RewardAmountIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def SeasonRecordRewardExcelStart(builder):
    builder.StartObject(5)

def Start(builder):
    SeasonRecordRewardExcelStart(builder)

def SeasonRecordRewardExcelAddGroupId(builder, groupId):
    builder.PrependInt64Slot(0, groupId, 0)

def AddGroupId(builder, groupId):
    SeasonRecordRewardExcelAddGroupId(builder, groupId)

def SeasonRecordRewardExcelAddDay(builder, day):
    builder.PrependInt32Slot(1, day, 0)

def AddDay(builder, day):
    SeasonRecordRewardExcelAddDay(builder, day)

def SeasonRecordRewardExcelAddRewardParcelType(builder, rewardParcelType):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(rewardParcelType), 0)

def AddRewardParcelType(builder, rewardParcelType):
    SeasonRecordRewardExcelAddRewardParcelType(builder, rewardParcelType)

def SeasonRecordRewardExcelStartRewardParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRewardParcelTypeVector(builder, numElems):
    return SeasonRecordRewardExcelStartRewardParcelTypeVector(builder, numElems)

def SeasonRecordRewardExcelAddRewardParcelId(builder, rewardParcelId):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(rewardParcelId), 0)

def AddRewardParcelId(builder, rewardParcelId):
    SeasonRecordRewardExcelAddRewardParcelId(builder, rewardParcelId)

def SeasonRecordRewardExcelStartRewardParcelIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartRewardParcelIdVector(builder, numElems):
    return SeasonRecordRewardExcelStartRewardParcelIdVector(builder, numElems)

def SeasonRecordRewardExcelAddRewardAmount(builder, rewardAmount):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(rewardAmount), 0)

def AddRewardAmount(builder, rewardAmount):
    SeasonRecordRewardExcelAddRewardAmount(builder, rewardAmount)

def SeasonRecordRewardExcelStartRewardAmountVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartRewardAmountVector(builder, numElems):
    return SeasonRecordRewardExcelStartRewardAmountVector(builder, numElems)

def SeasonRecordRewardExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return SeasonRecordRewardExcelEnd(builder)

try:
    from typing import List
except:
    pass

class SeasonRecordRewardExcelT(object):

    # SeasonRecordRewardExcelT
    def __init__(
        self,
        groupId = 0,
        day = 0,
        rewardParcelType = None,
        rewardParcelId = None,
        rewardAmount = None,
    ):
        self.groupId = groupId  # type: int
        self.day = day  # type: int
        self.rewardParcelType = rewardParcelType  # type: Optional[List[int]]
        self.rewardParcelId = rewardParcelId  # type: Optional[List[int]]
        self.rewardAmount = rewardAmount  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        seasonRecordRewardExcel = SeasonRecordRewardExcel()
        seasonRecordRewardExcel.Init(buf, pos)
        return cls.InitFromObj(seasonRecordRewardExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, seasonRecordRewardExcel):
        x = SeasonRecordRewardExcelT()
        x._UnPack(seasonRecordRewardExcel)
        return x

    # SeasonRecordRewardExcelT
    def _UnPack(self, seasonRecordRewardExcel):
        if seasonRecordRewardExcel is None:
            return
        self.groupId = seasonRecordRewardExcel.GroupId()
        self.day = seasonRecordRewardExcel.Day()
        if not seasonRecordRewardExcel.RewardParcelTypeIsNone():
            if np is None:
                self.rewardParcelType = []
                for i in range(seasonRecordRewardExcel.RewardParcelTypeLength()):
                    self.rewardParcelType.append(seasonRecordRewardExcel.RewardParcelType(i))
            else:
                self.rewardParcelType = seasonRecordRewardExcel.RewardParcelTypeAsNumpy()
        if not seasonRecordRewardExcel.RewardParcelIdIsNone():
            if np is None:
                self.rewardParcelId = []
                for i in range(seasonRecordRewardExcel.RewardParcelIdLength()):
                    self.rewardParcelId.append(seasonRecordRewardExcel.RewardParcelId(i))
            else:
                self.rewardParcelId = seasonRecordRewardExcel.RewardParcelIdAsNumpy()
        if not seasonRecordRewardExcel.RewardAmountIsNone():
            if np is None:
                self.rewardAmount = []
                for i in range(seasonRecordRewardExcel.RewardAmountLength()):
                    self.rewardAmount.append(seasonRecordRewardExcel.RewardAmount(i))
            else:
                self.rewardAmount = seasonRecordRewardExcel.RewardAmountAsNumpy()

    # SeasonRecordRewardExcelT
    def Pack(self, builder):
        if self.rewardParcelType is not None:
            if np is not None and type(self.rewardParcelType) is np.ndarray:
                rewardParcelType = builder.CreateNumpyVector(self.rewardParcelType)
            else:
                SeasonRecordRewardExcelStartRewardParcelTypeVector(builder, len(self.rewardParcelType))
                for i in reversed(range(len(self.rewardParcelType))):
                    builder.PrependInt32(self.rewardParcelType[i])
                rewardParcelType = builder.EndVector()
        if self.rewardParcelId is not None:
            if np is not None and type(self.rewardParcelId) is np.ndarray:
                rewardParcelId = builder.CreateNumpyVector(self.rewardParcelId)
            else:
                SeasonRecordRewardExcelStartRewardParcelIdVector(builder, len(self.rewardParcelId))
                for i in reversed(range(len(self.rewardParcelId))):
                    builder.PrependInt64(self.rewardParcelId[i])
                rewardParcelId = builder.EndVector()
        if self.rewardAmount is not None:
            if np is not None and type(self.rewardAmount) is np.ndarray:
                rewardAmount = builder.CreateNumpyVector(self.rewardAmount)
            else:
                SeasonRecordRewardExcelStartRewardAmountVector(builder, len(self.rewardAmount))
                for i in reversed(range(len(self.rewardAmount))):
                    builder.PrependInt64(self.rewardAmount[i])
                rewardAmount = builder.EndVector()
        SeasonRecordRewardExcelStart(builder)
        SeasonRecordRewardExcelAddGroupId(builder, self.groupId)
        SeasonRecordRewardExcelAddDay(builder, self.day)
        if self.rewardParcelType is not None:
            SeasonRecordRewardExcelAddRewardParcelType(builder, rewardParcelType)
        if self.rewardParcelId is not None:
            SeasonRecordRewardExcelAddRewardParcelId(builder, rewardParcelId)
        if self.rewardAmount is not None:
            SeasonRecordRewardExcelAddRewardAmount(builder, rewardAmount)
        seasonRecordRewardExcel = SeasonRecordRewardExcelEnd(builder)
        return seasonRecordRewardExcel
