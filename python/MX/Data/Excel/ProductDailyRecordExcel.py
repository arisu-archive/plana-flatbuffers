# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ProductDailyRecordExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ProductDailyRecordExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsProductDailyRecordExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ProductDailyRecordExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ProductDailyRecordExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductDailyRecordExcel
    def ProductId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ProductDailyRecordExcel
    def StoreType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ProductDailyRecordExcel
    def Price(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductDailyRecordExcel
    def PurchaseCountLimit(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductDailyRecordExcel
    def RewardId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductDailyRecordExcel
    def ParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # ProductDailyRecordExcel
    def ParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # ProductDailyRecordExcel
    def ParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductDailyRecordExcel
    def ParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        return o == 0

    # ProductDailyRecordExcel
    def ParcelId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductDailyRecordExcel
    def ParcelIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductDailyRecordExcel
    def ParcelIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductDailyRecordExcel
    def ParcelIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        return o == 0

    # ProductDailyRecordExcel
    def ParcelAmount(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductDailyRecordExcel
    def ParcelAmountAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductDailyRecordExcel
    def ParcelAmountLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductDailyRecordExcel
    def ParcelAmountIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        return o == 0

    # ProductDailyRecordExcel
    def TitleImagePath(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def ProductDailyRecordExcelStart(builder):
    builder.StartObject(10)

def Start(builder):
    ProductDailyRecordExcelStart(builder)

def ProductDailyRecordExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    ProductDailyRecordExcelAddId(builder, id)

def ProductDailyRecordExcelAddProductId(builder, productId):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(productId), 0)

def AddProductId(builder, productId):
    ProductDailyRecordExcelAddProductId(builder, productId)

def ProductDailyRecordExcelAddStoreType(builder, storeType):
    builder.PrependInt32Slot(2, storeType, 0)

def AddStoreType(builder, storeType):
    ProductDailyRecordExcelAddStoreType(builder, storeType)

def ProductDailyRecordExcelAddPrice(builder, price):
    builder.PrependInt64Slot(3, price, 0)

def AddPrice(builder, price):
    ProductDailyRecordExcelAddPrice(builder, price)

def ProductDailyRecordExcelAddPurchaseCountLimit(builder, purchaseCountLimit):
    builder.PrependInt64Slot(4, purchaseCountLimit, 0)

def AddPurchaseCountLimit(builder, purchaseCountLimit):
    ProductDailyRecordExcelAddPurchaseCountLimit(builder, purchaseCountLimit)

def ProductDailyRecordExcelAddRewardId(builder, rewardId):
    builder.PrependInt64Slot(5, rewardId, 0)

def AddRewardId(builder, rewardId):
    ProductDailyRecordExcelAddRewardId(builder, rewardId)

def ProductDailyRecordExcelAddParcelType(builder, parcelType):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(parcelType), 0)

def AddParcelType(builder, parcelType):
    ProductDailyRecordExcelAddParcelType(builder, parcelType)

def ProductDailyRecordExcelStartParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartParcelTypeVector(builder, numElems):
    return ProductDailyRecordExcelStartParcelTypeVector(builder, numElems)

def ProductDailyRecordExcelAddParcelId(builder, parcelId):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(parcelId), 0)

def AddParcelId(builder, parcelId):
    ProductDailyRecordExcelAddParcelId(builder, parcelId)

def ProductDailyRecordExcelStartParcelIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartParcelIdVector(builder, numElems):
    return ProductDailyRecordExcelStartParcelIdVector(builder, numElems)

def ProductDailyRecordExcelAddParcelAmount(builder, parcelAmount):
    builder.PrependUOffsetTRelativeSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(parcelAmount), 0)

def AddParcelAmount(builder, parcelAmount):
    ProductDailyRecordExcelAddParcelAmount(builder, parcelAmount)

def ProductDailyRecordExcelStartParcelAmountVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartParcelAmountVector(builder, numElems):
    return ProductDailyRecordExcelStartParcelAmountVector(builder, numElems)

def ProductDailyRecordExcelAddTitleImagePath(builder, titleImagePath):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(titleImagePath), 0)

def AddTitleImagePath(builder, titleImagePath):
    ProductDailyRecordExcelAddTitleImagePath(builder, titleImagePath)

def ProductDailyRecordExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return ProductDailyRecordExcelEnd(builder)

try:
    from typing import List
except:
    pass

class ProductDailyRecordExcelT(object):

    # ProductDailyRecordExcelT
    def __init__(
        self,
        id = 0,
        productId = None,
        storeType = 0,
        price = 0,
        purchaseCountLimit = 0,
        rewardId = 0,
        parcelType = None,
        parcelId = None,
        parcelAmount = None,
        titleImagePath = None,
    ):
        self.id = id  # type: int
        self.productId = productId  # type: Optional[str]
        self.storeType = storeType  # type: int
        self.price = price  # type: int
        self.purchaseCountLimit = purchaseCountLimit  # type: int
        self.rewardId = rewardId  # type: int
        self.parcelType = parcelType  # type: Optional[List[int]]
        self.parcelId = parcelId  # type: Optional[List[int]]
        self.parcelAmount = parcelAmount  # type: Optional[List[int]]
        self.titleImagePath = titleImagePath  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        productDailyRecordExcel = ProductDailyRecordExcel()
        productDailyRecordExcel.Init(buf, pos)
        return cls.InitFromObj(productDailyRecordExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, productDailyRecordExcel):
        x = ProductDailyRecordExcelT()
        x._UnPack(productDailyRecordExcel)
        return x

    # ProductDailyRecordExcelT
    def _UnPack(self, productDailyRecordExcel):
        if productDailyRecordExcel is None:
            return
        self.id = productDailyRecordExcel.Id()
        self.productId = productDailyRecordExcel.ProductId()
        self.storeType = productDailyRecordExcel.StoreType()
        self.price = productDailyRecordExcel.Price()
        self.purchaseCountLimit = productDailyRecordExcel.PurchaseCountLimit()
        self.rewardId = productDailyRecordExcel.RewardId()
        if not productDailyRecordExcel.ParcelTypeIsNone():
            if np is None:
                self.parcelType = []
                for i in range(productDailyRecordExcel.ParcelTypeLength()):
                    self.parcelType.append(productDailyRecordExcel.ParcelType(i))
            else:
                self.parcelType = productDailyRecordExcel.ParcelTypeAsNumpy()
        if not productDailyRecordExcel.ParcelIdIsNone():
            if np is None:
                self.parcelId = []
                for i in range(productDailyRecordExcel.ParcelIdLength()):
                    self.parcelId.append(productDailyRecordExcel.ParcelId(i))
            else:
                self.parcelId = productDailyRecordExcel.ParcelIdAsNumpy()
        if not productDailyRecordExcel.ParcelAmountIsNone():
            if np is None:
                self.parcelAmount = []
                for i in range(productDailyRecordExcel.ParcelAmountLength()):
                    self.parcelAmount.append(productDailyRecordExcel.ParcelAmount(i))
            else:
                self.parcelAmount = productDailyRecordExcel.ParcelAmountAsNumpy()
        self.titleImagePath = productDailyRecordExcel.TitleImagePath()

    # ProductDailyRecordExcelT
    def Pack(self, builder):
        if self.productId is not None:
            productId = builder.CreateString(self.productId)
        if self.parcelType is not None:
            if np is not None and type(self.parcelType) is np.ndarray:
                parcelType = builder.CreateNumpyVector(self.parcelType)
            else:
                ProductDailyRecordExcelStartParcelTypeVector(builder, len(self.parcelType))
                for i in reversed(range(len(self.parcelType))):
                    builder.PrependInt32(self.parcelType[i])
                parcelType = builder.EndVector()
        if self.parcelId is not None:
            if np is not None and type(self.parcelId) is np.ndarray:
                parcelId = builder.CreateNumpyVector(self.parcelId)
            else:
                ProductDailyRecordExcelStartParcelIdVector(builder, len(self.parcelId))
                for i in reversed(range(len(self.parcelId))):
                    builder.PrependInt64(self.parcelId[i])
                parcelId = builder.EndVector()
        if self.parcelAmount is not None:
            if np is not None and type(self.parcelAmount) is np.ndarray:
                parcelAmount = builder.CreateNumpyVector(self.parcelAmount)
            else:
                ProductDailyRecordExcelStartParcelAmountVector(builder, len(self.parcelAmount))
                for i in reversed(range(len(self.parcelAmount))):
                    builder.PrependInt64(self.parcelAmount[i])
                parcelAmount = builder.EndVector()
        if self.titleImagePath is not None:
            titleImagePath = builder.CreateString(self.titleImagePath)
        ProductDailyRecordExcelStart(builder)
        ProductDailyRecordExcelAddId(builder, self.id)
        if self.productId is not None:
            ProductDailyRecordExcelAddProductId(builder, productId)
        ProductDailyRecordExcelAddStoreType(builder, self.storeType)
        ProductDailyRecordExcelAddPrice(builder, self.price)
        ProductDailyRecordExcelAddPurchaseCountLimit(builder, self.purchaseCountLimit)
        ProductDailyRecordExcelAddRewardId(builder, self.rewardId)
        if self.parcelType is not None:
            ProductDailyRecordExcelAddParcelType(builder, parcelType)
        if self.parcelId is not None:
            ProductDailyRecordExcelAddParcelId(builder, parcelId)
        if self.parcelAmount is not None:
            ProductDailyRecordExcelAddParcelAmount(builder, parcelAmount)
        if self.titleImagePath is not None:
            ProductDailyRecordExcelAddTitleImagePath(builder, titleImagePath)
        productDailyRecordExcel = ProductDailyRecordExcelEnd(builder)
        return productDailyRecordExcel
