# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ProductAutoSelectionGroupExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ProductAutoSelectionGroupExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsProductAutoSelectionGroupExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ProductAutoSelectionGroupExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ProductAutoSelectionGroupExcel
    def ProductAutoSelectionGroupId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductAutoSelectionGroupExcel
    def CharacterId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductAutoSelectionGroupExcel
    def RewardParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # ProductAutoSelectionGroupExcel
    def RewardParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # ProductAutoSelectionGroupExcel
    def RewardParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductAutoSelectionGroupExcel
    def RewardParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # ProductAutoSelectionGroupExcel
    def RewardParcelId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductAutoSelectionGroupExcel
    def RewardParcelIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductAutoSelectionGroupExcel
    def RewardParcelIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductAutoSelectionGroupExcel
    def RewardParcelIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # ProductAutoSelectionGroupExcel
    def ResultAmount(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductAutoSelectionGroupExcel
    def ResultAmountAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductAutoSelectionGroupExcel
    def ResultAmountLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductAutoSelectionGroupExcel
    def ResultAmountIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # ProductAutoSelectionGroupExcel
    def ConditionParcelType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ProductAutoSelectionGroupExcel
    def ConditionParcelId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

def ProductAutoSelectionGroupExcelStart(builder):
    builder.StartObject(7)

def Start(builder):
    ProductAutoSelectionGroupExcelStart(builder)

def ProductAutoSelectionGroupExcelAddProductAutoSelectionGroupId(builder, productAutoSelectionGroupId):
    builder.PrependInt64Slot(0, productAutoSelectionGroupId, 0)

def AddProductAutoSelectionGroupId(builder, productAutoSelectionGroupId):
    ProductAutoSelectionGroupExcelAddProductAutoSelectionGroupId(builder, productAutoSelectionGroupId)

def ProductAutoSelectionGroupExcelAddCharacterId(builder, characterId):
    builder.PrependInt64Slot(1, characterId, 0)

def AddCharacterId(builder, characterId):
    ProductAutoSelectionGroupExcelAddCharacterId(builder, characterId)

def ProductAutoSelectionGroupExcelAddRewardParcelType(builder, rewardParcelType):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(rewardParcelType), 0)

def AddRewardParcelType(builder, rewardParcelType):
    ProductAutoSelectionGroupExcelAddRewardParcelType(builder, rewardParcelType)

def ProductAutoSelectionGroupExcelStartRewardParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRewardParcelTypeVector(builder, numElems):
    return ProductAutoSelectionGroupExcelStartRewardParcelTypeVector(builder, numElems)

def ProductAutoSelectionGroupExcelAddRewardParcelId(builder, rewardParcelId):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(rewardParcelId), 0)

def AddRewardParcelId(builder, rewardParcelId):
    ProductAutoSelectionGroupExcelAddRewardParcelId(builder, rewardParcelId)

def ProductAutoSelectionGroupExcelStartRewardParcelIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartRewardParcelIdVector(builder, numElems):
    return ProductAutoSelectionGroupExcelStartRewardParcelIdVector(builder, numElems)

def ProductAutoSelectionGroupExcelAddResultAmount(builder, resultAmount):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(resultAmount), 0)

def AddResultAmount(builder, resultAmount):
    ProductAutoSelectionGroupExcelAddResultAmount(builder, resultAmount)

def ProductAutoSelectionGroupExcelStartResultAmountVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartResultAmountVector(builder, numElems):
    return ProductAutoSelectionGroupExcelStartResultAmountVector(builder, numElems)

def ProductAutoSelectionGroupExcelAddConditionParcelType(builder, conditionParcelType):
    builder.PrependInt32Slot(5, conditionParcelType, 0)

def AddConditionParcelType(builder, conditionParcelType):
    ProductAutoSelectionGroupExcelAddConditionParcelType(builder, conditionParcelType)

def ProductAutoSelectionGroupExcelAddConditionParcelId(builder, conditionParcelId):
    builder.PrependInt64Slot(6, conditionParcelId, 0)

def AddConditionParcelId(builder, conditionParcelId):
    ProductAutoSelectionGroupExcelAddConditionParcelId(builder, conditionParcelId)

def ProductAutoSelectionGroupExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return ProductAutoSelectionGroupExcelEnd(builder)

try:
    from typing import List
except:
    pass

class ProductAutoSelectionGroupExcelT(object):

    # ProductAutoSelectionGroupExcelT
    def __init__(
        self,
        productAutoSelectionGroupId = 0,
        characterId = 0,
        rewardParcelType = None,
        rewardParcelId = None,
        resultAmount = None,
        conditionParcelType = 0,
        conditionParcelId = 0,
    ):
        self.productAutoSelectionGroupId = productAutoSelectionGroupId  # type: int
        self.characterId = characterId  # type: int
        self.rewardParcelType = rewardParcelType  # type: Optional[List[int]]
        self.rewardParcelId = rewardParcelId  # type: Optional[List[int]]
        self.resultAmount = resultAmount  # type: Optional[List[int]]
        self.conditionParcelType = conditionParcelType  # type: int
        self.conditionParcelId = conditionParcelId  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        productAutoSelectionGroupExcel = ProductAutoSelectionGroupExcel()
        productAutoSelectionGroupExcel.Init(buf, pos)
        return cls.InitFromObj(productAutoSelectionGroupExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, productAutoSelectionGroupExcel):
        x = ProductAutoSelectionGroupExcelT()
        x._UnPack(productAutoSelectionGroupExcel)
        return x

    # ProductAutoSelectionGroupExcelT
    def _UnPack(self, productAutoSelectionGroupExcel):
        if productAutoSelectionGroupExcel is None:
            return
        self.productAutoSelectionGroupId = productAutoSelectionGroupExcel.ProductAutoSelectionGroupId()
        self.characterId = productAutoSelectionGroupExcel.CharacterId()
        if not productAutoSelectionGroupExcel.RewardParcelTypeIsNone():
            if np is None:
                self.rewardParcelType = []
                for i in range(productAutoSelectionGroupExcel.RewardParcelTypeLength()):
                    self.rewardParcelType.append(productAutoSelectionGroupExcel.RewardParcelType(i))
            else:
                self.rewardParcelType = productAutoSelectionGroupExcel.RewardParcelTypeAsNumpy()
        if not productAutoSelectionGroupExcel.RewardParcelIdIsNone():
            if np is None:
                self.rewardParcelId = []
                for i in range(productAutoSelectionGroupExcel.RewardParcelIdLength()):
                    self.rewardParcelId.append(productAutoSelectionGroupExcel.RewardParcelId(i))
            else:
                self.rewardParcelId = productAutoSelectionGroupExcel.RewardParcelIdAsNumpy()
        if not productAutoSelectionGroupExcel.ResultAmountIsNone():
            if np is None:
                self.resultAmount = []
                for i in range(productAutoSelectionGroupExcel.ResultAmountLength()):
                    self.resultAmount.append(productAutoSelectionGroupExcel.ResultAmount(i))
            else:
                self.resultAmount = productAutoSelectionGroupExcel.ResultAmountAsNumpy()
        self.conditionParcelType = productAutoSelectionGroupExcel.ConditionParcelType()
        self.conditionParcelId = productAutoSelectionGroupExcel.ConditionParcelId()

    # ProductAutoSelectionGroupExcelT
    def Pack(self, builder):
        if self.rewardParcelType is not None:
            if np is not None and type(self.rewardParcelType) is np.ndarray:
                rewardParcelType = builder.CreateNumpyVector(self.rewardParcelType)
            else:
                ProductAutoSelectionGroupExcelStartRewardParcelTypeVector(builder, len(self.rewardParcelType))
                for i in reversed(range(len(self.rewardParcelType))):
                    builder.PrependInt32(self.rewardParcelType[i])
                rewardParcelType = builder.EndVector()
        if self.rewardParcelId is not None:
            if np is not None and type(self.rewardParcelId) is np.ndarray:
                rewardParcelId = builder.CreateNumpyVector(self.rewardParcelId)
            else:
                ProductAutoSelectionGroupExcelStartRewardParcelIdVector(builder, len(self.rewardParcelId))
                for i in reversed(range(len(self.rewardParcelId))):
                    builder.PrependInt64(self.rewardParcelId[i])
                rewardParcelId = builder.EndVector()
        if self.resultAmount is not None:
            if np is not None and type(self.resultAmount) is np.ndarray:
                resultAmount = builder.CreateNumpyVector(self.resultAmount)
            else:
                ProductAutoSelectionGroupExcelStartResultAmountVector(builder, len(self.resultAmount))
                for i in reversed(range(len(self.resultAmount))):
                    builder.PrependInt64(self.resultAmount[i])
                resultAmount = builder.EndVector()
        ProductAutoSelectionGroupExcelStart(builder)
        ProductAutoSelectionGroupExcelAddProductAutoSelectionGroupId(builder, self.productAutoSelectionGroupId)
        ProductAutoSelectionGroupExcelAddCharacterId(builder, self.characterId)
        if self.rewardParcelType is not None:
            ProductAutoSelectionGroupExcelAddRewardParcelType(builder, rewardParcelType)
        if self.rewardParcelId is not None:
            ProductAutoSelectionGroupExcelAddRewardParcelId(builder, rewardParcelId)
        if self.resultAmount is not None:
            ProductAutoSelectionGroupExcelAddResultAmount(builder, resultAmount)
        ProductAutoSelectionGroupExcelAddConditionParcelType(builder, self.conditionParcelType)
        ProductAutoSelectionGroupExcelAddConditionParcelId(builder, self.conditionParcelId)
        productAutoSelectionGroupExcel = ProductAutoSelectionGroupExcelEnd(builder)
        return productAutoSelectionGroupExcel
