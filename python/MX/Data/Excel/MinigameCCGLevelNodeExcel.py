# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class MinigameCCGLevelNodeExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = MinigameCCGLevelNodeExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMinigameCCGLevelNodeExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # MinigameCCGLevelNodeExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # MinigameCCGLevelNodeExcel
    def LevelId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # MinigameCCGLevelNodeExcel
    def NodeId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # MinigameCCGLevelNodeExcel
    def NodeIcon(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # MinigameCCGLevelNodeExcel
    def StageGroupId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # MinigameCCGLevelNodeExcel
    def NextNodeId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # MinigameCCGLevelNodeExcel
    def NextNodeIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # MinigameCCGLevelNodeExcel
    def NextNodeIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # MinigameCCGLevelNodeExcel
    def NextNodeIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def MinigameCCGLevelNodeExcelStart(builder):
    builder.StartObject(5)

def Start(builder):
    MinigameCCGLevelNodeExcelStart(builder)

def MinigameCCGLevelNodeExcelAddLevelId(builder, levelId):
    builder.PrependInt64Slot(0, levelId, 0)

def AddLevelId(builder, levelId):
    MinigameCCGLevelNodeExcelAddLevelId(builder, levelId)

def MinigameCCGLevelNodeExcelAddNodeId(builder, nodeId):
    builder.PrependInt64Slot(1, nodeId, 0)

def AddNodeId(builder, nodeId):
    MinigameCCGLevelNodeExcelAddNodeId(builder, nodeId)

def MinigameCCGLevelNodeExcelAddNodeIcon(builder, nodeIcon):
    builder.PrependInt32Slot(2, nodeIcon, 0)

def AddNodeIcon(builder, nodeIcon):
    MinigameCCGLevelNodeExcelAddNodeIcon(builder, nodeIcon)

def MinigameCCGLevelNodeExcelAddStageGroupId(builder, stageGroupId):
    builder.PrependInt64Slot(3, stageGroupId, 0)

def AddStageGroupId(builder, stageGroupId):
    MinigameCCGLevelNodeExcelAddStageGroupId(builder, stageGroupId)

def MinigameCCGLevelNodeExcelAddNextNodeId(builder, nextNodeId):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(nextNodeId), 0)

def AddNextNodeId(builder, nextNodeId):
    MinigameCCGLevelNodeExcelAddNextNodeId(builder, nextNodeId)

def MinigameCCGLevelNodeExcelStartNextNodeIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartNextNodeIdVector(builder, numElems):
    return MinigameCCGLevelNodeExcelStartNextNodeIdVector(builder, numElems)

def MinigameCCGLevelNodeExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return MinigameCCGLevelNodeExcelEnd(builder)

try:
    from typing import List
except:
    pass

class MinigameCCGLevelNodeExcelT(object):

    # MinigameCCGLevelNodeExcelT
    def __init__(
        self,
        levelId = 0,
        nodeId = 0,
        nodeIcon = 0,
        stageGroupId = 0,
        nextNodeId = None,
    ):
        self.levelId = levelId  # type: int
        self.nodeId = nodeId  # type: int
        self.nodeIcon = nodeIcon  # type: int
        self.stageGroupId = stageGroupId  # type: int
        self.nextNodeId = nextNodeId  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        minigameCcglevelNodeExcel = MinigameCCGLevelNodeExcel()
        minigameCcglevelNodeExcel.Init(buf, pos)
        return cls.InitFromObj(minigameCcglevelNodeExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, minigameCcglevelNodeExcel):
        x = MinigameCCGLevelNodeExcelT()
        x._UnPack(minigameCcglevelNodeExcel)
        return x

    # MinigameCCGLevelNodeExcelT
    def _UnPack(self, minigameCcglevelNodeExcel):
        if minigameCcglevelNodeExcel is None:
            return
        self.levelId = minigameCcglevelNodeExcel.LevelId()
        self.nodeId = minigameCcglevelNodeExcel.NodeId()
        self.nodeIcon = minigameCcglevelNodeExcel.NodeIcon()
        self.stageGroupId = minigameCcglevelNodeExcel.StageGroupId()
        if not minigameCcglevelNodeExcel.NextNodeIdIsNone():
            if np is None:
                self.nextNodeId = []
                for i in range(minigameCcglevelNodeExcel.NextNodeIdLength()):
                    self.nextNodeId.append(minigameCcglevelNodeExcel.NextNodeId(i))
            else:
                self.nextNodeId = minigameCcglevelNodeExcel.NextNodeIdAsNumpy()

    # MinigameCCGLevelNodeExcelT
    def Pack(self, builder):
        if self.nextNodeId is not None:
            if np is not None and type(self.nextNodeId) is np.ndarray:
                nextNodeId = builder.CreateNumpyVector(self.nextNodeId)
            else:
                MinigameCCGLevelNodeExcelStartNextNodeIdVector(builder, len(self.nextNodeId))
                for i in reversed(range(len(self.nextNodeId))):
                    builder.PrependInt64(self.nextNodeId[i])
                nextNodeId = builder.EndVector()
        MinigameCCGLevelNodeExcelStart(builder)
        MinigameCCGLevelNodeExcelAddLevelId(builder, self.levelId)
        MinigameCCGLevelNodeExcelAddNodeId(builder, self.nodeId)
        MinigameCCGLevelNodeExcelAddNodeIcon(builder, self.nodeIcon)
        MinigameCCGLevelNodeExcelAddStageGroupId(builder, self.stageGroupId)
        if self.nextNodeId is not None:
            MinigameCCGLevelNodeExcelAddNextNodeId(builder, nextNodeId)
        minigameCcglevelNodeExcel = MinigameCCGLevelNodeExcelEnd(builder)
        return minigameCcglevelNodeExcel
