# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class StreakRecordRewardExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = StreakRecordRewardExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsStreakRecordRewardExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # StreakRecordRewardExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # StreakRecordRewardExcel
    def GroupId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # StreakRecordRewardExcel
    def Step(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # StreakRecordRewardExcel
    def StreakDay(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # StreakRecordRewardExcel
    def RewardParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # StreakRecordRewardExcel
    def RewardParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # StreakRecordRewardExcel
    def RewardParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # StreakRecordRewardExcel
    def RewardParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

    # StreakRecordRewardExcel
    def RewardParcelId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # StreakRecordRewardExcel
    def RewardParcelIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # StreakRecordRewardExcel
    def RewardParcelIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # StreakRecordRewardExcel
    def RewardParcelIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

    # StreakRecordRewardExcel
    def RewardParcelAmount(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # StreakRecordRewardExcel
    def RewardParcelAmountAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # StreakRecordRewardExcel
    def RewardParcelAmountLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # StreakRecordRewardExcel
    def RewardParcelAmountIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        return o == 0

def StreakRecordRewardExcelStart(builder):
    builder.StartObject(6)

def Start(builder):
    StreakRecordRewardExcelStart(builder)

def StreakRecordRewardExcelAddGroupId(builder, groupId):
    builder.PrependInt64Slot(0, groupId, 0)

def AddGroupId(builder, groupId):
    StreakRecordRewardExcelAddGroupId(builder, groupId)

def StreakRecordRewardExcelAddStep(builder, step):
    builder.PrependInt32Slot(1, step, 0)

def AddStep(builder, step):
    StreakRecordRewardExcelAddStep(builder, step)

def StreakRecordRewardExcelAddStreakDay(builder, streakDay):
    builder.PrependInt32Slot(2, streakDay, 0)

def AddStreakDay(builder, streakDay):
    StreakRecordRewardExcelAddStreakDay(builder, streakDay)

def StreakRecordRewardExcelAddRewardParcelType(builder, rewardParcelType):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(rewardParcelType), 0)

def AddRewardParcelType(builder, rewardParcelType):
    StreakRecordRewardExcelAddRewardParcelType(builder, rewardParcelType)

def StreakRecordRewardExcelStartRewardParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartRewardParcelTypeVector(builder, numElems):
    return StreakRecordRewardExcelStartRewardParcelTypeVector(builder, numElems)

def StreakRecordRewardExcelAddRewardParcelId(builder, rewardParcelId):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(rewardParcelId), 0)

def AddRewardParcelId(builder, rewardParcelId):
    StreakRecordRewardExcelAddRewardParcelId(builder, rewardParcelId)

def StreakRecordRewardExcelStartRewardParcelIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartRewardParcelIdVector(builder, numElems):
    return StreakRecordRewardExcelStartRewardParcelIdVector(builder, numElems)

def StreakRecordRewardExcelAddRewardParcelAmount(builder, rewardParcelAmount):
    builder.PrependUOffsetTRelativeSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(rewardParcelAmount), 0)

def AddRewardParcelAmount(builder, rewardParcelAmount):
    StreakRecordRewardExcelAddRewardParcelAmount(builder, rewardParcelAmount)

def StreakRecordRewardExcelStartRewardParcelAmountVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartRewardParcelAmountVector(builder, numElems):
    return StreakRecordRewardExcelStartRewardParcelAmountVector(builder, numElems)

def StreakRecordRewardExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return StreakRecordRewardExcelEnd(builder)

try:
    from typing import List
except:
    pass

class StreakRecordRewardExcelT(object):

    # StreakRecordRewardExcelT
    def __init__(
        self,
        groupId = 0,
        step = 0,
        streakDay = 0,
        rewardParcelType = None,
        rewardParcelId = None,
        rewardParcelAmount = None,
    ):
        self.groupId = groupId  # type: int
        self.step = step  # type: int
        self.streakDay = streakDay  # type: int
        self.rewardParcelType = rewardParcelType  # type: Optional[List[int]]
        self.rewardParcelId = rewardParcelId  # type: Optional[List[int]]
        self.rewardParcelAmount = rewardParcelAmount  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        streakRecordRewardExcel = StreakRecordRewardExcel()
        streakRecordRewardExcel.Init(buf, pos)
        return cls.InitFromObj(streakRecordRewardExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, streakRecordRewardExcel):
        x = StreakRecordRewardExcelT()
        x._UnPack(streakRecordRewardExcel)
        return x

    # StreakRecordRewardExcelT
    def _UnPack(self, streakRecordRewardExcel):
        if streakRecordRewardExcel is None:
            return
        self.groupId = streakRecordRewardExcel.GroupId()
        self.step = streakRecordRewardExcel.Step()
        self.streakDay = streakRecordRewardExcel.StreakDay()
        if not streakRecordRewardExcel.RewardParcelTypeIsNone():
            if np is None:
                self.rewardParcelType = []
                for i in range(streakRecordRewardExcel.RewardParcelTypeLength()):
                    self.rewardParcelType.append(streakRecordRewardExcel.RewardParcelType(i))
            else:
                self.rewardParcelType = streakRecordRewardExcel.RewardParcelTypeAsNumpy()
        if not streakRecordRewardExcel.RewardParcelIdIsNone():
            if np is None:
                self.rewardParcelId = []
                for i in range(streakRecordRewardExcel.RewardParcelIdLength()):
                    self.rewardParcelId.append(streakRecordRewardExcel.RewardParcelId(i))
            else:
                self.rewardParcelId = streakRecordRewardExcel.RewardParcelIdAsNumpy()
        if not streakRecordRewardExcel.RewardParcelAmountIsNone():
            if np is None:
                self.rewardParcelAmount = []
                for i in range(streakRecordRewardExcel.RewardParcelAmountLength()):
                    self.rewardParcelAmount.append(streakRecordRewardExcel.RewardParcelAmount(i))
            else:
                self.rewardParcelAmount = streakRecordRewardExcel.RewardParcelAmountAsNumpy()

    # StreakRecordRewardExcelT
    def Pack(self, builder):
        if self.rewardParcelType is not None:
            if np is not None and type(self.rewardParcelType) is np.ndarray:
                rewardParcelType = builder.CreateNumpyVector(self.rewardParcelType)
            else:
                StreakRecordRewardExcelStartRewardParcelTypeVector(builder, len(self.rewardParcelType))
                for i in reversed(range(len(self.rewardParcelType))):
                    builder.PrependInt32(self.rewardParcelType[i])
                rewardParcelType = builder.EndVector()
        if self.rewardParcelId is not None:
            if np is not None and type(self.rewardParcelId) is np.ndarray:
                rewardParcelId = builder.CreateNumpyVector(self.rewardParcelId)
            else:
                StreakRecordRewardExcelStartRewardParcelIdVector(builder, len(self.rewardParcelId))
                for i in reversed(range(len(self.rewardParcelId))):
                    builder.PrependInt64(self.rewardParcelId[i])
                rewardParcelId = builder.EndVector()
        if self.rewardParcelAmount is not None:
            if np is not None and type(self.rewardParcelAmount) is np.ndarray:
                rewardParcelAmount = builder.CreateNumpyVector(self.rewardParcelAmount)
            else:
                StreakRecordRewardExcelStartRewardParcelAmountVector(builder, len(self.rewardParcelAmount))
                for i in reversed(range(len(self.rewardParcelAmount))):
                    builder.PrependInt64(self.rewardParcelAmount[i])
                rewardParcelAmount = builder.EndVector()
        StreakRecordRewardExcelStart(builder)
        StreakRecordRewardExcelAddGroupId(builder, self.groupId)
        StreakRecordRewardExcelAddStep(builder, self.step)
        StreakRecordRewardExcelAddStreakDay(builder, self.streakDay)
        if self.rewardParcelType is not None:
            StreakRecordRewardExcelAddRewardParcelType(builder, rewardParcelType)
        if self.rewardParcelId is not None:
            StreakRecordRewardExcelAddRewardParcelId(builder, rewardParcelId)
        if self.rewardParcelAmount is not None:
            StreakRecordRewardExcelAddRewardParcelAmount(builder, rewardParcelAmount)
        streakRecordRewardExcel = StreakRecordRewardExcelEnd(builder)
        return streakRecordRewardExcel
