# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class InteractiveWorldRaidCarrierRecipeExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = InteractiveWorldRaidCarrierRecipeExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsInteractiveWorldRaidCarrierRecipeExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # InteractiveWorldRaidCarrierRecipeExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # InteractiveWorldRaidCarrierRecipeExcel
    def SkillId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # InteractiveWorldRaidCarrierRecipeExcel
    def SkillSlot(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # InteractiveWorldRaidCarrierRecipeExcel
    def Level(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # InteractiveWorldRaidCarrierRecipeExcel
    def RecipeIngredientId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # InteractiveWorldRaidCarrierRecipeExcel
    def RecipeIngredientIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # InteractiveWorldRaidCarrierRecipeExcel
    def RecipeIngredientIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # InteractiveWorldRaidCarrierRecipeExcel
    def RecipeIngredientIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        return o == 0

def InteractiveWorldRaidCarrierRecipeExcelStart(builder):
    builder.StartObject(4)

def Start(builder):
    InteractiveWorldRaidCarrierRecipeExcelStart(builder)

def InteractiveWorldRaidCarrierRecipeExcelAddSkillId(builder, skillId):
    builder.PrependInt64Slot(0, skillId, 0)

def AddSkillId(builder, skillId):
    InteractiveWorldRaidCarrierRecipeExcelAddSkillId(builder, skillId)

def InteractiveWorldRaidCarrierRecipeExcelAddSkillSlot(builder, skillSlot):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(skillSlot), 0)

def AddSkillSlot(builder, skillSlot):
    InteractiveWorldRaidCarrierRecipeExcelAddSkillSlot(builder, skillSlot)

def InteractiveWorldRaidCarrierRecipeExcelAddLevel(builder, level):
    builder.PrependInt32Slot(2, level, 0)

def AddLevel(builder, level):
    InteractiveWorldRaidCarrierRecipeExcelAddLevel(builder, level)

def InteractiveWorldRaidCarrierRecipeExcelAddRecipeIngredientId(builder, recipeIngredientId):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(recipeIngredientId), 0)

def AddRecipeIngredientId(builder, recipeIngredientId):
    InteractiveWorldRaidCarrierRecipeExcelAddRecipeIngredientId(builder, recipeIngredientId)

def InteractiveWorldRaidCarrierRecipeExcelStartRecipeIngredientIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartRecipeIngredientIdVector(builder, numElems):
    return InteractiveWorldRaidCarrierRecipeExcelStartRecipeIngredientIdVector(builder, numElems)

def InteractiveWorldRaidCarrierRecipeExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return InteractiveWorldRaidCarrierRecipeExcelEnd(builder)

try:
    from typing import List
except:
    pass

class InteractiveWorldRaidCarrierRecipeExcelT(object):

    # InteractiveWorldRaidCarrierRecipeExcelT
    def __init__(
        self,
        skillId = 0,
        skillSlot = None,
        level = 0,
        recipeIngredientId = None,
    ):
        self.skillId = skillId  # type: int
        self.skillSlot = skillSlot  # type: Optional[str]
        self.level = level  # type: int
        self.recipeIngredientId = recipeIngredientId  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        interactiveWorldRaidCarrierRecipeExcel = InteractiveWorldRaidCarrierRecipeExcel()
        interactiveWorldRaidCarrierRecipeExcel.Init(buf, pos)
        return cls.InitFromObj(interactiveWorldRaidCarrierRecipeExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, interactiveWorldRaidCarrierRecipeExcel):
        x = InteractiveWorldRaidCarrierRecipeExcelT()
        x._UnPack(interactiveWorldRaidCarrierRecipeExcel)
        return x

    # InteractiveWorldRaidCarrierRecipeExcelT
    def _UnPack(self, interactiveWorldRaidCarrierRecipeExcel):
        if interactiveWorldRaidCarrierRecipeExcel is None:
            return
        self.skillId = interactiveWorldRaidCarrierRecipeExcel.SkillId()
        self.skillSlot = interactiveWorldRaidCarrierRecipeExcel.SkillSlot()
        self.level = interactiveWorldRaidCarrierRecipeExcel.Level()
        if not interactiveWorldRaidCarrierRecipeExcel.RecipeIngredientIdIsNone():
            if np is None:
                self.recipeIngredientId = []
                for i in range(interactiveWorldRaidCarrierRecipeExcel.RecipeIngredientIdLength()):
                    self.recipeIngredientId.append(interactiveWorldRaidCarrierRecipeExcel.RecipeIngredientId(i))
            else:
                self.recipeIngredientId = interactiveWorldRaidCarrierRecipeExcel.RecipeIngredientIdAsNumpy()

    # InteractiveWorldRaidCarrierRecipeExcelT
    def Pack(self, builder):
        if self.skillSlot is not None:
            skillSlot = builder.CreateString(self.skillSlot)
        if self.recipeIngredientId is not None:
            if np is not None and type(self.recipeIngredientId) is np.ndarray:
                recipeIngredientId = builder.CreateNumpyVector(self.recipeIngredientId)
            else:
                InteractiveWorldRaidCarrierRecipeExcelStartRecipeIngredientIdVector(builder, len(self.recipeIngredientId))
                for i in reversed(range(len(self.recipeIngredientId))):
                    builder.PrependInt64(self.recipeIngredientId[i])
                recipeIngredientId = builder.EndVector()
        InteractiveWorldRaidCarrierRecipeExcelStart(builder)
        InteractiveWorldRaidCarrierRecipeExcelAddSkillId(builder, self.skillId)
        if self.skillSlot is not None:
            InteractiveWorldRaidCarrierRecipeExcelAddSkillSlot(builder, skillSlot)
        InteractiveWorldRaidCarrierRecipeExcelAddLevel(builder, self.level)
        if self.recipeIngredientId is not None:
            InteractiveWorldRaidCarrierRecipeExcelAddRecipeIngredientId(builder, recipeIngredientId)
        interactiveWorldRaidCarrierRecipeExcel = InteractiveWorldRaidCarrierRecipeExcelEnd(builder)
        return interactiveWorldRaidCarrierRecipeExcel
