# automatically generated by the FlatBuffers compiler, do not modify

# namespace: Excel

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ProductSelectExcel(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ProductSelectExcel()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsProductSelectExcel(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ProductSelectExcel
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ProductSelectExcel
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductSelectExcel
    def ProductSelectSubType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ProductSelectExcel
    def AutoSelectPopupType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ProductSelectExcel
    def ProductId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ProductSelectExcel
    def StoreType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ProductSelectExcel
    def Price(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductSelectExcel
    def PriceReference(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ProductSelectExcel
    def PurchasePeriodType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ProductSelectExcel
    def PurchasePeriodLimit(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ProductSelectExcel
    def ParcelType(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # ProductSelectExcel
    def ParcelTypeAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # ProductSelectExcel
    def ParcelTypeLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductSelectExcel
    def ParcelTypeIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        return o == 0

    # ProductSelectExcel
    def ParcelId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductSelectExcel
    def ParcelIdAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductSelectExcel
    def ParcelIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductSelectExcel
    def ParcelIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        return o == 0

    # ProductSelectExcel
    def ParcelAmount(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductSelectExcel
    def ParcelAmountAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductSelectExcel
    def ParcelAmountLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductSelectExcel
    def ParcelAmountIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # ProductSelectExcel
    def ProductSelectionSlot(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int64Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 8))
        return 0

    # ProductSelectExcel
    def ProductSelectionSlotAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int64Flags, o)
        return 0

    # ProductSelectExcel
    def ProductSelectionSlotLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ProductSelectExcel
    def ProductSelectionSlotIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

def ProductSelectExcelStart(builder):
    builder.StartObject(13)

def Start(builder):
    ProductSelectExcelStart(builder)

def ProductSelectExcelAddId(builder, id):
    builder.PrependInt64Slot(0, id, 0)

def AddId(builder, id):
    ProductSelectExcelAddId(builder, id)

def ProductSelectExcelAddProductSelectSubType(builder, productSelectSubType):
    builder.PrependInt32Slot(1, productSelectSubType, 0)

def AddProductSelectSubType(builder, productSelectSubType):
    ProductSelectExcelAddProductSelectSubType(builder, productSelectSubType)

def ProductSelectExcelAddAutoSelectPopupType(builder, autoSelectPopupType):
    builder.PrependInt32Slot(2, autoSelectPopupType, 0)

def AddAutoSelectPopupType(builder, autoSelectPopupType):
    ProductSelectExcelAddAutoSelectPopupType(builder, autoSelectPopupType)

def ProductSelectExcelAddProductId(builder, productId):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(productId), 0)

def AddProductId(builder, productId):
    ProductSelectExcelAddProductId(builder, productId)

def ProductSelectExcelAddStoreType(builder, storeType):
    builder.PrependInt32Slot(4, storeType, 0)

def AddStoreType(builder, storeType):
    ProductSelectExcelAddStoreType(builder, storeType)

def ProductSelectExcelAddPrice(builder, price):
    builder.PrependInt64Slot(5, price, 0)

def AddPrice(builder, price):
    ProductSelectExcelAddPrice(builder, price)

def ProductSelectExcelAddPriceReference(builder, priceReference):
    builder.PrependUOffsetTRelativeSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(priceReference), 0)

def AddPriceReference(builder, priceReference):
    ProductSelectExcelAddPriceReference(builder, priceReference)

def ProductSelectExcelAddPurchasePeriodType(builder, purchasePeriodType):
    builder.PrependInt32Slot(7, purchasePeriodType, 0)

def AddPurchasePeriodType(builder, purchasePeriodType):
    ProductSelectExcelAddPurchasePeriodType(builder, purchasePeriodType)

def ProductSelectExcelAddPurchasePeriodLimit(builder, purchasePeriodLimit):
    builder.PrependInt64Slot(8, purchasePeriodLimit, 0)

def AddPurchasePeriodLimit(builder, purchasePeriodLimit):
    ProductSelectExcelAddPurchasePeriodLimit(builder, purchasePeriodLimit)

def ProductSelectExcelAddParcelType(builder, parcelType):
    builder.PrependUOffsetTRelativeSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(parcelType), 0)

def AddParcelType(builder, parcelType):
    ProductSelectExcelAddParcelType(builder, parcelType)

def ProductSelectExcelStartParcelTypeVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartParcelTypeVector(builder, numElems):
    return ProductSelectExcelStartParcelTypeVector(builder, numElems)

def ProductSelectExcelAddParcelId(builder, parcelId):
    builder.PrependUOffsetTRelativeSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(parcelId), 0)

def AddParcelId(builder, parcelId):
    ProductSelectExcelAddParcelId(builder, parcelId)

def ProductSelectExcelStartParcelIdVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartParcelIdVector(builder, numElems):
    return ProductSelectExcelStartParcelIdVector(builder, numElems)

def ProductSelectExcelAddParcelAmount(builder, parcelAmount):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(parcelAmount), 0)

def AddParcelAmount(builder, parcelAmount):
    ProductSelectExcelAddParcelAmount(builder, parcelAmount)

def ProductSelectExcelStartParcelAmountVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartParcelAmountVector(builder, numElems):
    return ProductSelectExcelStartParcelAmountVector(builder, numElems)

def ProductSelectExcelAddProductSelectionSlot(builder, productSelectionSlot):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(productSelectionSlot), 0)

def AddProductSelectionSlot(builder, productSelectionSlot):
    ProductSelectExcelAddProductSelectionSlot(builder, productSelectionSlot)

def ProductSelectExcelStartProductSelectionSlotVector(builder, numElems):
    return builder.StartVector(8, numElems, 8)

def StartProductSelectionSlotVector(builder, numElems):
    return ProductSelectExcelStartProductSelectionSlotVector(builder, numElems)

def ProductSelectExcelEnd(builder):
    return builder.EndObject()

def End(builder):
    return ProductSelectExcelEnd(builder)

try:
    from typing import List
except:
    pass

class ProductSelectExcelT(object):

    # ProductSelectExcelT
    def __init__(
        self,
        id = 0,
        productSelectSubType = 0,
        autoSelectPopupType = 0,
        productId = None,
        storeType = 0,
        price = 0,
        priceReference = None,
        purchasePeriodType = 0,
        purchasePeriodLimit = 0,
        parcelType = None,
        parcelId = None,
        parcelAmount = None,
        productSelectionSlot = None,
    ):
        self.id = id  # type: int
        self.productSelectSubType = productSelectSubType  # type: int
        self.autoSelectPopupType = autoSelectPopupType  # type: int
        self.productId = productId  # type: Optional[str]
        self.storeType = storeType  # type: int
        self.price = price  # type: int
        self.priceReference = priceReference  # type: Optional[str]
        self.purchasePeriodType = purchasePeriodType  # type: int
        self.purchasePeriodLimit = purchasePeriodLimit  # type: int
        self.parcelType = parcelType  # type: Optional[List[int]]
        self.parcelId = parcelId  # type: Optional[List[int]]
        self.parcelAmount = parcelAmount  # type: Optional[List[int]]
        self.productSelectionSlot = productSelectionSlot  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        productSelectExcel = ProductSelectExcel()
        productSelectExcel.Init(buf, pos)
        return cls.InitFromObj(productSelectExcel)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, productSelectExcel):
        x = ProductSelectExcelT()
        x._UnPack(productSelectExcel)
        return x

    # ProductSelectExcelT
    def _UnPack(self, productSelectExcel):
        if productSelectExcel is None:
            return
        self.id = productSelectExcel.Id()
        self.productSelectSubType = productSelectExcel.ProductSelectSubType()
        self.autoSelectPopupType = productSelectExcel.AutoSelectPopupType()
        self.productId = productSelectExcel.ProductId()
        self.storeType = productSelectExcel.StoreType()
        self.price = productSelectExcel.Price()
        self.priceReference = productSelectExcel.PriceReference()
        self.purchasePeriodType = productSelectExcel.PurchasePeriodType()
        self.purchasePeriodLimit = productSelectExcel.PurchasePeriodLimit()
        if not productSelectExcel.ParcelTypeIsNone():
            if np is None:
                self.parcelType = []
                for i in range(productSelectExcel.ParcelTypeLength()):
                    self.parcelType.append(productSelectExcel.ParcelType(i))
            else:
                self.parcelType = productSelectExcel.ParcelTypeAsNumpy()
        if not productSelectExcel.ParcelIdIsNone():
            if np is None:
                self.parcelId = []
                for i in range(productSelectExcel.ParcelIdLength()):
                    self.parcelId.append(productSelectExcel.ParcelId(i))
            else:
                self.parcelId = productSelectExcel.ParcelIdAsNumpy()
        if not productSelectExcel.ParcelAmountIsNone():
            if np is None:
                self.parcelAmount = []
                for i in range(productSelectExcel.ParcelAmountLength()):
                    self.parcelAmount.append(productSelectExcel.ParcelAmount(i))
            else:
                self.parcelAmount = productSelectExcel.ParcelAmountAsNumpy()
        if not productSelectExcel.ProductSelectionSlotIsNone():
            if np is None:
                self.productSelectionSlot = []
                for i in range(productSelectExcel.ProductSelectionSlotLength()):
                    self.productSelectionSlot.append(productSelectExcel.ProductSelectionSlot(i))
            else:
                self.productSelectionSlot = productSelectExcel.ProductSelectionSlotAsNumpy()

    # ProductSelectExcelT
    def Pack(self, builder):
        if self.productId is not None:
            productId = builder.CreateString(self.productId)
        if self.priceReference is not None:
            priceReference = builder.CreateString(self.priceReference)
        if self.parcelType is not None:
            if np is not None and type(self.parcelType) is np.ndarray:
                parcelType = builder.CreateNumpyVector(self.parcelType)
            else:
                ProductSelectExcelStartParcelTypeVector(builder, len(self.parcelType))
                for i in reversed(range(len(self.parcelType))):
                    builder.PrependInt32(self.parcelType[i])
                parcelType = builder.EndVector()
        if self.parcelId is not None:
            if np is not None and type(self.parcelId) is np.ndarray:
                parcelId = builder.CreateNumpyVector(self.parcelId)
            else:
                ProductSelectExcelStartParcelIdVector(builder, len(self.parcelId))
                for i in reversed(range(len(self.parcelId))):
                    builder.PrependInt64(self.parcelId[i])
                parcelId = builder.EndVector()
        if self.parcelAmount is not None:
            if np is not None and type(self.parcelAmount) is np.ndarray:
                parcelAmount = builder.CreateNumpyVector(self.parcelAmount)
            else:
                ProductSelectExcelStartParcelAmountVector(builder, len(self.parcelAmount))
                for i in reversed(range(len(self.parcelAmount))):
                    builder.PrependInt64(self.parcelAmount[i])
                parcelAmount = builder.EndVector()
        if self.productSelectionSlot is not None:
            if np is not None and type(self.productSelectionSlot) is np.ndarray:
                productSelectionSlot = builder.CreateNumpyVector(self.productSelectionSlot)
            else:
                ProductSelectExcelStartProductSelectionSlotVector(builder, len(self.productSelectionSlot))
                for i in reversed(range(len(self.productSelectionSlot))):
                    builder.PrependInt64(self.productSelectionSlot[i])
                productSelectionSlot = builder.EndVector()
        ProductSelectExcelStart(builder)
        ProductSelectExcelAddId(builder, self.id)
        ProductSelectExcelAddProductSelectSubType(builder, self.productSelectSubType)
        ProductSelectExcelAddAutoSelectPopupType(builder, self.autoSelectPopupType)
        if self.productId is not None:
            ProductSelectExcelAddProductId(builder, productId)
        ProductSelectExcelAddStoreType(builder, self.storeType)
        ProductSelectExcelAddPrice(builder, self.price)
        if self.priceReference is not None:
            ProductSelectExcelAddPriceReference(builder, priceReference)
        ProductSelectExcelAddPurchasePeriodType(builder, self.purchasePeriodType)
        ProductSelectExcelAddPurchasePeriodLimit(builder, self.purchasePeriodLimit)
        if self.parcelType is not None:
            ProductSelectExcelAddParcelType(builder, parcelType)
        if self.parcelId is not None:
            ProductSelectExcelAddParcelId(builder, parcelId)
        if self.parcelAmount is not None:
            ProductSelectExcelAddParcelAmount(builder, parcelAmount)
        if self.productSelectionSlot is not None:
            ProductSelectExcelAddProductSelectionSlot(builder, productSelectionSlot)
        productSelectExcel = ProductSelectExcelEnd(builder)
        return productSelectExcel
