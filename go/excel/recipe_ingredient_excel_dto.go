// Code generated by fbsgen. DO NOT EDIT.
// Package excel contains generated code for a FlatBuffers schema.
package excel

import (
	fbsutils "github.com/arisu-archive/bluearchive-fbs-utils"
	flatbuffers "github.com/google/flatbuffers/go"
)

// RecipeIngredientExcelDto represents a FlatBuffers table.
type RecipeIngredientExcelDto struct {
	fbsutils.FlatBuffer
	ID                   int64        `json:"id"`
	RecipeType           RecipeType   `json:"recipe_type"`
	CostParcelType       []ParcelType `json:"cost_parcel_type"`
	CostID               []int64      `json:"cost_id"`
	CostAmount           []int64      `json:"cost_amount"`
	IngredientParcelType []ParcelType `json:"ingredient_parcel_type"`
	IngredientID         []int64      `json:"ingredient_id"`
	IngredientAmount     []int64      `json:"ingredient_amount"`
	CostTimeInSecond     int64        `json:"cost_time_in_second"`
}

// MarshalModel marshals the struct into a FlatBuffers offset.
func (t *RecipeIngredientExcelDto) MarshalModel(b *flatbuffers.Builder) flatbuffers.UOffsetT {
	RecipeIngredientExcelStartCostParcelTypeVector(b, len(t.CostParcelType))
	for i := len(t.CostParcelType) - 1; i >= 0; i-- {
		b.PrependInt32(int32(t.CostParcelType[i]))
	}
	costParcelTypeOffset := b.EndVector(len(t.CostParcelType))
	RecipeIngredientExcelStartCostIdVector(b, len(t.CostID))
	for i := len(t.CostID) - 1; i >= 0; i-- {
		b.PrependInt64(t.CostID[i])
	}
	costIDOffset := b.EndVector(len(t.CostID))
	RecipeIngredientExcelStartCostAmountVector(b, len(t.CostAmount))
	for i := len(t.CostAmount) - 1; i >= 0; i-- {
		b.PrependInt64(t.CostAmount[i])
	}
	costAmountOffset := b.EndVector(len(t.CostAmount))
	RecipeIngredientExcelStartIngredientParcelTypeVector(b, len(t.IngredientParcelType))
	for i := len(t.IngredientParcelType) - 1; i >= 0; i-- {
		b.PrependInt32(int32(t.IngredientParcelType[i]))
	}
	ingredientParcelTypeOffset := b.EndVector(len(t.IngredientParcelType))
	RecipeIngredientExcelStartIngredientIdVector(b, len(t.IngredientID))
	for i := len(t.IngredientID) - 1; i >= 0; i-- {
		b.PrependInt64(t.IngredientID[i])
	}
	ingredientIDOffset := b.EndVector(len(t.IngredientID))
	RecipeIngredientExcelStartIngredientAmountVector(b, len(t.IngredientAmount))
	for i := len(t.IngredientAmount) - 1; i >= 0; i-- {
		b.PrependInt64(t.IngredientAmount[i])
	}
	ingredientAmountOffset := b.EndVector(len(t.IngredientAmount))
	RecipeIngredientExcelStart(b)
	RecipeIngredientExcelAddId(b, t.ID)
	RecipeIngredientExcelAddRecipeType(b, t.RecipeType)
	RecipeIngredientExcelAddCostParcelType(b, costParcelTypeOffset)
	RecipeIngredientExcelAddCostId(b, costIDOffset)
	RecipeIngredientExcelAddCostAmount(b, costAmountOffset)
	RecipeIngredientExcelAddIngredientParcelType(b, ingredientParcelTypeOffset)
	RecipeIngredientExcelAddIngredientId(b, ingredientIDOffset)
	RecipeIngredientExcelAddIngredientAmount(b, ingredientAmountOffset)
	RecipeIngredientExcelAddCostTimeInSecond(b, t.CostTimeInSecond)
	return RecipeIngredientExcelEnd(b)
}

// Marshal marshals the struct into a FlatBuffers buffer.
func (t *RecipeIngredientExcelDto) Marshal() ([]byte, error) {
	b := flatbuffers.NewBuilder(0)
	b.Finish(t.MarshalModel(b))
	return b.FinishedBytes(), nil
}

// UnmarshalMessage unmarshals the struct from a FlatBuffers table.
func (t *RecipeIngredientExcelDto) UnmarshalMessage(e *RecipeIngredientExcel) error {
	t.ID = e.Id()
	t.RecipeType = e.RecipeType()
	t.CostParcelType = make([]ParcelType, e.CostParcelTypeLength())
	for i := 0; i < e.CostParcelTypeLength(); i++ {
		t.CostParcelType[i] = e.CostParcelType(i)
	}
	t.CostID = make([]int64, e.CostIdLength())
	for i := 0; i < e.CostIdLength(); i++ {
		t.CostID[i] = e.CostId(i)
	}
	t.CostAmount = make([]int64, e.CostAmountLength())
	for i := 0; i < e.CostAmountLength(); i++ {
		t.CostAmount[i] = e.CostAmount(i)
	}
	t.IngredientParcelType = make([]ParcelType, e.IngredientParcelTypeLength())
	for i := 0; i < e.IngredientParcelTypeLength(); i++ {
		t.IngredientParcelType[i] = e.IngredientParcelType(i)
	}
	t.IngredientID = make([]int64, e.IngredientIdLength())
	for i := 0; i < e.IngredientIdLength(); i++ {
		t.IngredientID[i] = e.IngredientId(i)
	}
	t.IngredientAmount = make([]int64, e.IngredientAmountLength())
	for i := 0; i < e.IngredientAmountLength(); i++ {
		t.IngredientAmount[i] = e.IngredientAmount(i)
	}
	t.CostTimeInSecond = e.CostTimeInSecond()
	return nil
}

// Unmarshal unmarshals the struct from a FlatBuffers buffer.
func (t *RecipeIngredientExcelDto) Unmarshal(data []byte) error {
	root := GetRootAsRecipeIngredientExcel(data, 0)
	return t.UnmarshalMessage(root)
}

// FlatDataName returns the FlatBuffers table name.
func (RecipeIngredientExcelDto) FlatDataName() string {
	return "RecipeIngredientExcel"
}
