// Code generated by fbsgen. DO NOT EDIT.
// Package excel contains generated code for a FlatBuffers schema.
package excel

import (
	fbsutils "github.com/arisu-archive/bluearchive-fbs-utils"
	flatbuffers "github.com/google/flatbuffers/go"
)

// CharacterTranscendenceExcelDto represents a FlatBuffers table.
type CharacterTranscendenceExcelDto struct {
	fbsutils.FlatBuffer
	CharacterID         int64    `json:"character_id"`
	MaxFavorLevel       []int32  `json:"max_favor_level"`
	StatBonusRateAttack []int64  `json:"stat_bonus_rate_attack"`
	StatBonusRateHp     []int64  `json:"stat_bonus_rate_hp"`
	StatBonusRateHeal   []int64  `json:"stat_bonus_rate_heal"`
	RecipeID            []int64  `json:"recipe_id"`
	SkillSlotA          []string `json:"skill_slot_a"`
	SkillSlotB          []string `json:"skill_slot_b"`
	SkillSlotC          []string `json:"skill_slot_c"`
	MaxlevelStar        []int32  `json:"maxlevel_star"`
}

// MarshalModel marshals the struct into a FlatBuffers offset.
func (t *CharacterTranscendenceExcelDto) MarshalModel(b *flatbuffers.Builder) flatbuffers.UOffsetT {
	CharacterTranscendenceExcelStartMaxFavorLevelVector(b, len(t.MaxFavorLevel))
	for i := len(t.MaxFavorLevel) - 1; i >= 0; i-- {
		b.PrependInt32(t.MaxFavorLevel[i])
	}
	maxFavorLevelOffset := b.EndVector(len(t.MaxFavorLevel))
	CharacterTranscendenceExcelStartStatBonusRateAttackVector(b, len(t.StatBonusRateAttack))
	for i := len(t.StatBonusRateAttack) - 1; i >= 0; i-- {
		b.PrependInt64(t.StatBonusRateAttack[i])
	}
	statBonusRateAttackOffset := b.EndVector(len(t.StatBonusRateAttack))
	CharacterTranscendenceExcelStartStatBonusRateHpVector(b, len(t.StatBonusRateHp))
	for i := len(t.StatBonusRateHp) - 1; i >= 0; i-- {
		b.PrependInt64(t.StatBonusRateHp[i])
	}
	statBonusRateHpOffset := b.EndVector(len(t.StatBonusRateHp))
	CharacterTranscendenceExcelStartStatBonusRateHealVector(b, len(t.StatBonusRateHeal))
	for i := len(t.StatBonusRateHeal) - 1; i >= 0; i-- {
		b.PrependInt64(t.StatBonusRateHeal[i])
	}
	statBonusRateHealOffset := b.EndVector(len(t.StatBonusRateHeal))
	CharacterTranscendenceExcelStartRecipeIdVector(b, len(t.RecipeID))
	for i := len(t.RecipeID) - 1; i >= 0; i-- {
		b.PrependInt64(t.RecipeID[i])
	}
	recipeIDOffset := b.EndVector(len(t.RecipeID))
	var skillSlotAOffset flatbuffers.UOffsetT
	skillSlotAOffsets := make([]flatbuffers.UOffsetT, len(t.SkillSlotA))
	for i := range t.SkillSlotA {
		skillSlotAOffsets[i] = b.CreateString(t.SkillSlotA[i])
	}
	CharacterTranscendenceExcelStartSkillSlotAVector(b, len(t.SkillSlotA))
	for i := len(skillSlotAOffsets) - 1; i >= 0; i-- {
		b.PrependUOffsetT(skillSlotAOffsets[i])
	}
	skillSlotAOffset = b.EndVector(len(t.SkillSlotA))
	var skillSlotBOffset flatbuffers.UOffsetT
	skillSlotBOffsets := make([]flatbuffers.UOffsetT, len(t.SkillSlotB))
	for i := range t.SkillSlotB {
		skillSlotBOffsets[i] = b.CreateString(t.SkillSlotB[i])
	}
	CharacterTranscendenceExcelStartSkillSlotBVector(b, len(t.SkillSlotB))
	for i := len(skillSlotBOffsets) - 1; i >= 0; i-- {
		b.PrependUOffsetT(skillSlotBOffsets[i])
	}
	skillSlotBOffset = b.EndVector(len(t.SkillSlotB))
	var skillSlotCOffset flatbuffers.UOffsetT
	skillSlotCOffsets := make([]flatbuffers.UOffsetT, len(t.SkillSlotC))
	for i := range t.SkillSlotC {
		skillSlotCOffsets[i] = b.CreateString(t.SkillSlotC[i])
	}
	CharacterTranscendenceExcelStartSkillSlotCVector(b, len(t.SkillSlotC))
	for i := len(skillSlotCOffsets) - 1; i >= 0; i-- {
		b.PrependUOffsetT(skillSlotCOffsets[i])
	}
	skillSlotCOffset = b.EndVector(len(t.SkillSlotC))
	CharacterTranscendenceExcelStartMaxlevelStarVector(b, len(t.MaxlevelStar))
	for i := len(t.MaxlevelStar) - 1; i >= 0; i-- {
		b.PrependInt32(t.MaxlevelStar[i])
	}
	maxlevelStarOffset := b.EndVector(len(t.MaxlevelStar))
	CharacterTranscendenceExcelStart(b)
	CharacterTranscendenceExcelAddCharacterId(b, t.CharacterID)
	CharacterTranscendenceExcelAddMaxFavorLevel(b, maxFavorLevelOffset)
	CharacterTranscendenceExcelAddStatBonusRateAttack(b, statBonusRateAttackOffset)
	CharacterTranscendenceExcelAddStatBonusRateHp(b, statBonusRateHpOffset)
	CharacterTranscendenceExcelAddStatBonusRateHeal(b, statBonusRateHealOffset)
	CharacterTranscendenceExcelAddRecipeId(b, recipeIDOffset)
	CharacterTranscendenceExcelAddSkillSlotA(b, skillSlotAOffset)
	CharacterTranscendenceExcelAddSkillSlotB(b, skillSlotBOffset)
	CharacterTranscendenceExcelAddSkillSlotC(b, skillSlotCOffset)
	CharacterTranscendenceExcelAddMaxlevelStar(b, maxlevelStarOffset)
	return CharacterTranscendenceExcelEnd(b)
}

// Marshal marshals the struct into a FlatBuffers buffer.
func (t *CharacterTranscendenceExcelDto) Marshal() ([]byte, error) {
	b := flatbuffers.NewBuilder(0)
	b.Finish(t.MarshalModel(b))
	return b.FinishedBytes(), nil
}

// UnmarshalMessage unmarshals the struct from a FlatBuffers table.
func (t *CharacterTranscendenceExcelDto) UnmarshalMessage(e *CharacterTranscendenceExcel) error {
	t.CharacterID = e.CharacterId()
	t.MaxFavorLevel = make([]int32, e.MaxFavorLevelLength())
	for i := 0; i < e.MaxFavorLevelLength(); i++ {
		t.MaxFavorLevel[i] = e.MaxFavorLevel(i)
	}
	t.StatBonusRateAttack = make([]int64, e.StatBonusRateAttackLength())
	for i := 0; i < e.StatBonusRateAttackLength(); i++ {
		t.StatBonusRateAttack[i] = e.StatBonusRateAttack(i)
	}
	t.StatBonusRateHp = make([]int64, e.StatBonusRateHpLength())
	for i := 0; i < e.StatBonusRateHpLength(); i++ {
		t.StatBonusRateHp[i] = e.StatBonusRateHp(i)
	}
	t.StatBonusRateHeal = make([]int64, e.StatBonusRateHealLength())
	for i := 0; i < e.StatBonusRateHealLength(); i++ {
		t.StatBonusRateHeal[i] = e.StatBonusRateHeal(i)
	}
	t.RecipeID = make([]int64, e.RecipeIdLength())
	for i := 0; i < e.RecipeIdLength(); i++ {
		t.RecipeID[i] = e.RecipeId(i)
	}
	t.SkillSlotA = make([]string, e.SkillSlotALength())
	for i := 0; i < e.SkillSlotALength(); i++ {
		t.SkillSlotA[i] = string(e.SkillSlotA(i))
	}
	t.SkillSlotB = make([]string, e.SkillSlotBLength())
	for i := 0; i < e.SkillSlotBLength(); i++ {
		t.SkillSlotB[i] = string(e.SkillSlotB(i))
	}
	t.SkillSlotC = make([]string, e.SkillSlotCLength())
	for i := 0; i < e.SkillSlotCLength(); i++ {
		t.SkillSlotC[i] = string(e.SkillSlotC(i))
	}
	t.MaxlevelStar = make([]int32, e.MaxlevelStarLength())
	for i := 0; i < e.MaxlevelStarLength(); i++ {
		t.MaxlevelStar[i] = e.MaxlevelStar(i)
	}
	return nil
}

// Unmarshal unmarshals the struct from a FlatBuffers buffer.
func (t *CharacterTranscendenceExcelDto) Unmarshal(data []byte) error {
	root := GetRootAsCharacterTranscendenceExcel(data, 0)
	return t.UnmarshalMessage(root)
}

// FlatDataName returns the FlatBuffers table name.
func (CharacterTranscendenceExcelDto) FlatDataName() string {
	return "CharacterTranscendenceExcel"
}
