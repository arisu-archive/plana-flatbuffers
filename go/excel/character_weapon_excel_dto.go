// Code generated by fbsgen. DO NOT EDIT.
// Package excel contains generated code for a FlatBuffers schema.
package excel

import (
	fbsutils "github.com/arisu-archive/bluearchive-fbs-utils"
	flatbuffers "github.com/google/flatbuffers/go"
)

// CharacterWeaponExcelDto represents a FlatBuffers table.
type CharacterWeaponExcelDto struct {
	fbsutils.FlatBuffer
	ID              int64                 `json:"id"`
	ImagePath       string                `json:"image_path"`
	SetRecipe       int64                 `json:"set_recipe"`
	StatLevelUpType StatLevelUpType       `json:"stat_level_up_type"`
	AttackPower     int64                 `json:"attack_power"`
	AttackPower100  int64                 `json:"attack_power100"`
	MaxHp           int64                 `json:"max_hp"`
	MaxHp100        int64                 `json:"max_hp100"`
	HealPower       int64                 `json:"heal_power"`
	HealPower100    int64                 `json:"heal_power100"`
	Unlock          []bool                `json:"unlock"`
	RecipeID        []int64               `json:"recipe_id"`
	MaxLevel        []int32               `json:"max_level"`
	LearnSkillSlot  []string              `json:"learn_skill_slot"`
	StatType        []EquipmentOptionType `json:"stat_type"`
	StatValue       []int64               `json:"stat_value"`
}

// MarshalModel marshals the struct into a FlatBuffers offset.
func (t *CharacterWeaponExcelDto) MarshalModel(b *flatbuffers.Builder) flatbuffers.UOffsetT {
	imagePathOffset := b.CreateString(t.ImagePath)
	CharacterWeaponExcelStartUnlockVector(b, len(t.Unlock))
	for i := len(t.Unlock) - 1; i >= 0; i-- {
		b.PrependBool(t.Unlock[i])
	}
	unlockOffset := b.EndVector(len(t.Unlock))
	CharacterWeaponExcelStartRecipeIdVector(b, len(t.RecipeID))
	for i := len(t.RecipeID) - 1; i >= 0; i-- {
		b.PrependInt64(t.RecipeID[i])
	}
	recipeIDOffset := b.EndVector(len(t.RecipeID))
	CharacterWeaponExcelStartMaxLevelVector(b, len(t.MaxLevel))
	for i := len(t.MaxLevel) - 1; i >= 0; i-- {
		b.PrependInt32(t.MaxLevel[i])
	}
	maxLevelOffset := b.EndVector(len(t.MaxLevel))
	var learnSkillSlotOffset flatbuffers.UOffsetT
	learnSkillSlotOffsets := make([]flatbuffers.UOffsetT, len(t.LearnSkillSlot))
	for i := range t.LearnSkillSlot {
		learnSkillSlotOffsets[i] = b.CreateString(t.LearnSkillSlot[i])
	}
	CharacterWeaponExcelStartLearnSkillSlotVector(b, len(t.LearnSkillSlot))
	for i := len(learnSkillSlotOffsets) - 1; i >= 0; i-- {
		b.PrependUOffsetT(learnSkillSlotOffsets[i])
	}
	learnSkillSlotOffset = b.EndVector(len(t.LearnSkillSlot))
	CharacterWeaponExcelStartStatTypeVector(b, len(t.StatType))
	for i := len(t.StatType) - 1; i >= 0; i-- {
		b.PrependInt32(int32(t.StatType[i]))
	}
	statTypeOffset := b.EndVector(len(t.StatType))
	CharacterWeaponExcelStartStatValueVector(b, len(t.StatValue))
	for i := len(t.StatValue) - 1; i >= 0; i-- {
		b.PrependInt64(t.StatValue[i])
	}
	statValueOffset := b.EndVector(len(t.StatValue))
	CharacterWeaponExcelStart(b)
	CharacterWeaponExcelAddId(b, t.ID)
	CharacterWeaponExcelAddImagePath(b, imagePathOffset)
	CharacterWeaponExcelAddSetRecipe(b, t.SetRecipe)
	CharacterWeaponExcelAddStatLevelUpType(b, t.StatLevelUpType)
	CharacterWeaponExcelAddAttackPower(b, t.AttackPower)
	CharacterWeaponExcelAddAttackPower100(b, t.AttackPower100)
	CharacterWeaponExcelAddMaxHp(b, t.MaxHp)
	CharacterWeaponExcelAddMaxHp100(b, t.MaxHp100)
	CharacterWeaponExcelAddHealPower(b, t.HealPower)
	CharacterWeaponExcelAddHealPower100(b, t.HealPower100)
	CharacterWeaponExcelAddUnlock(b, unlockOffset)
	CharacterWeaponExcelAddRecipeId(b, recipeIDOffset)
	CharacterWeaponExcelAddMaxLevel(b, maxLevelOffset)
	CharacterWeaponExcelAddLearnSkillSlot(b, learnSkillSlotOffset)
	CharacterWeaponExcelAddStatType(b, statTypeOffset)
	CharacterWeaponExcelAddStatValue(b, statValueOffset)
	return CharacterWeaponExcelEnd(b)
}

// Marshal marshals the struct into a FlatBuffers buffer.
func (t *CharacterWeaponExcelDto) Marshal() ([]byte, error) {
	b := flatbuffers.NewBuilder(0)
	b.Finish(t.MarshalModel(b))
	return b.FinishedBytes(), nil
}

// UnmarshalMessage unmarshals the struct from a FlatBuffers table.
func (t *CharacterWeaponExcelDto) UnmarshalMessage(e *CharacterWeaponExcel) error {
	t.ID = e.Id()
	t.ImagePath = string(e.ImagePath())
	t.SetRecipe = e.SetRecipe()
	t.StatLevelUpType = e.StatLevelUpType()
	t.AttackPower = e.AttackPower()
	t.AttackPower100 = e.AttackPower100()
	t.MaxHp = e.MaxHp()
	t.MaxHp100 = e.MaxHp100()
	t.HealPower = e.HealPower()
	t.HealPower100 = e.HealPower100()
	t.Unlock = make([]bool, e.UnlockLength())
	for i := 0; i < e.UnlockLength(); i++ {
		t.Unlock[i] = e.Unlock(i)
	}
	t.RecipeID = make([]int64, e.RecipeIdLength())
	for i := 0; i < e.RecipeIdLength(); i++ {
		t.RecipeID[i] = e.RecipeId(i)
	}
	t.MaxLevel = make([]int32, e.MaxLevelLength())
	for i := 0; i < e.MaxLevelLength(); i++ {
		t.MaxLevel[i] = e.MaxLevel(i)
	}
	t.LearnSkillSlot = make([]string, e.LearnSkillSlotLength())
	for i := 0; i < e.LearnSkillSlotLength(); i++ {
		t.LearnSkillSlot[i] = string(e.LearnSkillSlot(i))
	}
	t.StatType = make([]EquipmentOptionType, e.StatTypeLength())
	for i := 0; i < e.StatTypeLength(); i++ {
		t.StatType[i] = e.StatType(i)
	}
	t.StatValue = make([]int64, e.StatValueLength())
	for i := 0; i < e.StatValueLength(); i++ {
		t.StatValue[i] = e.StatValue(i)
	}
	return nil
}

// Unmarshal unmarshals the struct from a FlatBuffers buffer.
func (t *CharacterWeaponExcelDto) Unmarshal(data []byte) error {
	root := GetRootAsCharacterWeaponExcel(data, 0)
	return t.UnmarshalMessage(root)
}

// FlatDataName returns the FlatBuffers table name.
func (CharacterWeaponExcelDto) FlatDataName() string {
	return "CharacterWeaponExcel"
}
