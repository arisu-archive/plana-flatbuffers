// Code generated by fbsgen. DO NOT EDIT.
// Package excel contains generated code for a FlatBuffers schema.
package excel

import (
	fbsutils "github.com/arisu-archive/bluearchive-fbs-utils"
	flatbuffers "github.com/google/flatbuffers/go"
)

// VoiceTimelineExcelDto represents a FlatBuffers table.
type VoiceTimelineExcelDto struct {
	fbsutils.FlatBuffer
	UniqueID    int64     `json:"unique_id"`
	ID          uint32    `json:"id"`
	Nation      []Nation  `json:"nation"`
	Path        []string  `json:"path"`
	SoundVolume []float32 `json:"sound_volume"`
}

// MarshalModel marshals the struct into a FlatBuffers offset.
func (t *VoiceTimelineExcelDto) MarshalModel(b *flatbuffers.Builder) flatbuffers.UOffsetT {
	VoiceTimelineExcelStartNationVector(b, len(t.Nation))
	for i := len(t.Nation) - 1; i >= 0; i-- {
		b.PrependInt32(int32(t.Nation[i]))
	}
	nationOffset := b.EndVector(len(t.Nation))
	var pathOffset flatbuffers.UOffsetT
	pathOffsets := make([]flatbuffers.UOffsetT, len(t.Path))
	for i := range t.Path {
		pathOffsets[i] = b.CreateString(t.Path[i])
	}
	VoiceTimelineExcelStartPathVector(b, len(t.Path))
	for i := len(pathOffsets) - 1; i >= 0; i-- {
		b.PrependUOffsetT(pathOffsets[i])
	}
	pathOffset = b.EndVector(len(t.Path))
	VoiceTimelineExcelStartSoundVolumeVector(b, len(t.SoundVolume))
	for i := len(t.SoundVolume) - 1; i >= 0; i-- {
		b.PrependFloat32(t.SoundVolume[i])
	}
	soundVolumeOffset := b.EndVector(len(t.SoundVolume))
	VoiceTimelineExcelStart(b)
	VoiceTimelineExcelAddUniqueId(b, t.UniqueID)
	VoiceTimelineExcelAddId(b, t.ID)
	VoiceTimelineExcelAddNation(b, nationOffset)
	VoiceTimelineExcelAddPath(b, pathOffset)
	VoiceTimelineExcelAddSoundVolume(b, soundVolumeOffset)
	return VoiceTimelineExcelEnd(b)
}

// Marshal marshals the struct into a FlatBuffers buffer.
func (t *VoiceTimelineExcelDto) Marshal() ([]byte, error) {
	b := flatbuffers.NewBuilder(0)
	b.Finish(t.MarshalModel(b))
	return b.FinishedBytes(), nil
}

// UnmarshalMessage unmarshals the struct from a FlatBuffers table.
func (t *VoiceTimelineExcelDto) UnmarshalMessage(e *VoiceTimelineExcel) error {
	t.UniqueID = e.UniqueId()
	t.ID = e.Id()
	t.Nation = make([]Nation, e.NationLength())
	for i := 0; i < e.NationLength(); i++ {
		t.Nation[i] = e.Nation(i)
	}
	t.Path = make([]string, e.PathLength())
	for i := 0; i < e.PathLength(); i++ {
		t.Path[i] = string(e.Path(i))
	}
	t.SoundVolume = make([]float32, e.SoundVolumeLength())
	for i := 0; i < e.SoundVolumeLength(); i++ {
		t.SoundVolume[i] = e.SoundVolume(i)
	}
	return nil
}

// Unmarshal unmarshals the struct from a FlatBuffers buffer.
func (t *VoiceTimelineExcelDto) Unmarshal(data []byte) error {
	root := GetRootAsVoiceTimelineExcel(data, 0)
	return t.UnmarshalMessage(root)
}

// FlatDataName returns the FlatBuffers table name.
func (VoiceTimelineExcelDto) FlatDataName() string {
	return "VoiceTimelineExcel"
}
