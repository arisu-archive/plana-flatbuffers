// Code generated by fbsgen. DO NOT EDIT.
// Package excel contains generated code for a FlatBuffers schema.
package excel

import (
	fbsutils "github.com/arisu-archive/bluearchive-fbs-utils"
	flatbuffers "github.com/google/flatbuffers/go"
)

// VideoExcelDto represents a FlatBuffers table.
type VideoExcelDto struct {
	fbsutils.FlatBuffer
	ID          int64     `json:"id"`
	Nation      []Nation  `json:"nation"`
	VideoPath   []string  `json:"video_path"`
	SoundPath   []string  `json:"sound_path"`
	SoundVolume []float32 `json:"sound_volume"`
}

// MarshalModel marshals the struct into a FlatBuffers offset.
func (t *VideoExcelDto) MarshalModel(b *flatbuffers.Builder) flatbuffers.UOffsetT {
	VideoExcelStartNationVector(b, len(t.Nation))
	for i := len(t.Nation) - 1; i >= 0; i-- {
		b.PrependInt32(int32(t.Nation[i]))
	}
	nationOffset := b.EndVector(len(t.Nation))
	var videoPathOffset flatbuffers.UOffsetT
	videoPathOffsets := make([]flatbuffers.UOffsetT, len(t.VideoPath))
	for i := range t.VideoPath {
		videoPathOffsets[i] = b.CreateString(t.VideoPath[i])
	}
	VideoExcelStartVideoPathVector(b, len(t.VideoPath))
	for i := len(videoPathOffsets) - 1; i >= 0; i-- {
		b.PrependUOffsetT(videoPathOffsets[i])
	}
	videoPathOffset = b.EndVector(len(t.VideoPath))
	var soundPathOffset flatbuffers.UOffsetT
	soundPathOffsets := make([]flatbuffers.UOffsetT, len(t.SoundPath))
	for i := range t.SoundPath {
		soundPathOffsets[i] = b.CreateString(t.SoundPath[i])
	}
	VideoExcelStartSoundPathVector(b, len(t.SoundPath))
	for i := len(soundPathOffsets) - 1; i >= 0; i-- {
		b.PrependUOffsetT(soundPathOffsets[i])
	}
	soundPathOffset = b.EndVector(len(t.SoundPath))
	VideoExcelStartSoundVolumeVector(b, len(t.SoundVolume))
	for i := len(t.SoundVolume) - 1; i >= 0; i-- {
		b.PrependFloat32(t.SoundVolume[i])
	}
	soundVolumeOffset := b.EndVector(len(t.SoundVolume))
	VideoExcelStart(b)
	VideoExcelAddId(b, t.ID)
	VideoExcelAddNation(b, nationOffset)
	VideoExcelAddVideoPath(b, videoPathOffset)
	VideoExcelAddSoundPath(b, soundPathOffset)
	VideoExcelAddSoundVolume(b, soundVolumeOffset)
	return VideoExcelEnd(b)
}

// Marshal marshals the struct into a FlatBuffers buffer.
func (t *VideoExcelDto) Marshal() ([]byte, error) {
	b := flatbuffers.NewBuilder(0)
	b.Finish(t.MarshalModel(b))
	return b.FinishedBytes(), nil
}

// UnmarshalMessage unmarshals the struct from a FlatBuffers table.
func (t *VideoExcelDto) UnmarshalMessage(e *VideoExcel) error {
	t.ID = e.Id()
	t.Nation = make([]Nation, e.NationLength())
	for i := 0; i < e.NationLength(); i++ {
		t.Nation[i] = e.Nation(i)
	}
	t.VideoPath = make([]string, e.VideoPathLength())
	for i := 0; i < e.VideoPathLength(); i++ {
		t.VideoPath[i] = string(e.VideoPath(i))
	}
	t.SoundPath = make([]string, e.SoundPathLength())
	for i := 0; i < e.SoundPathLength(); i++ {
		t.SoundPath[i] = string(e.SoundPath(i))
	}
	t.SoundVolume = make([]float32, e.SoundVolumeLength())
	for i := 0; i < e.SoundVolumeLength(); i++ {
		t.SoundVolume[i] = e.SoundVolume(i)
	}
	return nil
}

// Unmarshal unmarshals the struct from a FlatBuffers buffer.
func (t *VideoExcelDto) Unmarshal(data []byte) error {
	root := GetRootAsVideoExcel(data, 0)
	return t.UnmarshalMessage(root)
}

// FlatDataName returns the FlatBuffers table name.
func (VideoExcelDto) FlatDataName() string {
	return "VideoExcel"
}
