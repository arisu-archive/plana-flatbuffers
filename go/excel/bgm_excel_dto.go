// Code generated by fbsgen. DO NOT EDIT.
// Package excel contains generated code for a FlatBuffers schema.
package excel

import (
	fbsutils "github.com/arisu-archive/bluearchive-fbs-utils"
	flatbuffers "github.com/google/flatbuffers/go"
)

// BGMExcelDto represents a FlatBuffers table.
type BGMExcelDto struct {
	fbsutils.FlatBuffer
	ID                int64     `json:"id"`
	Nation            []Nation  `json:"nation"`
	Path              []string  `json:"path"`
	Volume            []float32 `json:"volume"`
	LoopStartTime     []float32 `json:"loop_start_time"`
	LoopEndTime       []float32 `json:"loop_end_time"`
	LoopTranstionTime []float32 `json:"loop_transtion_time"`
	LoopOffsetTime    []float32 `json:"loop_offset_time"`
}

// MarshalModel marshals the struct into a FlatBuffers offset.
func (t *BGMExcelDto) MarshalModel(b *flatbuffers.Builder) flatbuffers.UOffsetT {
	BGMExcelStartNationVector(b, len(t.Nation))
	for i := len(t.Nation) - 1; i >= 0; i-- {
		b.PrependInt32(int32(t.Nation[i]))
	}
	nationOffset := b.EndVector(len(t.Nation))
	var pathOffset flatbuffers.UOffsetT
	pathOffsets := make([]flatbuffers.UOffsetT, len(t.Path))
	for i := range t.Path {
		pathOffsets[i] = b.CreateString(t.Path[i])
	}
	BGMExcelStartPathVector(b, len(t.Path))
	for i := len(pathOffsets) - 1; i >= 0; i-- {
		b.PrependUOffsetT(pathOffsets[i])
	}
	pathOffset = b.EndVector(len(t.Path))
	BGMExcelStartVolumeVector(b, len(t.Volume))
	for i := len(t.Volume) - 1; i >= 0; i-- {
		b.PrependFloat32(t.Volume[i])
	}
	volumeOffset := b.EndVector(len(t.Volume))
	BGMExcelStartLoopStartTimeVector(b, len(t.LoopStartTime))
	for i := len(t.LoopStartTime) - 1; i >= 0; i-- {
		b.PrependFloat32(t.LoopStartTime[i])
	}
	loopStartTimeOffset := b.EndVector(len(t.LoopStartTime))
	BGMExcelStartLoopEndTimeVector(b, len(t.LoopEndTime))
	for i := len(t.LoopEndTime) - 1; i >= 0; i-- {
		b.PrependFloat32(t.LoopEndTime[i])
	}
	loopEndTimeOffset := b.EndVector(len(t.LoopEndTime))
	BGMExcelStartLoopTranstionTimeVector(b, len(t.LoopTranstionTime))
	for i := len(t.LoopTranstionTime) - 1; i >= 0; i-- {
		b.PrependFloat32(t.LoopTranstionTime[i])
	}
	loopTranstionTimeOffset := b.EndVector(len(t.LoopTranstionTime))
	BGMExcelStartLoopOffsetTimeVector(b, len(t.LoopOffsetTime))
	for i := len(t.LoopOffsetTime) - 1; i >= 0; i-- {
		b.PrependFloat32(t.LoopOffsetTime[i])
	}
	loopOffsetTimeOffset := b.EndVector(len(t.LoopOffsetTime))
	BGMExcelStart(b)
	BGMExcelAddId(b, t.ID)
	BGMExcelAddNation(b, nationOffset)
	BGMExcelAddPath(b, pathOffset)
	BGMExcelAddVolume(b, volumeOffset)
	BGMExcelAddLoopStartTime(b, loopStartTimeOffset)
	BGMExcelAddLoopEndTime(b, loopEndTimeOffset)
	BGMExcelAddLoopTranstionTime(b, loopTranstionTimeOffset)
	BGMExcelAddLoopOffsetTime(b, loopOffsetTimeOffset)
	return BGMExcelEnd(b)
}

// Marshal marshals the struct into a FlatBuffers buffer.
func (t *BGMExcelDto) Marshal() ([]byte, error) {
	b := flatbuffers.NewBuilder(0)
	b.Finish(t.MarshalModel(b))
	return b.FinishedBytes(), nil
}

// UnmarshalMessage unmarshals the struct from a FlatBuffers table.
func (t *BGMExcelDto) UnmarshalMessage(e *BGMExcel) error {
	t.ID = e.Id()
	t.Nation = make([]Nation, e.NationLength())
	for i := 0; i < e.NationLength(); i++ {
		t.Nation[i] = e.Nation(i)
	}
	t.Path = make([]string, e.PathLength())
	for i := 0; i < e.PathLength(); i++ {
		t.Path[i] = string(e.Path(i))
	}
	t.Volume = make([]float32, e.VolumeLength())
	for i := 0; i < e.VolumeLength(); i++ {
		t.Volume[i] = e.Volume(i)
	}
	t.LoopStartTime = make([]float32, e.LoopStartTimeLength())
	for i := 0; i < e.LoopStartTimeLength(); i++ {
		t.LoopStartTime[i] = e.LoopStartTime(i)
	}
	t.LoopEndTime = make([]float32, e.LoopEndTimeLength())
	for i := 0; i < e.LoopEndTimeLength(); i++ {
		t.LoopEndTime[i] = e.LoopEndTime(i)
	}
	t.LoopTranstionTime = make([]float32, e.LoopTranstionTimeLength())
	for i := 0; i < e.LoopTranstionTimeLength(); i++ {
		t.LoopTranstionTime[i] = e.LoopTranstionTime(i)
	}
	t.LoopOffsetTime = make([]float32, e.LoopOffsetTimeLength())
	for i := 0; i < e.LoopOffsetTimeLength(); i++ {
		t.LoopOffsetTime[i] = e.LoopOffsetTime(i)
	}
	return nil
}

// Unmarshal unmarshals the struct from a FlatBuffers buffer.
func (t *BGMExcelDto) Unmarshal(data []byte) error {
	root := GetRootAsBGMExcel(data, 0)
	return t.UnmarshalMessage(root)
}

// FlatDataName returns the FlatBuffers table name.
func (BGMExcelDto) FlatDataName() string {
	return "BGMExcel"
}
