// Code generated by fbsgen. DO NOT EDIT.
// Package excel contains generated code for a FlatBuffers schema.
package excel

import (
	fbsutils "github.com/arisu-archive/bluearchive-fbs-utils"
	flatbuffers "github.com/google/flatbuffers/go"
)

// RecipeExcelDto represents a FlatBuffers table.
type RecipeExcelDto struct {
	fbsutils.FlatBuffer
	ID                     int64        `json:"id"`
	RecipeType             RecipeType   `json:"recipe_type"`
	RecipeIngredientID     int64        `json:"recipe_ingredient_id"`
	RecipeSelectionGroupID int64        `json:"recipe_selection_group_id"`
	ParcelType             []ParcelType `json:"parcel_type"`
	ParcelID               []int64      `json:"parcel_id"`
	ResultAmountMin        []int64      `json:"result_amount_min"`
	ResultAmountMax        []int64      `json:"result_amount_max"`
}

// MarshalModel marshals the struct into a FlatBuffers offset.
func (t *RecipeExcelDto) MarshalModel(b *flatbuffers.Builder) flatbuffers.UOffsetT {
	RecipeExcelStartParcelTypeVector(b, len(t.ParcelType))
	for i := len(t.ParcelType) - 1; i >= 0; i-- {
		b.PrependInt32(int32(t.ParcelType[i]))
	}
	parcelTypeOffset := b.EndVector(len(t.ParcelType))
	RecipeExcelStartParcelIdVector(b, len(t.ParcelID))
	for i := len(t.ParcelID) - 1; i >= 0; i-- {
		b.PrependInt64(t.ParcelID[i])
	}
	parcelIDOffset := b.EndVector(len(t.ParcelID))
	RecipeExcelStartResultAmountMinVector(b, len(t.ResultAmountMin))
	for i := len(t.ResultAmountMin) - 1; i >= 0; i-- {
		b.PrependInt64(t.ResultAmountMin[i])
	}
	resultAmountMinOffset := b.EndVector(len(t.ResultAmountMin))
	RecipeExcelStartResultAmountMaxVector(b, len(t.ResultAmountMax))
	for i := len(t.ResultAmountMax) - 1; i >= 0; i-- {
		b.PrependInt64(t.ResultAmountMax[i])
	}
	resultAmountMaxOffset := b.EndVector(len(t.ResultAmountMax))
	RecipeExcelStart(b)
	RecipeExcelAddId(b, t.ID)
	RecipeExcelAddRecipeType(b, t.RecipeType)
	RecipeExcelAddRecipeIngredientId(b, t.RecipeIngredientID)
	RecipeExcelAddRecipeSelectionGroupId(b, t.RecipeSelectionGroupID)
	RecipeExcelAddParcelType(b, parcelTypeOffset)
	RecipeExcelAddParcelId(b, parcelIDOffset)
	RecipeExcelAddResultAmountMin(b, resultAmountMinOffset)
	RecipeExcelAddResultAmountMax(b, resultAmountMaxOffset)
	return RecipeExcelEnd(b)
}

// Marshal marshals the struct into a FlatBuffers buffer.
func (t *RecipeExcelDto) Marshal() ([]byte, error) {
	b := flatbuffers.NewBuilder(0)
	b.Finish(t.MarshalModel(b))
	return b.FinishedBytes(), nil
}

// UnmarshalMessage unmarshals the struct from a FlatBuffers table.
func (t *RecipeExcelDto) UnmarshalMessage(e *RecipeExcel) error {
	t.ID = e.Id()
	t.RecipeType = e.RecipeType()
	t.RecipeIngredientID = e.RecipeIngredientId()
	t.RecipeSelectionGroupID = e.RecipeSelectionGroupId()
	t.ParcelType = make([]ParcelType, e.ParcelTypeLength())
	for i := 0; i < e.ParcelTypeLength(); i++ {
		t.ParcelType[i] = e.ParcelType(i)
	}
	t.ParcelID = make([]int64, e.ParcelIdLength())
	for i := 0; i < e.ParcelIdLength(); i++ {
		t.ParcelID[i] = e.ParcelId(i)
	}
	t.ResultAmountMin = make([]int64, e.ResultAmountMinLength())
	for i := 0; i < e.ResultAmountMinLength(); i++ {
		t.ResultAmountMin[i] = e.ResultAmountMin(i)
	}
	t.ResultAmountMax = make([]int64, e.ResultAmountMaxLength())
	for i := 0; i < e.ResultAmountMaxLength(); i++ {
		t.ResultAmountMax[i] = e.ResultAmountMax(i)
	}
	return nil
}

// Unmarshal unmarshals the struct from a FlatBuffers buffer.
func (t *RecipeExcelDto) Unmarshal(data []byte) error {
	root := GetRootAsRecipeExcel(data, 0)
	return t.UnmarshalMessage(root)
}

// FlatDataName returns the FlatBuffers table name.
func (RecipeExcelDto) FlatDataName() string {
	return "RecipeExcel"
}
