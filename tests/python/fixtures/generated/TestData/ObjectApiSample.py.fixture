# automatically generated by the FlatBuffers compiler, do not modify

# namespace: TestData

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class ObjectApiSample(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ObjectApiSample()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsObjectApiSample(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ObjectApiSample
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # ObjectApiSample
    def Count(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ObjectApiSample
    def Total(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return 0

    # ObjectApiSample
    def UnsignedCount(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # ObjectApiSample
    def BigCount(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint64Flags, o + self._tab.Pos)
        return 0

    # ObjectApiSample
    def SmallCount(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int8Flags, o + self._tab.Pos)
        return 0

    # ObjectApiSample
    def Ratio(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # ObjectApiSample
    def PreciseRatio(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # ObjectApiSample
    def Title(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # ObjectApiSample
    def Enabled(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # ObjectApiSample
    def Self(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # ObjectApiSample
    def Kind(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return 0

    # ObjectApiSample
    def Values(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Int32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # ObjectApiSample
    def ValuesAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Int32Flags, o)
        return 0

    # ObjectApiSample
    def ValuesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ObjectApiSample
    def ValuesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        return o == 0

    # ObjectApiSample
    def Labels(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # ObjectApiSample
    def LabelsLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ObjectApiSample
    def LabelsIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        return o == 0

    # ObjectApiSample
    def Child(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            from TestData.Child import Child
            obj = Child()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ObjectApiSample
    def Children(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            from TestData.Child import Child
            obj = Child()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # ObjectApiSample
    def ChildrenLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # ObjectApiSample
    def ChildrenIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(32))
        return o == 0

def ObjectApiSampleStart(builder):
    builder.StartObject(15)

def Start(builder):
    ObjectApiSampleStart(builder)

def ObjectApiSampleAddCount(builder, count):
    builder.PrependInt32Slot(0, count, 0)

def AddCount(builder, count):
    ObjectApiSampleAddCount(builder, count)

def ObjectApiSampleAddTotal(builder, total):
    builder.PrependInt64Slot(1, total, 0)

def AddTotal(builder, total):
    ObjectApiSampleAddTotal(builder, total)

def ObjectApiSampleAddUnsignedCount(builder, unsignedCount):
    builder.PrependUint32Slot(2, unsignedCount, 0)

def AddUnsignedCount(builder, unsignedCount):
    ObjectApiSampleAddUnsignedCount(builder, unsignedCount)

def ObjectApiSampleAddBigCount(builder, bigCount):
    builder.PrependUint64Slot(3, bigCount, 0)

def AddBigCount(builder, bigCount):
    ObjectApiSampleAddBigCount(builder, bigCount)

def ObjectApiSampleAddSmallCount(builder, smallCount):
    builder.PrependInt8Slot(4, smallCount, 0)

def AddSmallCount(builder, smallCount):
    ObjectApiSampleAddSmallCount(builder, smallCount)

def ObjectApiSampleAddRatio(builder, ratio):
    builder.PrependFloat32Slot(5, ratio, 0.0)

def AddRatio(builder, ratio):
    ObjectApiSampleAddRatio(builder, ratio)

def ObjectApiSampleAddPreciseRatio(builder, preciseRatio):
    builder.PrependFloat64Slot(6, preciseRatio, 0.0)

def AddPreciseRatio(builder, preciseRatio):
    ObjectApiSampleAddPreciseRatio(builder, preciseRatio)

def ObjectApiSampleAddTitle(builder, title):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(title), 0)

def AddTitle(builder, title):
    ObjectApiSampleAddTitle(builder, title)

def ObjectApiSampleAddEnabled(builder, enabled):
    builder.PrependBoolSlot(8, enabled, 0)

def AddEnabled(builder, enabled):
    ObjectApiSampleAddEnabled(builder, enabled)

def ObjectApiSampleAddSelf(builder, self):
    builder.PrependBoolSlot(9, self, 0)

def AddSelf(builder, self):
    ObjectApiSampleAddSelf(builder, self)

def ObjectApiSampleAddKind(builder, kind):
    builder.PrependInt32Slot(10, kind, 0)

def AddKind(builder, kind):
    ObjectApiSampleAddKind(builder, kind)

def ObjectApiSampleAddValues(builder, values):
    builder.PrependUOffsetTRelativeSlot(11, flatbuffers.number_types.UOffsetTFlags.py_type(values), 0)

def AddValues(builder, values):
    ObjectApiSampleAddValues(builder, values)

def ObjectApiSampleStartValuesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartValuesVector(builder, numElems):
    return ObjectApiSampleStartValuesVector(builder, numElems)

def ObjectApiSampleAddLabels(builder, labels):
    builder.PrependUOffsetTRelativeSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(labels), 0)

def AddLabels(builder, labels):
    ObjectApiSampleAddLabels(builder, labels)

def ObjectApiSampleStartLabelsVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartLabelsVector(builder, numElems):
    return ObjectApiSampleStartLabelsVector(builder, numElems)

def ObjectApiSampleAddChild(builder, child):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(child), 0)

def AddChild(builder, child):
    ObjectApiSampleAddChild(builder, child)

def ObjectApiSampleAddChildren(builder, children):
    builder.PrependUOffsetTRelativeSlot(14, flatbuffers.number_types.UOffsetTFlags.py_type(children), 0)

def AddChildren(builder, children):
    ObjectApiSampleAddChildren(builder, children)

def ObjectApiSampleStartChildrenVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartChildrenVector(builder, numElems):
    return ObjectApiSampleStartChildrenVector(builder, numElems)

def ObjectApiSampleEnd(builder):
    return builder.EndObject()

def End(builder):
    return ObjectApiSampleEnd(builder)

import TestData.Child
try:
    from typing import List, Optional
except:
    pass

class ObjectApiSampleT(object):

    # ObjectApiSampleT
    def __init__(
        self,
        count = 0,
        total = 0,
        unsignedCount = 0,
        bigCount = 0,
        smallCount = 0,
        ratio = 0.0,
        preciseRatio = 0.0,
        title = None,
        enabled = False,
        self = False,
        kind = 0,
        values = None,
        labels = None,
        child = None,
        children = None,
    ):
        self.count = count  # type: int
        self.total = total  # type: int
        self.unsignedCount = unsignedCount  # type: int
        self.bigCount = bigCount  # type: int
        self.smallCount = smallCount  # type: int
        self.ratio = ratio  # type: float
        self.preciseRatio = preciseRatio  # type: float
        self.title = title  # type: Optional[str]
        self.enabled = enabled  # type: bool
        self.self = self  # type: bool
        self.kind = kind  # type: int
        self.values = values  # type: Optional[List[int]]
        self.labels = labels  # type: Optional[List[Optional[str]]]
        self.child = child  # type: Optional[TestData.Child.ChildT]
        self.children = children  # type: Optional[List[TestData.Child.ChildT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        objectApiSample = ObjectApiSample()
        objectApiSample.Init(buf, pos)
        return cls.InitFromObj(objectApiSample)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, objectApiSample):
        x = ObjectApiSampleT()
        x._UnPack(objectApiSample)
        return x

    # ObjectApiSampleT
    def _UnPack(self, objectApiSample):
        if objectApiSample is None:
            return
        self.count = objectApiSample.Count()
        self.total = objectApiSample.Total()
        self.unsignedCount = objectApiSample.UnsignedCount()
        self.bigCount = objectApiSample.BigCount()
        self.smallCount = objectApiSample.SmallCount()
        self.ratio = objectApiSample.Ratio()
        self.preciseRatio = objectApiSample.PreciseRatio()
        self.title = objectApiSample.Title()
        self.enabled = objectApiSample.Enabled()
        self.self = objectApiSample.Self()
        self.kind = objectApiSample.Kind()
        if not objectApiSample.ValuesIsNone():
            if np is None:
                self.values = []
                for i in range(objectApiSample.ValuesLength()):
                    self.values.append(objectApiSample.Values(i))
            else:
                self.values = objectApiSample.ValuesAsNumpy()
        if not objectApiSample.LabelsIsNone():
            self.labels = []
            for i in range(objectApiSample.LabelsLength()):
                self.labels.append(objectApiSample.Labels(i))
        if objectApiSample.Child() is not None:
            self.child = TestData.Child.ChildT.InitFromObj(objectApiSample.Child())
        if not objectApiSample.ChildrenIsNone():
            self.children = []
            for i in range(objectApiSample.ChildrenLength()):
                if objectApiSample.Children(i) is None:
                    self.children.append(None)
                else:
                    child_ = TestData.Child.ChildT.InitFromObj(objectApiSample.Children(i))
                    self.children.append(child_)

    # ObjectApiSampleT
    def Pack(self, builder):
        if self.title is not None:
            title = builder.CreateString(self.title)
        if self.values is not None:
            if np is not None and type(self.values) is np.ndarray:
                values = builder.CreateNumpyVector(self.values)
            else:
                ObjectApiSampleStartValuesVector(builder, len(self.values))
                for i in reversed(range(len(self.values))):
                    builder.PrependInt32(self.values[i])
                values = builder.EndVector()
        if self.labels is not None:
            labelslist = []
            for i in range(len(self.labels)):
                labelslist.append(builder.CreateString(self.labels[i]))
            ObjectApiSampleStartLabelsVector(builder, len(self.labels))
            for i in reversed(range(len(self.labels))):
                builder.PrependUOffsetTRelative(labelslist[i])
            labels = builder.EndVector()
        if self.child is not None:
            child = self.child.Pack(builder)
        if self.children is not None:
            childrenlist = []
            for i in range(len(self.children)):
                childrenlist.append(self.children[i].Pack(builder))
            ObjectApiSampleStartChildrenVector(builder, len(self.children))
            for i in reversed(range(len(self.children))):
                builder.PrependUOffsetTRelative(childrenlist[i])
            children = builder.EndVector()
        ObjectApiSampleStart(builder)
        ObjectApiSampleAddCount(builder, self.count)
        ObjectApiSampleAddTotal(builder, self.total)
        ObjectApiSampleAddUnsignedCount(builder, self.unsignedCount)
        ObjectApiSampleAddBigCount(builder, self.bigCount)
        ObjectApiSampleAddSmallCount(builder, self.smallCount)
        ObjectApiSampleAddRatio(builder, self.ratio)
        ObjectApiSampleAddPreciseRatio(builder, self.preciseRatio)
        if self.title is not None:
            ObjectApiSampleAddTitle(builder, title)
        ObjectApiSampleAddEnabled(builder, self.enabled)
        ObjectApiSampleAddSelf(builder, self.self)
        ObjectApiSampleAddKind(builder, self.kind)
        if self.values is not None:
            ObjectApiSampleAddValues(builder, values)
        if self.labels is not None:
            ObjectApiSampleAddLabels(builder, labels)
        if self.child is not None:
            ObjectApiSampleAddChild(builder, child)
        if self.children is not None:
            ObjectApiSampleAddChildren(builder, children)
        objectApiSample = ObjectApiSampleEnd(builder)
        return objectApiSample
