include "ParcelType.fbs";

namespace MX.Data.Excel;

table SeasonRecordRewardExcel {
    group_id: long;
    day: int;
    reward_parcel_type: [ParcelType];
    reward_parcel_id: [long];
    reward_amount: [long];
}

root_type SeasonRecordRewardExcel;