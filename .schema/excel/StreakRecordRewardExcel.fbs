include "ParcelType.fbs";

namespace MX.Data.Excel;

table StreakRecordRewardExcel {
    group_id: long;
    step: int;
    streak_day: int;
    reward_parcel_type: [ParcelType];
    reward_parcel_id: [long];
    reward_parcel_amount: [long];
}

root_type StreakRecordRewardExcel;